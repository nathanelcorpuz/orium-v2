# Orium — Product Spec

**The single source of truth.** Product definition, data model, engine rules, and the active roadmap live here. Working rules for Claude live in CLAUDE.md; open bugs in BUGS.md; build write-ups for finished tasks in ARCHIVE.md (indexed at the bottom of this file); the production-safety and staging plan in STAGING.md. (This file absorbed and replaced SPEC2.md in July 2026; task numbers are continuous across both.)

*Split 2026-07-31: this file had reached ~318KB, almost entirely completed-task write-ups, which made a file that must be read before every session too large to read in one pass. The history moved to ARCHIVE.md verbatim and nothing was reworded. Finished tasks are still findable here through the index in "Completed work" at the bottom.*

## What Orium is

Orium is a family cash-flow forecasting app. Users manually enter their account balances, recurring bills, income, debt payments, savings goals, one-off expenses, and spending budgets. Orium projects every upcoming transaction in chronological order and shows the **running future balance** at each date, color-coded by safety level. The core promise: *know exactly how much money you'll have on any future date, and never miss a bill.*

v1 (shipped) rebuilt the original Orium (github.com/nathanelcorpuz/orium) on a cleaner engine. The current work replaces its fixed 4-frequency recurrence with calendar-style rules (Phase 6A), restyles everything Notion-like (Phase 7), and adds Forecast/Dashboard clarity features (Phase 8/9). Budgets converted to replenishing cycles in Phase 6B, then — after that model proved confusing in practice — got rebuilt again as a simple running ledger in Phase 10 (in progress); see "Budgets v3" below for why.

## Core purpose (user, 2026-07-26)
**Orium exists to show you how much money you'll have at any point in the future.** Everything else - accounts, bills, income, debt, savings, budgets, misc, settling - is machinery serving that one question; the forecast's running balance *is* the product, not one page among several. User-facing copy should lead with that promise (a future balance you can point at) rather than generic budgeting or expense-tracking language, and settling should be explained as what keeps that number honest. Applied to the tour's opening/forecast steps and the welcome modal in T120.

## Writing style
- **No long em dashes (—) anywhere** - not in UI copy, seed/sample data, commit messages, or this file's own prose (code comments in this file are the one exception, to keep entries readable). Use a plain hyphen `-` instead. (User rule, 2026-07-25/26; enforced retroactively in seed data by T109, going forward by T113.) **Automatically enforced** by `src/lib/writingStyle.test.ts` (T113), which fails `npm run test` naming the exact file and line. That check covers `src/` and the re-runnable `supabase/*.sql` scripts; `supabase/migrations/` is deliberately outside it (an applied migration records what really ran, so it isn't rewritten to satisfy a later style rule), as are this file and the other project docs, whose historical entries still contain them.

## Tech stack (fixed — do not add alternatives)

- Next.js (App Router) + TypeScript + Tailwind CSS
- Supabase: Postgres, Auth (email/password), Row Level Security
- `@supabase/supabase-js` + `@supabase/ssr` per official Next.js patterns
- Vitest for unit tests (engine only)
- No other dependencies without asking the user first

## Core design rules (non-negotiable)

1. **Money is integer centavos.** Never floats. `₱1,500.00` = `150000`. Format only at the UI layer. Default currency `₱`, user-configurable.
2. **Due dates are calendar dates** — `YYYY-MM-DD` strings / Postgres `date`. Never UTC timestamps for due dates.
3. **Rules, not pre-generated rows.** Recurrence is stored as rules; occurrences are computed on the fly.
4. **The forecast engine is pure TypeScript** in `src/lib/engine/` — no database or network calls, fully unit-tested.
5. **Every table has `user_id`** with owner-only RLS.
6. **Make sure any edge cases are handled properly when fixing bugs or adding/editing new features.**

## Data model (Postgres — migrations in `supabase/migrations/`)

### `balances`
Real money accounts. `id, user_id, name, amount (bigint centavos), comments, created_at, updated_at`. Plus (T172, migration 0032): `transaction_fee_centavos (bigint >= 0, default 0)` - a flat cost auto-deducted from every forecasted transaction connected to this account, both incoming and outgoing.

### `recurring_items`
One row per recurring rule (bills, income, debt, savings). Amount sign convention: income positive; bills/debt/savings negative (DB-enforced).

Identity: `id, user_id, name, type (bill|income|debt|savings), amount, comments`.

**Recurrence rule (new model, live from migration 0004):**
| column | type | notes |
|---|---|---|
| `interval` | int ≥ 1 | repeat every N units |
| `unit` | enum | `day` \| `week` \| `month` \| `year` |
| `weekdays` | int[] nullable | 0=Sun…6=Sat; required when unit=week (multi-select) |
| `days_of_month` | int[] nullable | 1–31 for unit=month; days beyond a month's length clamp to its last day |
| `ordinal` + `ordinal_weekday` | int nullable pair | nth-weekday monthly rules: ordinal 1–4 or −1 (last); (3,2)=third Tuesday, (−1,5)=last Friday. unit=month uses **either** `days_of_month` **or** the ordinal pair |
| `start_date` | date | anchor; first possible occurrence |
| `ends_type` | enum | `never` \| `on_date` \| `after_count` (`never` stays available except for Debt/Savings, T72 — as of T85, Phase 12, it's implicitly bounded by the forecast horizon, which is itself capped at `MAX_TRACKING_YEARS` from today; an explicit `on_date`/`after_count` end is separately rejected app-level if it would resolve past that same cap, no DB constraint) |
| `end_date` | date nullable | set iff ends_type=on_date |
| `occurrence_count` | int nullable | set iff ends_type=after_count |

**Legacy columns `frequency`/`day_of_month`/`weekday` are gone** — dropped by migration 0005 (2026-07-21) once T35 shipped. Backfill mapping used to populate the new columns before the drop: monthly → (1, month, days=[day_of_month, falling back to start_date's day]) · weekly → (1, week) and biweekly → (2, week), both with weekdays=[dow(start_date)] — **not** the legacy `weekday` column, which the v1 engine never read and which may have been null or disagreed with the real schedule · semi_monthly_15_30 → (1, month, days=[15,30]); existing rows got `ends_type='on_date'` + their then-current end_date.

### `occurrence_overrides`
Per-instance edits to a recurring rule (calendar-exception style). `id, user_id, recurring_item_id (fk), original_date, new_date, new_amount, new_name, skipped (bool)`. Unique on (`recurring_item_id`, `original_date`).

### `one_off_items`
"Extras" — single dated transactions. `id, user_id, name, amount (signed), due_date, comments`.

### `settlements`
"History" — what actually happened, written when the user settles an occurrence or logs a budget spend. `id, user_id, source_type (recurring|one_off|budget), source_id, name, type (bill|income|debt|savings|extra|budget), forecasted_amount, actual_amount, forecasted_date, actual_date, forecasted_balance`. Settling a recurring occurrence also writes a `skipped` override so it leaves the forecast.

### `budgets` (T36 columns live; app still on the pre-6B baseline until T37/T38)
`id, user_id, name, created_at`, plus (still pre-6B, current app): `monthly_allocation (bigint ≥ 0)`. Plus (T36, migration 0006, added 2026-07-21): `allocation (bigint ≥ 0, NOT NULL, backfilled 1:1 from monthly_allocation)`, `carryover_enabled (bool, default true)`, `linked_income_id (uuid nullable, REFERENCES recurring_items ON DELETE SET NULL — app-level rule: must point at a type=income item, not DB-enforced)`, and the full recurrence rule shape (all nullable — `start_date, interval, unit, weekdays, days_of_month, ordinal, ordinal_weekday, ends_type, end_date, occurrence_count`; `interval`/`unit`/`start_date`/`ends_type` are constrained to move together as a complete-or-nothing group). `monthly_allocation` is dropped by migration 0007 once T38 ships and the app no longer reads it — same additive-then-drop pattern as 0004/0005.

### `budget_entries`
The budget ledger. `id, user_id, budget_id (fk cascade), entry_date, amount (bigint > 0, always a positive magnitude), note, created_at`. Unchanged by 6B. Plus (Phase 10, migration 0009, added 2026-07-23): `direction (text, 'incoming' | 'outgoing', not null, no default — every insert must say which)`. Existing rows backfilled `'outgoing'` (they're all past logged spends, so their meaning is unchanged).

### `reminders`
`id, user_id, text, created_at`.

### `preferences`
One row per user, created on signup. `user_id pk, currency (default '₱'), balance_ranges (bigint[] — 5 ascending centavo thresholds: [danger, low, medium, high, higher]; balance ≤ ranges[0] = danger, above ranges[4] = highest)`. Plus (T174, migration 0033): `active_scenario_id (uuid nullable, references scenarios, ON DELETE SET NULL)` - the scenario currently merged into the Forecast/Dashboard, if any.

### `scenarios`, `scenario_recurring_items`, `scenario_one_off_items` (T174, migration 0033)
A "what-if" scenario is a named container (`scenarios`: `id, user_id, name, created_at`) for hypothetical bills/income/debt/savings (`scenario_recurring_items`) and one-offs (`scenario_one_off_items`) - genuinely separate tables mirroring `recurring_items`/`one_off_items`'s own columns and constraints exactly, not a shared `scenario_id` tag column on the real tables (see the Roadmap write-up for why). Deleting a scenario cascades to both child tables. Never touched by any existing query in the app - the only integration point is `loadForecast()`, which merges the active scenario's rows into its own arrays when `preferences.active_scenario_id` is set.

## The forecast engine (`src/lib/engine/`)

Pure functions. `generateForecast({ balances, recurringItems, overrides, oneOffs, budgets?, budgetEntries?, today, horizon }) → ForecastRow[]` where each row is `{ sourceType, sourceId, originalDate, name, amount, dueDate, type, runningBalance }`.

Pipeline: expand each recurring rule from `max(today, start_date)` to the rule's end (capped at horizon) → apply overrides (move/change/skip) → merge one-offs dated today or later → merge budget rows (below) → sort by due date, then incoming-before-outgoing within a date (stable beyond that) → running balance = sum of `balances.amount` plus cumulative signed amounts, always integer centavos. Occurrences strictly before `today` are excluded (they belong in settlements).

**Same-day ordering (T148):** rows sharing a `dueDate` put incoming amounts (`amount >= 0`) ahead of outgoing ones, so a salary landing on the 5th is credited before a bill due the same day is deducted - the running balance never shows a dip that wouldn't really happen. Rows with the same date *and* the same sign keep insertion order (`Array.prototype.sort` is stable from ES2019).

**Past-due occurrences (T150, replaces the old "strictly before today" exclusion):** an occurrence whose date has passed and which was never settled **stays in the forecast** and keeps counting against the running balance. It carries `pastDue: true` (omitted, not false, for ordinary rows). Recurring rules expand from their own `start_date` rather than from `today`, and past-dated one-offs are no longer skipped. The lookback is **unbounded** - the user chose this explicitly over a fixed window (2026-07-31) - and stays sane because settling writes a `skipped` override (recurring) or deletes the row outright (one-offs), so anything still showing is genuinely outstanding. Past-due is judged on a row's *effective* date, so moving an occurrence forward with an override clears it.

The backlog runs through the same cumulative running balance as everything else, before today's rows: the account balances the user maintains are what they hold *now*, and an unsettled obligation has not left the account yet. `splitPastDue(rows, startingBalance)` divides the two and reports `balanceAfterPastDue`; forward-looking stats (lowest balance ahead, first danger point, Peaks and Drops) take `upcoming` plus that balance, so a past-due dip is never mistaken for a future event while the numbers still start from a realistic position.

Two surfaces deliberately opt out. The Debt and Savings pages filter past-due out of their per-item "upcoming" lists, since a missed payment from months ago showing as the *next* one would read as a bug. The preview-mode sample fixture (`sampleFixture.ts`) drops past-due entirely: it backdates every rule 180 days so the recurrence summaries look established, nothing in preview can be settled, and the new-user tour would otherwise open on ~60 red rows.

**Known follow-up:** `seed_sample_data` (migration 0016) uses hardcoded January 2026 dates, so an account that restores sample data now shows about seven months of past-due. It is reached only through Settings > Restore sample data (typing "RESTORE"), never automatically at signup, so this is a wart rather than a blocker - and the hardcoded dates were already going stale on their own. Tracked as **T169**.

### Recurrence expansion (target semantics, T33–T34)
- **day**: start_date, then every `interval` days.
- **week**: weeks anchored to the week containing start_date; every `interval` weeks emit each selected weekday; skip dates before start_date.
- **month**: months anchored to start_date's month, stepping by `interval`; emit each clamped day in `days_of_month` (day > month length → last day of month), or the resolved nth-weekday date (`ordinal` 1–4 or −1=last + `ordinal_weekday`).
- **year**: start_date's month/day every `interval` years; Feb 29 → Feb 28 in non-leap years.
- **ends**: `never` → generate to horizon; `on_date` inclusive; `after_count` → stop after N emitted occurrences.
- `ends_type=never` items are excluded from finite "remaining total" stats (e.g. total remaining debt).

The old fixed 4-frequency expansion (monthly/weekly/biweekly/semi_monthly_15_30) is gone — removed along with its columns by migration 0005 once T35 shipped. Every `recurring_items` row now uses this rule shape exclusively; `RecurringItem` has no legacy fields.

### Monthly-equivalent summary stat
Summary cards ("Total Monthly Income/Bills") use integer multipliers, never fractional math (a displayed `86,666.7` means float leakage — bug). Generalized form: `occurrencesPerMonth = round(f)` where f = day: 30/interval · week: (4 × len(weekdays))/interval · month: (len(days_of_month) or 1)/interval · year: 1/(12 × interval); minimum 0. `monthlyEquivalent = amount × occurrencesPerMonth` (integer × integer). Old presets yield ×4 (weekly), ×2 (biweekly), ×2 (semi-monthly), ×1 (monthly). The *forecast* keeps real occurrence dates (a 5-Saturday month genuinely shows 5 incomes) — only summary stats use multipliers.

### Budgets v2 engine (T37, superseded by Budgets v3 below as of T57 — `budgetCycles.ts` and this whole model are deleted; kept here only as a historical record of how it worked)
A budget replenishes at each **cycle boundary**; everything derives from logged spends — no manual resets. `budgets.ts`'s T24/T25 monthly-only functions (`currentMonthBudgetStatus`, `expandBudgetOccurrences`) are kept exactly as they were rather than rewritten in place — they're still what the currently-deployed Budgets page calls, and per the T35→0005 incident this session isn't taking chances with a caller it can't fully verify mid-task. T38/T39 do the actual cutover once the UI can produce v2 data; `budgets.ts` gets deleted then, mirroring how `monthly.ts`/`interval.ts`/`semi-monthly.ts` were deleted only once nothing called them.

**Boundary source:** linked income set → that income's *effective* occurrence dates (after overrides — moved dates move the boundary; skipped occurrences produce no reset, the cycle extends). Else own recurrence set → its occurrences. Else (e.g. linked income deleted) → fallback monthly on the 1st + a "needs a schedule" badge in the UI. After the final boundary, the last cycle extends to the horizon.

**Cycle math:** cycle k spans [boundaryₖ, boundaryₖ₊₁). Entries belong to the cycle containing entry_date; boundary-date entries belong to the **new** cycle; entries before the first boundary count toward the first cycle. `available₀ = allocation`; `availableₖ = allocation + (carryover_enabled ? availableₖ₋₁ − spentₖ₋₁ : 0)` — carryover may be negative. `remaining = max(available_current − spent_current, 0)`; `over = max(spent_current − available_current, 0)`.

**Forecast integration:** current cycle → one row per budget, "{name} — remaining this cycle", amount −remaining, dated today, omitted when remaining = 0. Each future boundary within the horizon → "{name} — allocation", amount −allocation, dated the boundary. Budget rows are type `budget` (teal), never settleable (logging a spend covers that). The current-cycle row also stays uneditable — it's a live status snapshot always dated today, not a discrete future transaction. Future boundary rows *are* editable (T42): move date, change amount, or skip, via `budget_occurrence_overrides` (keyed `budget_id`+`original_date`, mirroring `occurrence_overrides`), applied in `forecast.ts`'s merge loop the same way recurring-item overrides are.

**Future-dated spends (T43):** an entry dated after today counts toward `spent`/`remaining`/`over` for whichever cycle it actually falls in — current or a later one — everywhere those appear (Budgets page, Dashboard card, current-cycle forecast row); the entries list and the totals now always agree (was Bug #3). `computeBudgetCycleStatus` gained `currentCycleEnd` (the next boundary, resolved via `horizon` — defaulted to a ~1yr lookahead for callers with no real forecast horizon, e.g. `BudgetCard.tsx`/`BudgetsPanel.tsx`/the Dashboard card; `expandBudgetCycleOccurrences` passes the real one) so a future-dated entry can be bounded to the correct cycle instead of leaking into whichever one is "current". Each future-dated entry also renders as its own Forecast row (sourceType `budget_entry`, sourceId = the entry's own id) on its `entry_date` — name `"{budget} - {note}"` matching the settlement-naming convention, amount −entry amount — so the balance dips on the day the money actually leaves. Whichever boundary row (current-cycle "remaining" or a future "allocation") the entry would otherwise have inflated is reduced by exactly its amount (floored at 0) so the two never double-count: `futureBudgetEntries(entries, budgetId, today)` in `budgetCycles.ts` is the shared helper both `expandBudgetCycleOccurrences` (for the subtraction) and `forecast.ts` (for the standalone rows) use. `budget_entry` rows are editable/deletable directly from the Forecast (user decision 2026-07-21, consistent with T42's boundary-row editability) — `EditSettleModal` gets an amount+date+note form reusing the *existing* `updateBudgetEntry`/`deleteBudgetEntry` actions from `budgets/actions.ts` rather than new ones. `BudgetCard.tsx`'s current-cycle entries list is now bounded above by `currentCycleEnd` too — previously unbounded, so an entry from a *further* future cycle used to leak into the wrong card even though the totals never counted it (part of the Bug #3 root cause).

**Logging a spend** writes a `budget_entries` row AND a settlement (source_type `budget`, type `budget`, name = budget name + note, actual_amount = −amount, actual_date = entry_date, forecasted fields mirror actuals, forecasted_balance = 0). History renders budget rows with a "budget" tag, no forecast-vs-actual comparison.

### Budgets v3 — running ledger (Phase 10, live as of T57 — this is the current, only budget model)
User feedback 2026-07-23: the cycle-boundary model above was confusing in the Forecast table (reservation rows that aren't real transactions) and didn't match how people actually think about a budget — a pool of money, not a recurring reset. Rebuilt as a simple ledger in `budgetLedger.ts` (T54), staged in alongside the untouched `budgetCycles.ts` at first (same "build alongside, cut over once the UI can produce/consume it" pattern T37 used for `budgets.ts`) and fully cut over by T57, which deleted `budgetCycles.ts` and everything that depended on it.

**The model:** no cycle boundaries, allocation targets, or carryover math. Every `budget_entries` row is `'incoming'` (a replenishment) or `'outgoing'` (a logged spend, or a manual take) against a budget's running total. `computeBudgetBalance(entries, budgetId, asOf) = Σ incoming − Σ outgoing` for every entry dated `asOf` or earlier — carryover is implicit (nothing ever resets) and overspending is allowed to go negative, no clamping, exactly like a real account. A budget optionally links to an income (`linked_income_id`) with an `allocation` amount (how much gets added when it replenishes); **replenishment fires when that linked income's occurrence is actually settled** (T56) — an `'incoming'` entry for `allocation` gets written then, dated at the actual settle date, not a projected one. A budget with no linked income instead gets manual "Add funds"/"Take funds" actions (T55), each just another `'incoming'`/`'outgoing'` entry. There's no "replenishes on its own schedule" option anymore — every non-income budget is manual. `carryover_enabled` and the own-schedule recurrence columns are gone from `budgets` entirely (migration 0010, T57).

**Forecast integration:** budget rows in the Forecast are *only* future-dated ledger entries (`futureBudgetLedgerEntries` — same shape T43 established for future spends, now covering incoming entries too, e.g. a future-dated manual add or a future replenishment), never a projected boundary/reservation row — there's nothing to project, since the running total simply *is* whatever's been entered so far. No `budget_occurrence_overrides`, no uneditable "remaining this cycle" row, no T45 reservation-badge/grouping logic (all gone with the boundary rows they existed to soften). The Forecast sidebar's `BudgetsPanel` is gone — budget status and quick actions live on the Budgets page (T55) instead.

**Logging a spend / adding / taking funds** all write a `budget_entries` row *and* a matching settlement (`actual_amount` sign follows direction — incoming positive, outgoing negative) so History keeps a full record regardless of which kind of entry it is.

### Peaks and Drops
One row per year in the horizon, one column per calendar month (Jan–Dec); each cell shows that month's max (peak) and min (drop) running balance, color-coded by `balance_ranges`.

### Required test coverage (Vitest, all must stay green)
v1 core: day-31 monthly across Feb/Apr (clamping) · leap-year Feb 29 · semi-monthly across February · biweekly anchored mid-week across a month boundary · overrides move/change/skip · running balance with mixed signs · end_date cutoff and future start_date.
6A (T33–T34): every-2-weeks multi-weekday · days=[15,30] in February · interval-3 months · after_count · never→horizon · nth-weekday incl. last-X · generalized monthlyEquivalent.
6B (T37): linked-income boundaries incl. moved + skipped occurrence · own-schedule weekly cycles · fallback · income-ends extension · carryover on/off/negative · remaining/over · boundary-date entry · integer math.
6B (T43): future entry within the current cycle counts toward spent/remaining · a future entry in a LATER cycle doesn't leak into the current one · currentCycleEnd correctness (incl. null when the schedule ends) · a future boundary row reduced by known future spend in that specific cycle, floored at 0 · a future-dated entry renders as its own Forecast row without double-counting the running balance · a future-cycle entry reduces that boundary's row while still appearing as its own row.

## Pages & features

All pages require auth; unauthenticated users go to `/login`. Type colors: income green, debt orange, savings blue, extra purple, budget teal, bill default text.

1. **Auth**: sign up (email verification), log in, log out, password reset.
2. **Dashboard** (`/`): Total Balance, Total Monthly Bills, Total Monthly Income cards; per-balance breakdown; Remaining Debt + debt-free date + days until; a "Budgets" card (name + running balance, red when negative — T57); Peaks and Drops grid. Greeting shows profile name, else email local part.
3. **Forecast** (`/forecast`): total balance + editable balance chips; the full occurrence list color-coded by `balance_ranges` (danger = dark, low = red tint, medium = white, high→highest = deepening green); every row is clickable, opening Edit/Settle for recurring/one-off rows or a direct amount/date/note edit for a budget ledger entry (T57 — every budget row is now a real transaction, never a projection). Right sidebar: just Reminders CRUD (the old Budgets panel moved to the Budgets page itself, T55).
4. **Balances / Bills / Income / Debt / Savings / Extras** (`/balances`, `/bills`, `/income`, `/debt`, `/savings`, `/extra`): CRUD pages; each shows its summary total; all four recurring forms use the shared recurrence picker with a human-readable rule summary per row ("Every 2 weeks on Sat · until Apr 2030").
5. **Budgets** (`/budgets`): CRUD + a running balance per budget (red when negative) + a month-filterable entries list + Log spend, plus Add funds/Take funds for budgets not linked to an income (T55, Phase 10).
6. **History** (`/history`): settlements table — forecasted vs actual amount/date, forecasted balance, type (budget rows shown as a pill tag).
7. **Scenarios** (`/scenarios`, `/scenarios/[id]`, T174): create/rename/delete named "what-if" scenarios; turn one on and its hypothetical bills/income/debt/savings/misc items merge into the Forecast and Dashboard (an amber banner names the active one everywhere it applies); turn it off and they don't. "Activate permanently" copies a scenario's items into real data and deletes the scenario - irreversible, typed confirmation required. Only one scenario can be active at a time; scenario rows are never clickable on the Forecast or Calendar (edit them from the scenario's own page instead).
8. **Calendar** (`/calendar`, T164): a month grid over the same forecast data - each day shows a count badge when it has forecast transactions, click to list them, click a listed row to open the same Edit/Settle modal the Forecast page uses. A green-accented panel alongside the grid lists reminders (which have no date of their own, so they aren't plotted on specific days).
9. **Updates** (`/updates`, T163): a reverse-chronological, day-grouped feed over `activity_log` (T162) - every create/edit/delete/settle/toggle anywhere in the app, so a couple sharing one account can see what changed since they last looked. Entries newer than the viewer's own `activity_log_seen_at` watermark are marked new; opening the page updates that watermark for next time. The sidebar nav link carries an unseen-count badge.
10. **Settings** (`/settings`): profile, preferences (currency, balance ranges), log out, delete account (removes all user data).

### Recurrence picker (shared component, T35)
Used by Bills, Income, Debt, Savings, Budgets. Select with contextual presets computed from the chosen start date — "Monthly on the 21st", "Weekly on Tuesday", "Every 2 weeks on Tuesday", "Every 15th and 30th", "Monthly on the third Tuesday", "Custom…". Custom panel: Repeat every [N] [unit]; weekday chips (week); day list or nth-weekday (month); Ends: Never / On [date] / After [N] occurrences.

### Phase 7 — Notion-style redesign (restyle only, no behavior changes)
Notion palette (`#37352F` text, `#E9E9E7` hairlines, `#2383E2` accent, soft pill backgrounds), Inter, 14px base, full-width shell with 240px sidebar, tables with hairline dividers and hover `#F1F1EF`. Budget teal `#0B6E99`. Peaks and Drops keeps the v1 year×month pill grid.

**Edited-occurrence indicator** (added 2026-07-21, user request after verifying T42): any Forecast row whose occurrence differs from its rule — i.e. it has an `occurrence_overrides` or `budget_occurrence_overrides` row with a moved date or changed amount/name (skips never render, so they need no indicator) — shows a small icon next to the row name (pencil glyph or similar, muted slate, with a `title` tooltip like "Edited from its usual schedule"). Requires the forecast pipeline to expose an `edited` flag on `ForecastRow` at the merge layer where overrides are applied. Implemented as part of T29's Forecast restyle.

**Date display format**: replace every raw `YYYY-MM-DD` shown to the user with a human-readable format — full dates as "Jun 1, 2025" (Month Day, Year), month-only dates (e.g. a recurrence rule's "until" suffix, a debt-free date rounded to the month) as "Jun 2025" (Month Year). Storage/computation stays `YYYY-MM-DD` everywhere per the core design rules — this is purely a UI formatting change, which is why it's scoped to Phase 7 rather than done ad hoc. `recurrenceSummary.ts`'s internal `formatMonthYear` already produces the "Jun 2025" style; export it (or a shared equivalent) for reuse instead of duplicating. Applies wherever T28–T31 touch a page showing dates: Bills/Income/Debt/Savings row summaries and start/end dates, Forecast due dates, History forecasted/actual dates, Dashboard's debt-free date.

## Operations

- **`npm run build` is always run by Claude, never the user** (2026-07-24 — mirrored in CLAUDE.md's session workflow). If a dev server is already active on :3000, Claude stops it, runs the build, then restarts the dev server afterward rather than asking the user to run it themselves.
- **Migrations are written as files** in `supabase/migrations/` and applied by Claude directly via the connected Supabase MCP integration (falls back to the user pasting into the SQL editor if that connection isn't available in a session — see CLAUDE.md "Hard rules"). Back up first via `pg_dump` only when real data is at stake.
- **The recurrence migration was split across two files**, each paste-and-run whole: `0004_recurrence_rules.sql` (add columns + backfill, non-destructive) and `0005_recurrence_drop_legacy_after_t35.sql` (enforce NOT NULL + drop legacy columns). Both are applied — 0004 on 2026-07-21, 0005 the same day once T35 was live in production.
- **Sample data**: `supabase/seed.sql` fills every feature with a realistic family dataset (run after 0004; re-runnable; all seed rows share the id prefix `00000000-0000-4000-a000-` for easy wiping).
- **`/api/dev-login` is currently disabled** (noted 2026-07-31): `DEV_LOGIN_EMAIL` is present but empty in `.env.local`, and the route 404s unless *both* it and `DEV_LOGIN_PASSWORD` have values. This silently blocks browser verification in every session - the route just returns "Not found" with no hint as to why. To restore it, put the test account's email address in that variable. `DEV_LOGIN_PASSWORD` already has a value, so nothing else is needed. Until then, `/api/dev-new-account` still works (it uses the service-role key plus `DEV_LOGIN_PASSWORD`) but gives a brand-new empty account rather than the seeded test one.
- **Dev auto-login for browser verification** (added 2026-07-21): `GET /api/dev-login` signs in the dedicated test account so automated Browser-pane sessions reach a logged-in state without a password ever being typed. Dev-only twice over: it 404s unless `NODE_ENV` is development **and** `DEV_LOGIN_EMAIL`/`DEV_LOGIN_PASSWORD` exist in `.env.local` (gitignored; never set on Vercel). The route is listed in the middleware's `PUBLIC_PATHS` so a logged-out hit isn't bounced to `/login` first.

### How to test the onboarding flow (recipe - hand this to the user verbatim whenever they ask)
Added 2026-07-26 at the user's request, so the steps live here instead of in their head. Both routes are dev-only and send no email, so this can be repeated as often as needed (see T120/T121, and the "email rate limit exceeded" problem that made this necessary).

1. **Become a brand-new user:** open `http://localhost:3000/api/dev-new-account`. This creates a real, already-confirmed account and signs into it. The **Welcome to Orium** box should appear, with a "0 of 6" Getting started checklist and zero balances behind it.
2. **Pick a path to test.** The modal offers exactly three. Re-open the same link before each one so every run starts clean:
   - *Take the short tour* - the 6-step walkthrough: "Welcome to Orium" (centred, no highlight), then Accounts, Bills, Forecast, Settings, and back to the Dashboard's Peaks and Drops. It simply ends on the last step. **There is no post-tour prompt** - T123 deleted it, along with the tour's "Prefer step-by-step setup?" link, because between them the app was asking the same question up to four times.
   - *Start guided setup* - goes to `/setup`, a normal page (not a gate), stepping through accounts, bills and income, then debt, savings, budgets and misc as optional. Every step has "Exit setup for now", and the nav stays usable throughout.
   - *Maybe later* - skips onboarding; check it stays skipped while navigating, and that the modal does not return.
3. **To test that progress survives a logout:** start the tour, click Next once or twice, log out, then log back in with the account's own address (shown in the URL bar after step 1, and in the sidebar) plus `DEV_LOGIN_PASSWORD`. The tour must resume on the same step, not restart (T119).
4. **Clean up afterwards:** open `http://localhost:3000/api/dev-new-account?cleanup=1`. It deletes only accounts this route created (matched on the `orium-dev+` prefix), so the real test account can never be caught by it.
5. **Return to the normal seeded account:** open `http://localhost:3000/api/dev-login`.

Quicker alternative when a full fresh account isn't needed: `http://localhost:3000/api/dev-reset-onboarding` resets the *current* account's onboarding columns only (add `?wipe=1` to also clear its financial rows). Not a faithful new user - currency, balance thresholds, tier labels, dismissed form tips and the sample-data stamp all survive it - so prefer step 1 when testing the real first-run experience.

## Roadmap

**Task order is non-negotiable: work strictly top to bottom through this section — always the topmost unchecked (`- [ ]`) item.** Never skip ahead or cherry-pick a later task without the user explicitly saying so. (Mirrored in CLAUDE.md's session workflow.)

### Before MVP launch (needs discussion)
- ~~Interactive onboarding experience for new users~~ — discussed and scoped 2026-07-24. Decisions locked with the user: seeded sample data + reset/restore actions rather than a separate sandbox for v1 (a switchable demo sandbox is a possible follow-up if real-user testing shows demand — deliberately not scoped now); spotlight tours for the Dashboard/Forecast intros plus a persistent "Getting started" checklist for the add-data steps; all hand-built, no tour library without explicit approval. Now **Phase 16 (T96–T100)**.
- **Post-build milestones (logged 2026-07-24 — milestones, not tasks):** after Phases 13–16 ship: (1) the user's wife uses the app for 1–2 weeks and feedback is collected; (2) iterate on that feedback; (3) then a launch discussion — landing page, first users, marketing, and whether a mobile app comes before or after launch.
- ~~Per-item progress bars for Debt/Savings goals~~ — scoped 2026-07-24 and moved to the roadmap as **T72** (Phase 12). The open "what does progress mean" question was resolved by the user: progress is occurrence-count-based (occurrences settled / total occurrences), made computable by requiring every debt/savings item to have an ending.
- **A more insightful History page.** Flagged by the user (2026-07-23): History should help users understand past spending patterns and cash flow, not just list settlements. Not yet scoped — needs a discussion on what specific insights (spending by category over time? month-over-month comparison? something else?) before it becomes a T-number. Reaffirmed 2026-07-25 ("history - graphs insights") — same ask, still not yet scoped.
- **Phone notifications.** Raised 2026-07-25, explicitly informational only per the user ("don't set them as T yet, only informational once these start") — not yet scoped, and directly in tension with the existing "Out of scope" list below, which already names notifications. Needs a decision on whether that exclusion still holds before this becomes a real task.
- **Beta launch strategy.** Raised 2026-07-27, moved here from REMINDER.md 2026-07-28: sizing how many free beta users the current Supabase free-tier project can actually support; how to vet sign-ups as legitimate (currently leaning toward posting to the user's own Facebook page for friends/family, wary of Reddit's strict/unfamiliar-with-Filipino-subreddits self-promotion norms); the overall sequencing (beta ~3 months → act on feedback and build paid features 1-2 months → public launch with paid features → mobile app timing, possibly after a browser-only launch). A landing page draft already exists (`landing-page.html`, repo root). Not yet scoped into tasks — needs an actual discussion session, not a unilateral decision, since it's business/product strategy rather than an engineering call.
- **GTM playbook.** Raised 2026-07-27, moved here 2026-07-28: the user has a private GTM playbook they'll share for an ongoing planning discussion (explicitly **not** to be included in the public GitHub source — keep it out of any committed file). Claude should ask the user to send it when starting this discussion. Feeds into the beta launch strategy item above and into scope decisions on the future-features list below.
- **Future pre-launch feature list.** Raised 2026-07-27, moved here 2026-07-28 — the user asked to spec four things before public launch: (1) notifications (already covered by the Phone notifications bullet above), (2) import / templates / a guided first-time build so setup isn't solo, (3) sharing the app with a partner, (4) ~~"what-if" scenarios~~ (e.g. "what if we buy the car") - built 2026-07-31 as **T174**, no scope conflict (a scenario is single-account, same as everything else, so it never touched the "multi-user families" exclusion). Partner sharing still directly conflicts with the existing "Out of scope" list below, the same way phone notifications conflicts with "notifications" - still needs an explicit scope decision (keep the exclusion, or lift it) before becoming a real task. Import/templates remains the more straightforward remaining candidate to scope.
- **Production safety, staging, and inviting real users.** Raised 2026-07-30, triaged 2026-07-31. **Step 1 done 2026-08-01**: a second free Supabase project ("Orium Staging") now exists with all 33 migrations applied, and local `.env.local` points `npm run dev` at it instead of production - full details and the one remaining manual step (pasting the staging service-role key) in `STAGING.md`'s "Progress" section. Still open: custom SMTP (blocks inviting anyone), how signup gets gated, and building the one-way production-to-staging data refresh (decided to be on-request only, not automatic - not yet built).
- ~~Raise the tracking horizon from 5 years~~ - resolved as **T171** (below): the user gave a flat "final decision" of 50 years on 2026-07-31, superseding both this discussion and its earlier 25-year version, and superseding **T146**'s 2026-07-28 "keep 5" confirmation in turn. No split between a validation cap and a smaller rendered horizon - the single `MAX_TRACKING_YEARS` constant governs both, exactly as before, just larger.
- **Email notifications (three separate asks, all currently out of scope).** Raised 2026-07-27 through 2026-07-30: (1) a 12am email whenever any forecasted transaction falls due that day, (2) an alert for past-due unsettled transactions (the notification half of Bug #11 / T150), (3) the user's mental model of "one email with multiple toggles" rather than several separate emails. Together with the earlier "Phone notifications" bullet above, that is four requests against an "Out of scope" line that still reads *notifications*. That exclusion is now clearly stale and should be lifted deliberately. Beyond the scope call there is an infrastructure decision: a 12am send needs something running on a schedule (a cron trigger plus a transactional email provider), which is new infrastructure, a new dependency, and probably a new cost - none of which are covered by the fixed stack in this file. The existing Operations note about custom SMTP for signup email volume is related and should be settled at the same time.
- **User-defined categories.** Raised 2026-07-30: alongside the built-in types (bills, income, debt, savings) the user wants their own - birthdays, occasions, events - for money that is set aside and predictable but is not a bill. Their own suggested shape is "main categories" (the existing fixed set) plus "custom categories" (user-created). This is a data-model change: `recurring_items.type` is a DB-enforced enum, and type drives colors, per-type pages, per-type summary stats, and the debt/savings goal logic. Worth deciding whether custom categories are a genuine new *type* or just a **label/tag** on existing items - the second is dramatically cheaper and may cover the actual need (a birthday fund is arguably a Savings item with a tag).
- **Forecasted balance per account.** Raised 2026-07-30, and the user explicitly flagged it as needing discussion rather than a build. Today the forecast produces one running balance across all accounts; this would additionally answer "what will *this specific account* hold on a given future date." Feasible in principle since T71 already links most items to an account, but two things need answering first: what happens to rows with **no** linked account (excluded, or spread somehow), and whether per-account balances are allowed to be individually negative while the total stays healthy. The user's own scoping is helpful and should be honored: desktop only, surfaced on hover over an account, deliberately not on mobile.

### Carried over from Phase 19
One task from the pre-launch sweep is still open. Everything else in Phases 6A-19 is done and archived.

- [ ] **T138.** Fix: guided setup throws a network error after entering 10+ bills and advancing to the next step. *Investigated 2026-07-28, not yet reproduced: added 11 bills total (9 via the regular Bills page, matching the reported count) then loaded `/setup` fresh - it correctly resolved straight to "Step 3 of 7: Add income" with no error. Also drove the wizard's own "Add another income" -> reopen -> save cycle (the actual code path the report describes, distinct from the regular page) twice in a row with no error. Checked browser console and dev server logs throughout both attempts - clean. Left open rather than closed, since "not reproduced yet" isn't "doesn't exist" - a transient Supabase hiccup or a genuinely higher item count could still trigger it. Needs more specific repro info next time it happens: the exact error text/toast, whether it's a browser-level "Failed to fetch" or an app-level message, and roughly how many items were in the step at the time.<br>**Second pass, 2026-07-31, code audit rather than a click-through** (the Browser pane was stuck on its known "Loading..." issue this session): read every server action the wizard's add-form path touches - `createBillForWizard`/`markWizardStepSaved` and `setWizardReopen`/`clearWizardState` (`onboardingActions.ts`), `setup/page.tsx`'s queries, and `OnboardingWizard.tsx`'s state derivation. Found nothing that scales with item count: no row-count trigger or constraint on `recurring_items` (checked every migration touching that table), no place the wizard serializes the full item list back to the server (the preview list's own delete/edit forms carry only a single `id`, not the array), and the `Promise.all` queries in `setup/page.tsx` have no `.limit()` that 10 rows would approach. The original hypothesis - the previous session's repro used the regular Bills page for 9 of the 11 items rather than the wizard's own "Add another" -> reopen -> save loop the report actually describes - still stands as the most likely gap between what was tested and what happened, since that loop calls `setWizardReopen`/`router.refresh()`/the create action/`clearWizardState` in sequence and hasn't been driven more than twice in a row by anyone investigating this. Left open; still needs the exact error text/toast and a repro run through that specific loop 10+ times in immediate succession, ideally with dev server logs open throughout.*

### Phase 20 - Post-production backlog (scoped 2026-07-31, not yet built)
Everything below came from REMINDER.md's untriaged backlog (items added 2026-07-29 through 2026-07-30, 30 in total). Checked against the whole of this file for duplicates first: no true duplicates, but several items turned out to overlap existing work in ways worth recording - the "budget accounts" cluster is one coherent feature, not seven items (split out as Phase 21 below), the "yearly repeats" ask is a discoverability gap rather than a missing capability (same finding as T144), and two items turned out to describe **bugs in live production data** rather than feature requests.

**Ordering principle for this phase:** the app is now running against the user's real finances, so accuracy bugs come before features. T150 and T151 are both cases where the number Orium shows is wrong in a way that could drive a real financial decision, and they sit at the top for that reason. Five further items raised in the same batch are *not* listed here at all - horizon 5 → 25 years, email notifications, custom categories, per-account forecasted balance, and production/staging safety - because each needs a product decision before it can be a task; all five are written up in "Before MVP launch (needs discussion)" above.

- [x] **T148.** Forecast: same-day rows sort income before deductions, so a salary landing on the 5th is credited before a bill due the same day. *Shipped 2026-07-29 in commit `c6b85d3` ahead of this triage, which is why it has no build write-up of its own - documented here retroactively so the roadmap and `git log` agree. `generateForecast`'s final sort in `src/lib/engine/forecast.ts` gained a sign comparison between rows sharing a `dueDate` (`amount >= 0` sorts first), keeping insertion order for rows matching on both date and sign. Engine semantics section above updated to match, 2026-07-31. **Left an untested engine rule behind** - see T154.*
- [x] **T149.** Budgets: "Add funds" and "Take funds" show on every budget, not only budgets without a linked income. *Shipped 2026-07-29 in commit `f7aee0d` ahead of this triage, documented retroactively for the same reason as T148. `BudgetCard.tsx` dropped its `isManual` guard around the two buttons. The original Phase 10 design deliberately gated them (T55: income-linked budgets replenish automatically, so manual top-ups were assumed unnecessary), but every one of the user's real budgets is income-linked, which meant the buttons were invisible in actual use. Manual correction is now available regardless of replenish mode.*
- [x] **T150.** Fix: the Forecast silently drops past-dated transactions that were never settled (**Bug #11**, user-reported 2026-07-30 from real production data). An unsettled Jul 29 Misc payment vanished from the forecast on Jul 30, and with it a genuine negative balance around Sep 1 - the app reported the user as solvent when they were not. *Decision taken 2026-07-31: the user chose an **unbounded** lookback over a fixed window. Engine semantics are written up in "The forecast engine" above; this entry records the build.<br>**Engine** (`forecast.ts`): recurring rules expand from `item.startDate` instead of `today`, past-dated one-offs are no longer skipped, own-schedule budget replenishments expand from their own rule start, and every row whose `dueDate` is before `today` gets `pastDue: true`. Widening the expansion window is safe by construction: `expandRecurrenceOccurrences` already ignores candidates before `rule.startDate` and counts `after_count` from the start regardless of the window, so which occurrences *exist* is unchanged - only which ones are returned. New `splitPastDue(rows, startingBalance)` returns `{ pastDue, upcoming, pastDueTotal, balanceAfterPastDue }`, which is what keeps forward-looking stats honest: lowest-balance, first-danger and Peaks and Drops all take `upcoming` plus `balanceAfterPastDue`, so a past-due dip is never read as a future event while the numbers still start from a realistic position. No change was needed inside any of those three functions.<br>**UI**: past-due rows are tinted red with a "Past due" badge in the desktop table, and on the compact/mobile table the *date group header* carries the treatment (it is what gets read first on a narrow screen). Both the Forecast and the Dashboard gained a summary panel stating the count, the outstanding total, and the balance left once cleared - rendered only when a backlog exists, since a permanent "0 past due" line trains the user to stop seeing it. On the Dashboard it sits above the stat cards deliberately: "Total Balance" is money not yet actually spent, and this is what is already committed against it.<br>**Two deliberate opt-outs**, both documented in the engine section: Debt/Savings per-item "upcoming" lists filter past-due out (a months-old missed payment showing as the *next* one reads as a bug), and the preview fixture drops it entirely (it backdates every rule 180 days for realistic recurrence summaries, nothing in preview is settleable, and the new-user tour would otherwise open on ~60 red rows and a wildly negative balance - caught before shipping, not after).<br>**Tests**: 164/164. Seven new cases covering past-due recurring occurrences with the backlog's effect on the running balance, settled-past occurrences correctly dropping out via their skip override, a row on its own due date not being past-due, an override moving an occurrence out of the past, and both `splitPastDue` branches. One pre-existing test was rewritten rather than deleted - it asserted the old drop-past-one-offs behavior, which is precisely the bug. tsc, `npm run build`, eslint all clean (eslint unchanged from its pre-existing baseline).<br>**Not browser-verified** - same blocker as T152/T153 this session (Browser pane not compositing, `/api/dev-login` disabled). The engine half is covered by tests; the UI half is a tint, a badge and two conditional panels.<br>The email-notification half of the user's original report is deliberately **not** here - it depends on the notifications scope decision in "Before MVP launch".*
- [x] **T151.** Fix: settling an income linked to a budget double-counts the allocation (**Bug #14**, found 2026-07-31 while scoping the user's "20,000 income minus 1,000 budget = 19,000" request). *The user filed this as a feature request; it is actually a description of correct behavior the code did not implement.<br>**Fix**: `settleOccurrence` (`forecast/actions.ts`) now fetches the linked budgets *before* touching the cash side, and applies `actualAmount - totalAllocation` to the chosen account in a single write. Netting rather than an income credit followed by per-budget debits is deliberate: `applyToBalance` is a read-then-write, so each additional call is another chance for two concurrent settles to interleave and lose an update. The allocation is allowed to push the account negative when it exceeds the income - that matches both the projection and the ledger model, which lets budgets go negative rather than clamping. The `settlements` row the loop already wrote with `actual_amount: -allocation` described a cash movement that never happened; it is now true.<br>**No test.** The logic lives in a server action against Supabase and this project's Vitest is engine-only, so there is nothing to unit-test without new infrastructure (same position as T135/T136/T139). Extracting `actualAmount - totalAllocation` into a testable helper would be testing subtraction. Verified instead by reading every write path: `applyToBalance` has exactly one call site, and the only other writes to `balances.amount` are the Accounts page's own CRUD.<br>**Impact measured against live production data, corrected 2026-08-01**: the original 2026-07-31 measurement (₱26,000 across 5 settlements) queried `settlements` without filtering by `user_id`, and so silently summed three different Supabase auth accounts together - the real account plus two throwaway QA accounts (`+test`, `+test2`) created earlier this session. Re-run scoped to the real account only: **exactly 1 replenishment settlement**, "Orius Needs (GCash Tatay) - Replenished from Tatay - FGT", -₱2,000, dated 2026-07-29. That is the true production overstatement this bug caused.<br>**Historical balances were deliberately left alone.** Correcting the ₱2,000 means writing an inferred amount onto real money the user can verify against their actual bank, so it is their call, not an automatic migration.*
- [x] **T152.** Fix: the Forecast's balance chips open an account without its connected finances (**Bug #12**, user-reported 2026-07-30). *Root cause was duplication, not an oversight: the connected-items queries were inlined in `accounts/page.tsx` and the `ConnectedItem` type lived in `accounts/BalancesClient.tsx`, so the Forecast page had no shared way to produce the same data. It didn't duplicate them, and `BalanceModal`'s `connectedItems = []` default quietly absorbed the gap - the modal rendered with the section missing rather than failing.<br>Fixed by removing the duplication rather than adding a second copy. Two new modules: `src/lib/connectedItems.ts` (the `ConnectedItem` type plus `connectedItemsFromFixture`, both pure and free of any server import) and `src/lib/connectedItemsData.ts` (`loadConnectedItems`, the Supabase half). The split is deliberate - `BalanceModal`/`BalancesClient` are client components that need the type, and the pure mapper needed to be testable, so neither can sit in a module that pulls in `next/headers`. `accounts/page.tsx` now calls the shared loader instead of its own inlined queries, and `forecast/page.tsx` calls it too, threading the result through `ForecastClient` into the modal filtered by account id, exactly as the Balances page does. `BalanceModal`'s stale comment describing the omission as intentional was updated. Incidentally fixed a smaller inconsistency: the Balances page's `?preview=1` branch built its fixture list inline and omitted one-offs, and now shares the same mapper.<br>158/158 tests (3 new for `connectedItemsFromFixture`), tsc clean, `npm run build` clean, eslint unchanged from baseline. **Not browser-verified** - the Browser pane would not composite frames this session (the recurring "stuck on Loading..." issue noted in T145), and `/api/dev-login` is currently disabled because `DEV_LOGIN_EMAIL` is empty in `.env.local`. The prop wiring is guaranteed by tsc and the mapping by the new tests; what remains unconfirmed by eye is only that the modal section renders, which is unchanged code already working on the Balances page.*
- [x] **T153.** Fix: "First goes negative" is the wrong label whenever the danger threshold is above zero (**Bug #13**, user-reported 2026-07-30). *Both call sites render `findFirstDangerPoint(..., balanceRanges[0], ...)` - by its own name a danger-threshold crossing, not a zero crossing - and passed the figure through `Math.abs()`, so a positive ₱3,000 danger balance printed as "₱3,000.00" under the word "negative". Took the conditional wording the user preferred, of the two they proposed.<br>New `firstDangerLabel(balance)` in `src/lib/balanceColor.ts`, next to `lowestBalanceLabel`: returns "First goes negative:" below zero and "First hits danger:" otherwise. Wired into both call sites (`forecast/ForecastClient.tsx`, dashboard `page.tsx`). Deliberately **not** reusing T80's user-editable tier labels - those are complete phrases written for the "lowest balance" stat ("⚠ Goes negative by"), and "First ⚠ Goes negative by:" is not a sentence; the code comment records that reasoning so the next person doesn't try. `Math.abs()` stays: with the wording correct, "First goes negative: ₱1,234" and "First hits danger: ₱3,000" both read unambiguously.<br>tsc, build and tests clean. Same browser-verification caveat as T152 - the label is a pure function of the balance and its two branches are trivially inspectable, but it was not seen rendered this session.*
- [x] **T154.** Add engine test coverage for T148's same-day ordering rule. *T148 changed `generateForecast`'s sort without adding a test, leaving a project rule unmet: CLAUDE.md requires the engine keep passing coverage of the cases listed in this file, and same-day income-before-deduction is now one of them. Four tests in a new "generateForecast same-day ordering (T148)" block in `forecast.test.ts`: income ahead of a deduction on a shared date (listed bill-first in the input, so insertion order alone would fail it) with the running balance asserted to never dip; date beating sign across different dates; insertion order preserved for same-date same-sign rows; and the rule holding across sources, where a one-off income is merged after a recurring bill and still sorts ahead of it. 155/155 tests at the time, tsc clean.*
- [x] **T155.** Forecast row polish: bold the dates, and show a comment-bubble icon on any row whose underlying record has a comment, revealing that comment on hover. *Two asks from the same batch, grouped because they touch the same rows in the same file.<br>The comment half needed real plumbing: `comments` existed on `recurring_items` and `one_off_items` but was never selected by `loadForecast`, never present on the engine's `RecurringItem`/`OneOffItem`, and so never reached a row. Added to both queries, both engine types and both mappings, and emitted as `ForecastRow.comment`. The engine field is **optional** (`comments?: string | null`) rather than required-nullable like `balanceId`, deliberately: the engine never reads it, it is display metadata passing through, and optional left every existing fixture and constructor untouched. Empty and whitespace-only comments collapse to `undefined` at the engine boundary, so a blank comment can never render an indicator with nothing behind it.<br>The indicator reuses the edited-occurrence pattern sitting directly above it in `ForecastNameCell` (icon plus `title`) rather than a second hover mechanism - `title` also long-presses on touch and is announced by screen readers, which a CSS-only tooltip would not be. Dates bolded in both the desktop table and the compact table's date-group headers.<br>167/167 tests, three new covering the pass-through for both source types and the empty/null/whitespace collapsing. tsc, build, eslint all clean.*
- [x] **T156.** Show each item's start date on the Bills, Income, Debt and Savings list rows. *New `startDateLabel(rule)` in `recurrenceSummary.ts` (`"Started Jan 5, 2026"`, reusing `formatFullDate` so it matches Phase 7's date-format rule everywhere else), prefixed onto the existing meta line on all four pages - Bills, Income, and Debt/Savings via their shared `MonthlyGoalsClient`.<br>Deliberately a **separate function rather than folded into `summarizeRecurrence`**, contrary to the scoping note's suggestion: that function is also used for the Budgets page's compact replenish pill and T167's frequency line, neither of which has room for a second clause and neither of which asked for a start date. Extending `summarizeRecurrence` itself would have silently changed those too.<br>179/179 tests, two new. tsc, build, eslint clean.*
- [x] **T157.** Recurrence picker: add a "Yearly" preset, and make "Ends: never" the default for Bills and Income. *Two unrelated changes to the same shared component.<br>**Yearly preset**: appended to `computeRecurrencePresets`, labelled from the start date's own month and day ("Yearly on Jan 20") to match how `summarizeRecurrence` already renders a yearly rule. Purely a discoverability fix, exactly as scoped - `unit: "year"` has worked since Phase 6A and was always reachable through the Custom panel.<br>**"Ends: never" default**: keyed off the existing `allowNever` prop rather than adding a second one, since that flag already encodes precisely the right distinction - Debt/Savings pass `allowNever={false}` because T72 needs a finite end for their occurrence-count progress bars, and they keep the old `on_date` default untouched. **Budgets get the new default too**, which the request didn't mention: they share the picker, and it is a genuine improvement rather than an incidental side effect, because the old default was `on_date` with `endDate: null`, which `recurrenceForm.ts` rejects outright with "Choose an end date" - so every budget schedule previously had to have an end picked by hand before it would save.<br>181/181 tests. One pre-existing preset-label assertion needed updating (it asserts the exact preset list, so a new entry legitimately changes it); three new tests cover the yearly preset's label tracking the start date and the rule shape it produces. tsc, build, eslint clean.*
- [x] **T158.** Forecast: expand a clicked transaction to show its full detail, hidden behind a "see more" control. *A collapsible panel at the top of the existing `EditSettleModal`, listing every field the user asked for - type, date, amount, connected account, forecasted balance, and the record's comment - rather than a new surface. Collapsed by default: this is reference detail, not why the modal was opened, and expanding it by default would push the Edit/Settle form below the fold. Applies to every row kind, so a budget row gets the same panel a bill does.<br>The comment row only exists because T155 threaded `comments` from the underlying record through to `ForecastRow`; before that there would have been nothing to show.<br>**One structural change to avoid repeating Bug #12.** The type label lives in a `TYPE_LABEL` map (T106's "extra" → "misc" mapping) that was file-local to `ForecastClient.tsx`. That file imports `EditSettleModal`, so importing it back would be circular, and copying it would have created exactly the two-implementations-of-one-thing situation that caused Bug #12 - where the Forecast page silently kept its own stale copy. Extracted `TYPE_LABEL`/`TYPE_COLOR` to `src/lib/forecastLabels.ts` and pointed both files at it instead.<br>181/181 tests (no new ones - this is presentational, and the values it renders come from already-tested engine output). tsc, build, eslint clean.*
- [x] **T159.** Comprehensive filters on finance records. *Scoped by auditing what already existed rather than by asking, under the standing authority the user granted 2026-07-31. Of the four axes the original note listed, **three were already built**: free-text name search and amount-range filtering have been on every finance page since T52, and the four recurring pages have had recurrence-unit chips just as long. Misc additionally had a due-date range. The genuine gap was **connected account** - which is also the one that matters most in this account's real use, where items are spread across several accounts (BDO, GCash, Wise, UB).<br>Shipped as a shared `src/lib/accountFilter.ts` (`accountFilterOptions` + `matchesAccountFilter`) wired into all five pages that have a `balance_id`: Bills, Income, Debt, Savings (via their shared `MonthlyGoalsClient`) and Misc. Shared rather than reimplemented per page precisely because five call sites is where copy-paste starts to drift - the same failure mode as Bug #12. Budgets are deliberately excluded: they have no connected account, only a linked income, which their existing replenish-type filter already covers.<br>Two details worth recording: an empty selection means "filter not applied" rather than "exclude everything", matching every other multi-select on these pages; and "No account" is offered as its own bucket unconditionally, since "which of these did I forget to connect?" stays a useful question even once everything happens to be linked. The chip row hides itself when the user has no accounts at all.<br>Also fixed a latent bug introduced mid-task: `MonthlyGoalsClient`'s filter `useMemo` was missing `selectedAccounts` from its dependency array, which would have left the Debt/Savings list stale on the first toggle.<br>187/187 tests, six new covering the predicate and the option list. tsc, build, eslint clean.*
- [x] **T160.** Give completed debts and savings goals somewhere to go once fully settled, separate from History. *Decided rather than asked, per the user's standing authority: a collapsible archive section on the existing page (`MonthlyGoalsClient.tsx`, shared by Debt and Savings), not a separate route - simpler, and consistent with the app's existing "reveal more infrequently needed info" pattern (T44's sidebar collapse, T81's Forecast Insights), rather than a new nav item for what may be very few items.<br>Completion detection needed no new logic: T72's `goalProgress(rule, settledCount).fraction` reaches exactly `1` once every occurrence a rule will ever produce has been settled, already the case for every debt/savings item since they're DB-enforced to have a finite end. The list is split into `activeItems`/`completedItems` on every render (the split isn't persisted - it always reflects the current settlement state) and the completed section renders in a compact card (name, amount, a green "Complete" badge) rather than the full row with its now-redundant 100% progress bar, collapsed by default behind the same localStorage-backed toggle T44/T81 established, keyed per page since Debt and Savings share this one component.<br>**One real edge case caught before shipping**: the existing up/down reorder buttons operate on the full persisted order (`useOrderedList`), which still has completed items interspersed even though they're no longer displayed in the active list - a "move up" near that boundary would have swapped against a completed neighbor invisible on screen, moving it in a way nothing explained. `canReorder` now also requires `completedItems.length === 0`, same reasoning the existing `filtersActive` check already uses.<br>Delete needed its own confirm/cancel pair inside the completed section - it renders outside the active list's `.map`, so the active list's inline confirm state wasn't reachable from there; wiring the same button through the existing `confirmingDeleteId` state (rather than a second piece of state) keeps the two sections from ever disagreeing about which item is mid-confirm.<br>No new tests: `goalProgress`'s `fraction === 1` boundary this depends on was already covered (`goalProgress.test.ts`), and the split/collapse logic is presentational, same position as every other component-only change this session (no component-testing infrastructure exists, per CLAUDE.md's Vitest-is-engine-only stack). 196/196 (unchanged), tsc, build, eslint clean.*
- [x] **T161.** Forecast desktop: adopt the mobile date-grouped layout - one date header with that day's transactions beneath it, dropping the date column entirely. *T90's two-table split - a wide desktop table with its own Date column, replaced below `lg` by a compact date-grouped table - is gone. Rather than build desktop a second grouped layout, the existing compact table (already correct, already carrying T150's past-due and T173's due-today group-header treatment) now renders unconditionally at every width; the desktop-only table was deleted outright rather than left in place unused, since two implementations of the same rows is the exact failure mode that caused Bug #12.<br>Type becomes visible from `sm` and Account from `md`, exactly as they already did below `lg` - both breakpoints are already satisfied at `lg`+, so desktop shows every column with no further breakpoint changes. Sequenced after T150 and T155 as scoped, so this pass restyled nothing that had just been restyled.<br>Presentational only - the same rows, same grouping logic, same click/settle behavior, just rendered by one table instead of two. 196/196 (unchanged), tsc, build, eslint clean. **Not browser-verified**: the pane rendered the login page this session but got stuck on its known "Loading..." issue again past auth, both signed in and in `?preview=1` mode. Confidence comes from the compact table already being live and correct below `lg` before this change - nothing about its logic changed, only the condition governing when it renders.*
- [x] **T162.** Activity log foundation: a table recording every create, edit, delete and settle, plus completion timestamps on reminders. *The completion-timestamps half turned out to already exist - `setReminderCompleted` (`reminderActions.ts`) has written `completed_at` since T84, before this task was ever raised. All the real work here is the log itself.<br>**Decision taken**: application-level writes, not a Postgres trigger. A trigger would need per-table trigger functions on 7+ tables and still couldn't produce a human-readable line ("Settled Salary, ₱20,000.00") from a raw row diff - that string only exists in application code, which already has the friendly name/amount at hand from the mutation it just performed.<br>**Migration 0029**: `activity_log` (`user_id`, `created_at`, `action`, `entity_type`, `entity_name`, `detail`). Deliberately no foreign key to the entity it describes - a "Deleted bill: Gym" entry has to survive Gym's own row being gone, so `entity_name` is a snapshot taken at write time, not a live join. RLS has SELECT and INSERT only, no UPDATE or DELETE policy - the log is append-only even to its own owner.<br>**One shared writer**, `logActivity()` (`src/lib/activityLog.ts`), called from every mutating action after its real write succeeds - the same shape T175's `toggleRecordActive` already established. It swallows its own errors: a logging failure must never turn a successful create/edit/delete into a reported one, so the log is fire-and-forget from every caller's perspective.<br>**Wired into all ~30 mutating actions**: Accounts (create/update/delete/disconnect), Bills, Income, Debt+Savings (`recurringItem.ts`, shared by both), Misc, Budgets (create/update/delete/log-spend+add+take-funds via the one shared `writeLedgerEntry`/update-entry/delete-entry), Forecast (edit-occurrence, edit-one-off, settle - including the linked-budget replenishment loop inside it, edit/reset/settle-replenish), Reminders (create/update/delete/complete), and T175's own toggle. Several plain `(formData) => void` delete actions had no user or name available before this - each now fetches the deleted row's name via `.delete().select("name").single()`, the same round-trip-saving trick already used elsewhere, rather than a separate query. `deleteBudgetEntry` and `toggleRecordActive`'s recurring branch needed an embedded/branched select for a name or type that lives on a different or conditional table; two call sites in `EditSettleModal.tsx` needed a `budgetName` hidden field added since they'd never needed the name before.<br>No new tests: every added code path is a fire-and-forget insert with no return value the rest of the app depends on, and the mutation each one wraps already has its own coverage where that exists. tsc, build, eslint clean (eslint unchanged from its pre-existing baseline). Verified live against production: `pg_policies` confirms `activity_log` has exactly INSERT and SELECT policies, no UPDATE/DELETE.*
- [x] **T163.** "What's changed since you last logged in" feed, so a couple sharing one account can stay in sync. *A new `/updates` page (own sidebar nav entry, between History and Settings - both are "look back" pages, unlike the forward-looking Dashboard/Forecast at the top) - a plain reverse-chronological list over T162's `activity_log`, day-grouped the same way T161's Forecast groups rows by date.<br>**Migration 0030** adds `preferences.activity_log_seen_at` (nullable timestamptz) as the per-user watermark - one row per user already exists there (migration 0001), so a column is cheaper than a new table just for this. Null means "never viewed," so every existing entry counts as unseen the first time an account opens the feed rather than starting empty.<br>**The read-then-mark ordering matters**: `page.tsx` reads the *old* `activity_log_seen_at` before anything changes, uses it to decide which entries render as "new" (an accent dot plus a tinted card) for *this* render, then `UpdatesClient.tsx` calls the new `markUpdatesSeen()` action once mounted, stamping a fresh watermark for *next* time. Reading and marking in the same request order would make every entry always render as already-seen.<br>**Sidebar badge**: `(app)/layout.tsx` (which already re-fetches `preferences` on every navigation) now also runs one lightweight `count`-only query against `activity_log` for anything newer than the watermark, threaded through `AppShell` into a small accent badge on the Updates nav link - shown only when the count is above zero, and still visible next to the icon when the sidebar is collapsed, since a couple checking in after a few days away is exactly who this is for and might not expand the sidebar first.<br>**New date helpers** in `date.ts`: `dayKeyManila`/`formatTimeManila`, both resolved in the same Asia/Manila timezone `todayInManila()` already uses - `activity_log.created_at` is the first real timestamp (not a plain date) this app has needed to display, so "today" in the feed has to agree with "today" everywhere else rather than drifting a day near midnight on a UTC server.<br>**Follow-up caught while wiring this up, not originally scoped**: `wipeFinancialData.ts`'s shared table list (used by both "Restore sample data" and account deletion) didn't include `activity_log`. Without this fix, restoring sample data would have left a user's real change history sitting in the Updates feed next to fresh sample entries - the same "sample data mixed with real data" confusion the sample-data banner elsewhere in the app exists specifically to prevent. Added to that list and to `wipe_test_data.sql`, matching each file's own stated invariant that the two stay in sync.<br>No new tests: the page is a plain read-and-render with no engine logic, matching every other component-only addition this session. tsc, build, eslint clean (baseline unchanged). Not browser-verified - same Browser-pane blocker as the rest of this session.*
- [x] **T170.** ~~Leftover sample data is mixed into the production account~~ - **invalid finding, retracted 2026-08-01.** *Originally reported 2026-07-31 as "the production account contains a budget named 'Groceries' linked to 'Freelance - Aya', sitting alongside the user's own real 'Groceries'/'Freelance - Sheena' budget." That description was never true of one account - it was two different, unrelated rows from two different, separate throwaway QA accounts (`nathanelcorpuz+test@gmail.com` has "Groceries"/"Freelance - Sheena"; `nathanelcorpuz+test2@gmail.com` has "Groceries"/"Freelance - Aya", the em-dash one), stitched together into a false narrative by the same unfiltered cross-account query that inflated T151's number.<br>Confirmed directly against `auth.users`/`budgets`/`recurring_items` for the real account (`nathanelcorpuz@gmail.com`, id `fc997c8b-...`): its five budgets are "Food (Maya Nanay)", "Gas (GCash Tatay)", "Grocery (GCash Nanay)", "Laundry (GCash Nanay)", "Orius Needs (GCash Tatay)" - none named "Groceries", no "Freelance - Aya"/"Freelance - Sheena" income anywhere, no em-dash names, no sample data at all. The real account has no leftover-seed contamination. No deletion needed, none performed.*
- [x] **T169.** Make `seed_sample_data`'s dates relative to the seeding date instead of hardcoded January 2026. *Every date in the function (`0018_sample_data_family_names.sql`, the latest of two `create or replace` definitions - 0016 then 0018) was hardcoded to specific 2026-2031 literals. Rather than hand-recompute all ~110 of them individually (real risk of a transcription slip breaking one recurrence rule's shape), **migration 0031** wraps every existing literal as `(date 'YYYY-MM-DD' + v_shift)::date`, where `v_shift` is computed once - `make_interval(years => extract(year from current_date)::int - 2026)` - and reused everywhere. Only the *year* shifts; month, day, and every recurrence rule's shape (weekday arrays, nth-weekday pairs, day-of-month lists) are untouched, which is what keeps every relationship between events intact - "rent on the 1st," "a settlement 5 months into the story" - without recomputing a single one by hand. Confirmed safe for the week-unit items here (Freelance pay, Parking fee, the Dining budget's own schedule) - the engine anchors a week-unit rule to "the week containing start_date" and applies its own weekdays array, so what day-of-week the literal start_date itself falls on has never mattered to which dates actually get emitted.<br>The one non-`date` value - `reminders.completed_at`, a timestamptz literal - got the same treatment: `(timestamptz '...' + v_shift)`.<br>**Verified two ways against production directly**, not just by reading the SQL: `create or replace function` was accepted with no syntax error, and a standalone query confirmed the mechanism itself - at the current date (2026, same year as the story's anchor) `v_shift` computes to zero and every date passes through byte-identical to before; simulated one year ahead, the same query confirmed every date shifts by exactly one year, month and day untouched. The function was not actually invoked against any real account - would insert real rows - so this is the strongest verification available short of that.<br>**Deliberately did not touch** `supabase/seed.sql`, a separate, fixed-id, hand-run SQL-editor script (T88) for one-off dev/demo use, not something the app or any real user's account ever calls - out of this task's actual scope (the real user-facing path is Settings > Restore sample data and the auto-seed-at-signup path, both of which call the plpgsql function fixed here).<br>No app code changed, so the existing 196/196 test suite, tsc, and build are all unaffected (confirmed unchanged after applying).*
- [x] **T164.** Family calendar view built on the forecast - yellow for forecast transactions, green for reminders/todos. *New `/calendar` page, own nav entry next to History/Updates - all three are supporting views over the same forecast/settlement data, not primary entry pages. Its three open scoping questions were each decided directly rather than asked, per the user's standing authority.<br>**Month grid, not an agenda list** - "calendar" is the word the user used, and that's what it means colloquially; a grid also plays well with per-day color-coding, which the whole request was built around. Standard 7-column layout with leading blanks aligning the 1st to its real weekday, prev/next month navigation, today's cell outlined in the accent color.<br>**A busy day shows a count badge, not every row inline** - a grid cell has no room to list rows - clicking a day with entries opens a list (name, type-colored label, amount) in the shared `Modal` component, and clicking a listed row closes that list and opens the exact same `EditSettleModal` the Forecast page already uses. No second edit surface was built for the same rows.<br>**Reminders don't get plotted onto specific day cells - a genuine finding, not a stylistic choice.** The `reminders` table has no due-date column at all (`id`/`text`/`completed`/`completed_at` only), so there is no date to place one on; inventing a due-date concept this feature never had would have been a materially bigger change than "add a calendar view." The user's "green = todos/reminders" is honored instead as a green-accented panel alongside the grid.<br>**That panel does not reuse `RemindersPanel` directly** - caught before shipping, not after: that component's outer chrome (an `h-screen` `sticky` right rail with its own absolutely-positioned collapse toggle, plus a floating mobile bell button) is built specifically for the Forecast page's own full-height sidebar layout, and would have rendered broken or duplicated inside a different page's flex row. Its inner `RemindersContent` - the actual CRUD list, with no outer chrome - is now exported and reused directly instead, which is the one small change made to an existing shared file.<br>Presentational, no engine changes - `daysInMonth`/`formatDate` (`date-utils.ts`) and `TYPE_COLOR`/`TYPE_LABEL` (`forecastLabels.ts`, T158) were reused as-is rather than duplicated. No `?preview=1` support - the existing 6-step guided tour (T120/T129) never visits this page, so preview mode isn't required for the tour to work; a clean follow-up if wanted later.<br>No new tests (presentational, same position as every other component-only change this session). tsc, build, eslint clean (baseline unchanged). Not browser-verified - same Browser-pane blocker as the rest of this session.*
- [x] **T171.** Raise the hard tracking limit from 5 years to 50 - the user's own "final decision," 2026-07-31, closing out the discussion T146 (keep 5) and the unscoped 25-year proposal both left open. *A single change to `MAX_TRACKING_YEARS` (`src/lib/engine/date-utils.ts`), which already governed both the validation cap and the rendered forecast horizon together - the user's flat framing meant no split was wanted, so none was built.<br>**One real edge case surfaced and was fixed, not just the constant moved.** `recurrence.ts`'s `MAX_PERIODS` backstop (a defensive cap on periods scanned, meant only to guard against a runaway loop) was a flat `10000`, sized for a 5-year horizon. A daily-recurring rule (`unit: "day"`) is the one case where `periodIndex` counts individual calendar days one-for-one, so reaching the new 50-year horizon needs roughly 50 x 366 ~= 18,300 periods - comfortably past the old flat cap. Left unfixed, any daily bill/income/debt/savings item would have silently stopped projecting at ~27 years with no error, quietly breaking the promise the app now makes. `MAX_PERIODS` is now derived from `MAX_TRACKING_YEARS` (`(MAX_TRACKING_YEARS + 5) * 366`) instead of a flat number, so the two constants can't drift back out of sync if the horizon changes again - this is exactly the kind of edge case the core design rules ask to be caught, and it wouldn't have shown up until someone actually added a daily-recurring item years from now.<br>177/177 tests, one new proving a daily rule reaches the *entire* 50-year horizon (every calendar day counted, not just that the last date happens to land correctly) rather than silently truncating. tsc, build, eslint clean.*

### Phase 21 - Budgets v4: budget accounts (scoped 2026-07-31, built 2026-07-31)
Seven separate REMINDER.md items turned out to describe one coherent feature, so they were grouped here rather than scattered through Phase 20. The user's own description of the model, condensed: **cash-flow accounts are the first layer** - income lands there and bills are paid from there - and **budget accounts are a second layer** holding money already earmarked for a budget. When an income is settled, the budgets tied to it replenish automatically out of that income, and the money that moves into a budget account is **excluded from the cash-flow forecast**, because budget spending is unpredictable and should not be projected as if it were a scheduled transaction.

**Finding, before any code was written (2026-07-31):** this "second layer" already exists. It's the Budgets v3 running ledger (Phase 10, T57) - `computeBudgetBalance` already tracks money in and out per budget, already excluded from Total Balance and the cash-flow forecast except as real ledger transactions (never a projected recurring drain), and already auto-replenishes on income settle (T56/T59, corrected by T151 this session). Re-reading the seven original notes against that model line by line: every one of them describes behavior the ledger already has, except two genuine gaps - no combined total across every budget, and (already fixed as part of T151/T167) the settle-path double-count and the stale "replenishes today" label.

**Decision, made rather than asked** (the user granted standing authority over exactly this kind of call, 2026-07-31): do **not** build a second, parallel "budget account" concept - a new table, a flag on `balances`, or anything that tracks money the ledger already tracks. Two systems recording the same money is how a real discrepancy gets introduced, not avoided. The single missing piece - a combined figure - gets built directly on top of what exists, and the rest of the phase turns out to already be verified-satisfied by earlier work.

- [x] **T165.** Budget accounts: a running total across all of them (per-budget totals already existed via `computeBudgetBalance`, since Phase 10). *No new data model - the "exclusion from cash-flow forecast" and "replenish when income settles" halves of the original ask were already true of the existing ledger, confirmed by re-reading `forecast.ts` (budget rows are only ever real future-dated ledger entries, never a projected recurring one) and `settleOccurrence` (the linked-budget loop). What was missing was a single combined number - every individual budget had its own total, nothing summed them.<br>`toEngineEntries` (previously a `BudgetCard.tsx`-local helper) exported so `BudgetsClient.tsx` can reuse the exact same mapping rather than a third copy. Both pages that show budgets gained a "Total across all budgets" line: `BudgetsClient.tsx`'s page header (always every budget, ignoring the page's own filters/sort - the same convention Total Balance uses on the Dashboard, since a filter narrows what's *displayed*, not what's real) and the Dashboard's Budgets card (reusing the `budgetEntries`/`computeBudgetBalance` already loaded there). Red when negative, matching every other negative-balance convention in the app.<br>No new engine logic - this is `Array.reduce` over the already-tested `computeBudgetBalance`, so no new engine tests were needed; 177/177 still pass. tsc, build, eslint clean.*
- [x] **T166.** Auto-replenish budget accounts when a linked income is settled, distributing to each connected budget. *Verify-first, same pattern as T142/T146: re-read `settleOccurrence`'s `type === "income"` branch (`forecast/actions.ts`) rather than assume. It already loops every budget where `linked_income_id` matches the settled income and writes each one its own incoming ledger entry equal to that budget's `allocation` - exactly "distributing to each connected budget," built in T56/T59, and made correct on the cash side by T151 this session (previously double-counted the allocation into the account, see Bug #14). No new code - the feature already existed under a different name.*
- [x] **T167.** Budget display: show the configured allocation alongside the current remaining amount, show what a budget is tied to and how often, and update "replenishes today" to the next replenish date once the linked income is settled. *Three small asks, but the third turned out to be a real bug, not just stale wording.<br>**Allocation shown**: `BudgetCard.tsx` now prints "{allocation} allocated per replenishment" under the running total, hidden when `allocation` is 0 (a purely manual budget) rather than printing "of ₱0". Answers exactly the confusion the user hit - a fresh budget correctly shows ₱0 with nothing nearby explaining what it will eventually hold.<br>**Frequency shown**: a second line under the "Connected to {income}" pill now prints the linked income's own recurrence summary (`summarizeRecurrence`, the same function Bills/Income/Debt/Savings rows already use) - so "how often" is visible without opening the income. Skipped for schedule-mode budgets, whose pill already *is* the frequency.<br>**"Replenishes today" going stale - the actual bug**: `replenishProgress` searched for the next occurrence using only the recurrence rule, with no idea whether the "today" occurrence had already been settled. Settling a linked income doesn't move the rule's own dates - it writes a `budget_replenish_overrides` skip - so for the rest of settlement day the card kept insisting a replenishment was still due, even though the money had already moved. `replenishProgress` gained an optional third parameter, `handledDates: readonly string[]`, that filters the forward search; both callers (`BudgetCard.tsx`, Dashboard `page.tsx`) now pass every `skipped` override's `original_date` for that budget. Deliberately **not** filtering on unskipped rows - a T168 edit override is also a row in this same table, and the occurrence it describes hasn't happened yet, so treating it as handled would hide a real upcoming replenishment.<br>176/176 tests, four new for `handledDates` (skips an occurrence landing on `asOf` itself, leaves `previousDate` alone since only the forward search is filtered, a no-op when nothing is handled, still all-null for a manual budget). tsc, build, eslint clean.*
- [x] **T168.** Make budget replenishments editable per instance from the Forecast table - change the amount for one occurrence (₱8,000 instead of ₱10,000) or move its date earlier. **Prioritised to the front of the queue by the user, 2026-07-31.** *The scoping note said the mechanism already existed. It half did: `budget_replenish_overrides` was the right table but only had `skipped boolean not null default true` - nowhere to put an amount or a date.<br>**Migration 0027** adds nullable `new_date` / `new_amount`, bringing the table in line with `occurrence_overrides`' long-standing shape rather than inventing a second mechanism. Purely additive - two nullable columns and one check constraint, nothing rewritten - so it was safe to apply against live production data with no backup step. `new_amount` is a positive magnitude matching `budgets.allocation` (the value it replaces); the forecast negates it for display, so storing a negative would double-negate, and the constraint enforces that. `skipped` keeps its `default true`, so the new edit action passes `skipped: false` explicitly - flipping the default would have silently changed what the existing settle-path insert means.<br>**Engine**: the replenish loop applies `newAmount`/`newDate` independently (moving an occurrence must not reset its amount) and sets `edited` when either applies. An explicit date edit wins over the date an income-linked budget inherits from its income's own override, since the user set it on this row directly. Skip still wins over both - a settled replenishment must not reappear because it also carries edit fields.<br>**The part that wasn't in the scoping note, and mattered most:** income-linked `budget_replenish` rows were never clickable (Phase 11/T59, since they settle automatically with their income and had nothing to open). Every budget in the user's real account is income-linked, so shipping the editor without changing that would have made it unreachable. Rows are now clickable except in preview mode; the modal shows the Edit form alone for income-linked ones and the full Edit/Settle toggle only when the row is genuinely settleable here, rather than offering a Settle tab that would refuse to work. New `editBudgetReplenish` and `resetBudgetReplenish` actions; Reset appears only once there is an edit to undo, and deletes the override row so an un-edited occurrence has no row at all - the same end state as before it was ever touched.<br>172/172 tests, five new covering unedited defaults, amount-only, date-only (including an occurrence moved ahead of its own sibling while keeping its `originalDate` key), both together, and skip-beats-edit. tsc, build, eslint clean. Not browser-verified - same Browser-pane blocker as the rest of this session.*

### Phase 22 - Second reminder batch (scoped 2026-07-31, not yet built)
Six more items collected after the first Phase 20 batch. Two are quick, contained wins; two need their own scoping before a build starts (one of them a large feature); T171 (horizon to 50 years) is already recorded above, done as part of the first batch's cleanup rather than this one, since the user raised it before this batch was triaged.

- [x] **T172.** Per-transaction fee, set on the account and applied automatically to every forecasted transaction connected to it (e.g. "BDO" has a ₱10 fee, so every bill/income/debt/savings/misc row linked to that account shows the fee auto-deducted). *Both flagged design decisions were made directly, per standing authority to make these calls.<br>**Applies to every connected transaction, incoming or outgoing** - the user's own phrasing was "all forecasted transactions connected to BDO," with no carve-out for income, and some real banks do charge deposit fees. A fee is a cost regardless of which direction the underlying transaction moves.<br>**Shown as its own line, never folded into the transaction's own amount** - a bill's displayed amount always has to match its real record. `ForecastRow` gained an optional `feeAmount`, resolved once per account (`forecast.ts`) and applied on top of `row.amount` when accumulating the running balance, so the projection is correct while the displayed amount stays untouched. Shown as a small "- ₱X fee" sub-line on the Forecast table, the Calendar's day-detail list, and (read-only, since the fee belongs to the account, not the occurrence) the settle form itself - the last one directly answers the user's own framing, "in editing/settling a transaction."<br>**Migration 0032**: `balances.transaction_fee_centavos bigint not null default 0`, `>= 0` (a fee is a cost, never a rebate - a credited transaction is already representable as its own bill/income/misc entry, a different feature). Purely additive.<br>**A real correctness gap found and fixed while wiring this up, not part of the original ask**: the projected forecast already included the fee via the engine, but `settleOccurrence`'s actual balance update (`applyToBalance`) didn't - a settled transaction's real balance would have ended up higher than what the forecast had promised, the same class of forecast-versus-reality drift Bug #14/T151 was about, just in the opposite direction. Fixed inside the shared `applyToBalance` helper itself (now also fetches and deducts the fee) rather than at its one call site, so any future caller inherits the same correctness for free.<br>**A structural note surfaced along the way**: `forecastData.ts`'s `balances` array is used both as `generateForecast`'s own input (needs camelCase `transactionFeeCentavos`) and, unchanged, as what `ForecastClient.tsx` threads into `BalanceModal` (needs snake_case `transaction_fee_centavos`, matching every other "Row" type in this app). Rather than diverge the two, the mapped object now carries both field names - simpler than maintaining two parallel arrays for the same rows. `accounts/page.tsx` and `setup/page.tsx`'s own balances queries also picked up the new column, so editing an account via either the Balances page or the guided-setup wizard always sees its real current fee rather than silently resetting it to zero.<br>203/203 tests, 7 new covering both directions, a connected one-off, the fee applying independently to every occurrence rather than once, no-account/zero-fee/undefined-fee all behaving as no-fee. tsc, build, eslint clean (baseline unchanged). Not browser-verified - same Browser-pane blocker as the rest of this session; the account form and every display site were read through carefully instead.*
- [x] **T173.** "Due today" tag in the Forecast, yellow, alongside T150's past-due red tag. *Exactly as scoped - a sibling `dueToday?: true` flag set in the same place `pastDue` is, in `forecast.ts`'s final map. Kept as its own field rather than collapsing both into a shared status enum, so every existing `pastDue` check and the many `toEqual` test literals that omit both stay valid.<br>Mutually exclusive with `pastDue` by construction (the branch returns before it can be both). Rendered in amber across both tables: row tint, a "Due today" badge on the desktop date cell, and the date-group header treatment on the compact/mobile table, mirroring what T150 established for red. Dark amber text on the badge rather than white, since amber is too light to carry white legibly.<br>191/191 tests, four new: the flag lands only on today's row, both flags are never set together, the boundary moves correctly when "today" advances (yesterday's due-today row becomes past-due), and nothing is flagged when no row falls on today.*
- [x] **T174.** "Run possible scenario" - a temporary, save-and-toggle simulation layer across the Forecast and Dashboard, with an "activate" action to make it permanent. *The largest single feature raised this session, built with real care given the stakes: a mistake here means showing a real family wrong financial numbers, the worst failure mode for a forecasting tool, so the design pass the task's own note asked for happened before any code, not instead of it.<br>**Architecture decision - the opposite of what the original note assumed.** The obvious approach (a `scenario_id` tag column added to the real `recurring_items`/`one_off_items` tables) was considered and rejected: it would have needed a matching filter added to all ~18 existing files that already query those two tables (every CRUD page, every create/delete action, `connectedItemsData.ts`, `wipeFinancialData.ts`, `toggleActive.ts`...), each one a place a scenario row could leak into a real list if a filter were ever missing. Built instead with genuinely separate tables (`scenarios`, `scenario_recurring_items`, `scenario_one_off_items`, migration 0033) that mirror the real schema's own shape and constraints exactly (including its array-containment CHECK style, not a subquery - Postgres forbids those in CHECK constraints, as 0004's own comment already notes). This makes the leak structurally impossible: none of those ~18 files change at all, and none of them can ever return a scenario row, because scenario rows don't live in the tables those files query.<br>**The one integration point is `loadForecast()`** (`forecastData.ts`): when `preferences.active_scenario_id` is set, that scenario's rows are fetched and merged into the same `recurringItems`/`oneOffs` arrays already being built, additive to the existing queries rather than a filter change to them. The engine itself needed zero changes - it already just consumes whatever arrays it's given; every scenario row carries a new `fromScenario: true` flag, pure pass-through (same pattern as `active`/`comments`), so the UI can badge it. Since this is the one place both Forecast and Dashboard read from, "everything can assume that scenario, from the forecast to the dashboard" falls out as a consequence of one shared function rather than a scenario-aware read path built into each page.<br>**"Activate" is a copy, not a re-parent.** Because scenario rows never lived in the real tables, making them real is a plain `INSERT ... ` of the scenario's own rows into `recurring_items`/`one_off_items`, then deleting the scenario (cascade takes its own rows with it) - not the complex cross-table promotion transaction the original note worried about. Requires typing "ACTIVATE" to confirm, matching this app's own established pattern for irreversible actions (Settings' "Restore sample data" -> type RESTORE).<br>**A real leak caught and closed before shipping, not found in review after**: a scenario row's `sourceId` is a `scenario_recurring_items`/`scenario_one_off_items` id, not a real one - clicking it would have opened the same `EditSettleModal` a real row does, whose actions would then try to write an override/settlement against an id that doesn't exist in the tables those actions actually know about. Both the Forecast table and the Calendar's day-detail list now explicitly exclude `fromScenario` rows from being clickable at all; editing happens on the new `/scenarios/[id]` page instead, which reuses the real `RecurrencePicker`/`recurrenceForm.ts` validation exactly (one unified modal for bill/income/debt/savings, `allowNever` following T72's own debt/savings rule, plus a second modal for misc/one-off items mirroring `ExtraModal.tsx`).<br>**Scope, deliberately conservative for v1**: one active scenario at a time (matches the user's own "toggle it on or off" framing, not a simultaneous multi-scenario overlay); no budgets/budget-entries in scenarios (the user's own examples - "what if we buy the car" - were plain financial additions, not budget-ledger mechanics, and every budget column carries its own replenish logic that would have meaningfully widened this); a scenario item's start/due date is allowed in the past, unlike every real create form (T107) - "what if I'd started this 3 months ago" is a legitimate hypothetical the real-data restriction was never meant to block.<br>**Also fixed while wiring this up**: `wipeFinancialData.ts`'s shared table list didn't include `scenarios` - without this, "Restore sample data" would have left a user's real what-if scenarios (and, if one was active, their real items) merged into the fresh sample forecast, the same class of leak T163's `activity_log` fix addressed. Deleting `scenarios` cascades to both its child tables and (`ON DELETE SET NULL`) clears `active_scenario_id` for free. Added to that list and to `wipe_test_data.sql`, matching each file's own stated sync invariant.<br>206/206 tests, 4 new covering `fromScenario` pass-through for both entity types and confirming a scenario row still counts toward the running balance (it has to, to actually preview real impact). tsc, build, eslint clean (baseline unchanged). Server-side verified against production on a genuinely fresh account - `/`, `/scenarios`, `/calendar`, `/forecast` all returned clean 200s with the new code paths exercised (no scenario active) - but not fully interactively verified end to end (create a scenario, add an item, toggle it on, confirm the merge visually), since the Browser pane stayed stuck on its known issue this entire session.*
- [x] **T175.** Temporarily disable a financial record (bill, misc, income, debt, savings, budget) without deleting it, to preview the forecast's shape without it. *Built exactly as scoped - the pragmatic core of what-if analysis, without T174's full scenario system.<br>**Migration 0028**: `active boolean not null default true` on `recurring_items`, `one_off_items`, `budgets` - purely additive, no backfill needed since the default makes every existing row already correct. Applied directly against production; the same owner-only RLS update policies every other table already has cover this write path too, so a crafted request naming someone else's id simply updates zero rows.<br>**Engine**: `forecast.ts` filters both lists (`active !== false`, so undefined/older rows stay active) at the very top of expansion - before recurrence, overrides, or budget replenishment run - so every downstream calculation stays consistent with no separate exclusion logic to maintain. The interesting case is a disabled *income*: a linked budget borrows its replenish dates from the income's own occurrences, so filtering at the top takes the budget's projected rows with it automatically, confirmed by a dedicated test. A disabled budget's own running total on the Budgets page is deliberately untouched - that number is a record of money already moved, not a projection, so hiding it would misstate history rather than preview anything.<br>**UI**: one shared server action (`toggleRecordActive`) and one shared `ActiveToggle` component reused across Bills, Income, Debt, Savings and Misc, plus a bespoke icon-button version in `BudgetCard.tsx` matching that card's existing T112 icon-language rather than importing a text button that wouldn't fit. The table a toggle writes to is resolved through a fixed whitelist keyed by `kind`, never interpolated from form data directly. A disabled item stays visible (muted, with a small "Off" badge) rather than hidden - an item you can't find is an item you can't switch back on.<br>196/196 tests, five new covering a disabled recurring item, a disabled one-off, the undefined-means-active default, a disabled budget's own replenishments, and the income-disables-its-budget cascade. tsc, build, eslint clean.*
- [x] **T176.** History page reads too small on desktop. *Diagnosed directly rather than asked, per the standing authority the user granted 2026-07-31.<br>`history/page.tsx`'s container was `max-w-4xl` (896px) - a value used nowhere else in this codebase. Its table has 7 columns of `whitespace-nowrap` content (Name/Type/Forecasted/Actual/Forecasted date/Actual date/Balance), comparable to the Forecast table's 6, but Forecast's own wide-table container is `max-w-6xl` (1152px, `ForecastClient.tsx`). History was simply narrower than the app's own established convention for a wide multi-column table - not "small" as in font size, but as in forced into horizontal scroll sooner than it needed to be on an ordinary desktop viewport. Matched to Forecast's existing width rather than inventing a new one.<br>Presentational only - no logic or data changed, so no new tests. 196/196 (unchanged), tsc, build, eslint clean. **Not browser-verified**: the Browser pane did briefly render this session (confirmed by loading the login page), but got stuck on its known "Loading..." pattern again once past auth, in a fresh tab and after a full server restart - the same issue noted in T145/T150/T152/T153. Confidence here comes from directly comparing the two Tailwind values against the one other page with a matching table shape, not from a screenshot.*

### Phase 23 - Third reminder batch (scoped 2026-08-01, not yet built)
Eighteen items arrived in one message. One (T170's SM Tatay confusion) turned out to be a false alarm from a cross-account query bug, already corrected above, not a new task. The rest are ordered here smallest/most-contained first, with the four scenario-related notes (budgets in scenarios, unlimited active scenarios, the combined banner, and the Forecast "Scenarios" button/panel) grouped as two tasks since they're one coherent redesign of T174's toggle UI, not four separate features. T180 (per-account forecasted balance) resolves the "Before MVP launch" discussion item of the same name - the user's own rule for unlinked rows (pick the highest-balance connected account as of that date) was the missing piece that had kept it a discussion rather than a task.

- [x] **T177.** Reminders should be draggable (drag-and-drop reorder). *Reminders previously had no reorder mechanism at all - unlike Bills/Income/Debt/Savings/Misc/Budgets, which all got up/down buttons via T145's `useOrderedList` + `ReorderButtons`. That task's own comment explains why up/down was chosen there over real drag-and-drop: no touch support on native HTML5 drag, and a drag library needs explicit sign-off per CLAUDE.md's "no new dependencies" rule. This request asks for drag specifically, so built a small pointer-events-based drag (no library, no native HTML5 drag) that works on touch and mouse alike via `setPointerCapture` - the same technique used for slider/resize handles generally, new to this codebase.<br>`useOrderedList` gained a fourth method, `moveToIndex(id, targetIndex)`, alongside its existing `moveUp`/`moveDown` - same persisted-order mechanism (localStorage, client-only display preference, matching every other list), just reachable by index instead of by a fixed +/-1 step. A new `useDragReorder` hook in `RemindersPanel.tsx` tracks a `draggingId`, registers each row's DOM ref, and on every `pointermove` finds which sibling row the pointer's Y now sits above (comparing to each row's own bounding-rect midpoint) and calls `moveToIndex` live - so the list visibly reorders as you drag, not just on drop. A small `GripIcon` (new in `navIcons.tsx`) is the drag handle, `touch-action: none` set inline so touch-dragging doesn't fight the browser's own scroll gesture.<br>Scoped to the **active** reminders list only - completed reminders are collapsed by default and reordering them has no real use.<br>206/206 tests (unchanged - this is pure client interaction state, no engine/action logic), tsc, build, eslint clean (0 new problems; the pre-existing 6-problem baseline is unchanged and none are in the touched files). **Not browser-verified**: the same Browser-pane "stuck on Loading" issue as the rest of this session - confirmed server-side that every route returns clean 200s on a fresh account (`/`, `/forecast`) via `preview_logs`, but the account had zero reminders anyway (brand new), so even a working pane couldn't have shown an actual drag this time. Confidence comes from `useOrderedList`'s existing, already-tested-in-production persistence mechanism plus careful reading of the pointer-capture logic, not a screenshot.*
- [x] **T178.** Bills, Income, Debt and Savings are missing a date-range filter. *Confirmed the actual gap first rather than assuming T159 (which shipped "comprehensive filters") missed something wholesale: Misc already has a due-date range and Forecast has its own date filter, but the four recurring-item pages had none. Everything else T159 covers - name search, amount range, the account filter - already applies consistently across all five; only this one axis was missing.<br>Recurring items have no single "due date" the way a one-off does, so the filter is keyed on `start_date` (the one date field these pages already surface via T156's "Started {date}" label) rather than a computed next-occurrence date, which would need engine logic these plain list pages don't otherwise touch.<br>Same `DatePicker`-based "From"/"To" pair Misc already uses, added identically to `BillsClient.tsx`, `IncomeClient.tsx`, and `MonthlyGoalsClient.tsx` (shared by Debt/Savings) - three separate edits rather than a new shared component, matching how each of these pages already hand-rolls its own filter bar (T159's own precedent). Deliberately did **not** copy Forecast/Misc's other extra filter axes (transaction type, running-balance range, amount sort's absence here, infinite scroll) - those are Forecast/Misc-specific concepts that don't map onto a single-type finance list; this task is scoped to the one genuine gap.<br>206/206 tests (unchanged - presentational filter logic, no engine/action changes), tsc, build, eslint clean (0 new problems in the three touched files).*
- [x] **T179.** Make the connected-items list inside `BalanceModal` (T152) visually clearer and easier to scan. *The previous list was one flat, ungrouped `<li>` per item with the type spelled out inline as plain grey text ("Rent (bill, ₱1,800.00)") - readable, but scanning "which of my bills point here?" meant reading every row's parenthetical.<br>Now grouped by type in the same fixed order the sidebar nav uses (Bills, Income, Debt, Savings, Misc), each group headed by its own colored label and count, reusing T158's existing `TYPE_COLOR`/`TYPE_LABEL` maps (`forecastLabels.ts`) rather than inventing a second color system - the same green/orange/blue/purple a user already reads on the Forecast table. Each row's amount picks up that same color instead of a flat grey, and a running count badge on the "Connected items" heading itself answers "how many total" without counting rows. `ConnectedItem["type"]` is a plain string (never carries `"budget"` - T71 only ever links recurring/one-off items), so the two lookups fall back to the raw string if a type somehow isn't in either map, rather than a hard crash.<br>Disconnect behavior is completely unchanged - same form, same action, same one-item-at-a-time confirmation-free interaction; this was purely a display change.<br>206/206 tests (unchanged - presentational), tsc, build, eslint clean.*
- [x] **T181.** Give the History page its own filters (name search, type, amount range, date range). *History was the one remaining page with no filter bar at all - a plain server component rendering every settlement unconditionally. Split into a new `HistoryClient.tsx` (client component, filter state) plus a thinned `page.tsx` (just the two Supabase queries, unchanged), the same server/client split every other finance-record page already uses.<br>Four filters, reusing the exact shared components T159/T178 already established rather than inventing new ones: name search, a `MultiSelectChips` type filter (bill/income/debt/savings/misc/budget - the full `settlements.type` enum), a `DatePicker` range on `actual_date` (when a transaction really happened, not what it was forecasted for - the more useful axis for looking back), and `AmountRangeFilter` on `actual_amount`. No account filter or amount-sort control - `settlements` carries no `balance_id`, and the table is already date-sorted (most-recent-first) in a way a re-sort would fight rather than complement.<br>The table itself, its budget-row "-" placeholders (T59's own reasoning, comment carried over unchanged), and T176's `max-w-6xl` width are all untouched - purely additive filtering in front of the same rows.<br>206/206 tests (unchanged - presentational), tsc, build, eslint clean.*
- [x] **T184.** Toggled-off (T175 `active = false`) finance items should read more clearly disabled. *`opacity-60` alone let a bill/income/debt/savings/budget row's type-colored amount (green income, orange debt, etc.) stay fully saturated through the fade, so a disabled row could still read as "normal, just a bit lighter" rather than clearly off. Changed to `opacity-50 grayscale` at all five spots that use this pattern (`BillsClient.tsx`, `IncomeClient.tsx`, `ExtraClient.tsx`, `MonthlyGoalsClient.tsx`, `BudgetCard.tsx`) - the `grayscale` filter strips every row's own color out entirely regardless of type, so a disabled item reads as inert at a glance rather than needing the "Off" badge to be noticed first. The badge itself is unchanged.<br>206/206 tests (unchanged - a Tailwind class swap), tsc, build, eslint clean.*
- [x] **T185.** Let a user hide an Updates entry from their own view without deleting the underlying `activity_log` row. *T162's own migration made `activity_log` deliberately append-only - "no update/delete policy... even to its own owner" - so a `hidden` column on that table itself was never an option: it would need an UPDATE policy on a table whose entire point is that nothing can modify a row after it's written.<br>**Migration 0034**: a new `activity_log_dismissals` join table (`user_id`, `activity_log_id`, `dismissed_at`), select/insert-only RLS - no update/delete policy, since there's no "un-hide" feature being built and adding one speculatively would just be an unused write path. `activity_log_id` cascades on delete, so no addition to `wipeFinancialData.ts`/`wipe_test_data.sql` was needed: wiping a user's `activity_log` rows (already in both lists since T163) cascades their dismissals away for free.<br>`updates/page.tsx` now also fetches the current user's dismissed ids and filters them out **server-side** before any row reaches the client - a hidden entry is never sent to the browser at all, not hidden by CSS. New `dismissUpdate` action inserts one dismissal row and revalidates `/updates`; a small "×" button (reusing `CloseIcon`) sits next to each entry's timestamp.<br>**Applied to both Supabase projects** - production and the new Orium Staging project (set up earlier this session, see STAGING.md) - the first migration run against both since staging exists, the intended habit going forward rather than a one-off.<br>206/206 tests (unchanged - no engine logic), tsc, build, eslint clean.*
- [x] **T187.** Rework the Updates page (T163): stay-unread until explicit action, plus its own filters and lazy loading. *Two independent halves.<br>**Read-state**: removed the mount-time `useEffect` that silently called `markUpdatesSeen()` the instant the page rendered - that was T163's whole design, and this task reverses it deliberately. `markUpdatesSeen`'s existing bulk watermark (`preferences.activity_log_seen_at`) is kept for a new "Mark all as read" button (shown only while at least one entry is actually unread), and **migration 0035** adds `activity_log_reads` - the same insert-only join-table shape T185's `activity_log_dismissals` just established - for marking one entry read individually without moving the bulk watermark. An entry is unread when it's newer than the watermark **and** has no row in this table; a small check-mark button next to each unread entry's timestamp writes exactly that one row.<br>The sidebar's unread badge (`(app)/layout.tsx`, T163) needed a real fix here, not just a `revalidatePath` call: its count query only ever knew about the watermark, so an individually-read entry would still have inflated the badge even though the Updates page itself no longer considered it new. Added a second count query (an inner join from `activity_log_reads` to `activity_log`, filtered to entries newer than the watermark) and subtracted it from the raw count - the badge and the page's own "new" definition agree now.<br>**Filters and lazy loading**: name search (matches `entity_name` or `detail`), an entity-type `MultiSelectChips`, and a `DatePicker` day range - same shared components T159/T178/T181 already established, no new ones invented. Lazy loading mirrors Forecast's own client-side batching (T49) exactly - `visibleCount`/`ROWS_PER_BATCH`, an `IntersectionObserver` sentinel, the same render-phase `filterKey` comparison to reset the visible window on a filter change instead of an effect - **with one deliberate difference**: Forecast's observer root is a ref to its own internal scrollable div (that page has a fixed-height sidebar layout), but Updates is a plain full-page scroll with no such container, so the observer root is left as the default browser viewport rather than pointed at a div that would never actually clip or scroll its own content.<br>**Applied to both Supabase projects** (production and Orium Staging), continuing the habit T185 started now that staging exists.<br>206/206 tests (unchanged - no engine logic), tsc, build, eslint clean (one `react-hooks/exhaustive-deps` warning surfaced while writing this, fixed by inlining the unread predicate into the `useMemo` callback instead of calling a same-scope helper function from inside it).*
- [x] **T188.** When a finance item has an "edited from its usual schedule" mark (✎), add a way to view all its modified future transactions from its own finance page, and edit one from there. *Scoped to **Bills, Income, Debt, Savings** - Misc (one-off items) never carries the ✎ mark in the first place (it has no recurrence to deviate from, and settling one deletes the row entirely rather than leaving a schedule to compare against), so there was nothing to extend there.<br>Debt and Savings already had half of this - clicking an item opens `ItemTransactionsModal` (built 2026-07-24) showing its Upcoming/Paid transactions - but that view was pure display. Extended it, then reused it rather than building a second surface: **Bills and Income now get the exact same click-to-view behavior** (their own `viewingItem` state, `ItemTransactionsModal` render, and page.tsx now also runs `loadForecast()` plus a `settlements` query the same way `debt`/`savings` pages already did, grouped by `sourceId` with past-due excluded - the same "a months-old missed payment showing as next up would read as a bug" reasoning T150 already established for Debt/Savings).<br>**The actual new capability**: clicking a row in the "Upcoming" tab now opens the real `EditSettleModal` - the same one Forecast/Calendar use - instead of just showing the date and amount inertly. No second edit mechanism invented; `ItemTransactionsModal` now takes `currency`/`balances` (needed by `EditSettleModal`) and swaps to rendering it in place of its own list when a row is clicked, closing back to the list rather than stacking two overlays.<br>**One correctness detail carried over deliberately**: T174 excluded `fromScenario` rows from being clickable in the Forecast table and the Calendar for a real reason - a scenario row's `sourceId` belongs to `scenario_recurring_items`, not `recurring_items`, so `EditSettleModal`'s actions would write against an id that doesn't exist in the tables they actually know about. The same guard is applied here, since scenarios cover bill/income/debt/savings items and could otherwise put a scenario row into this exact "Upcoming" list.<br>206/206 tests (unchanged - no engine logic), tsc, build, eslint clean (0 new problems across all seven touched files).*
- [x] **T180.** Per-account forecasted balance, resolving the "Before MVP launch" discussion item of the same name. *The discussion had two open questions blocking it from becoming a real task: what happens to a row with no connected account, and whether per-account balances are allowed to go negative. The user's own answer to the first ("pick the highest-balance account as of that date") was the missing piece - the second was already implicitly "yes" (the combined total is already allowed negative, and there's no reason a single account should be clamped differently).<br>**New pure engine function**, `findAccountLowestPoints` (`src/lib/engine/accountBalances.ts`), modeled directly on the existing `findLowestBalancePoint`/`findFirstDangerPoint` (`lowestBalance.ts`) rather than folded into `forecast.ts` itself - a second, independent pass over the same rows answering a different question (per-account, not combined), which doesn't change `generateForecast`'s own output at all. Every row is attributed to an account by `row.balanceId` when it has one, or otherwise to whichever tracked account currently holds the highest balance just before that row - **this rule needs no special-casing for budget rows**: `budget_replenish`/`budget_entry` rows never carry a `balanceId` at all (budgets have no T71 connected account of their own), so they fall through to the same "highest balance" fallback automatically. Fees (T172) are subtracted the same way `forecast.ts` already does, on whichever account actually received the row.<br>**Scoped down to the single most actionable number** rather than a full per-date breakdown table: a hover tooltip (native `title`, same mechanism every other tooltip in this app already uses - "edited from schedule" ✎, etc. - which is inherently desktop-only since it only triggers on mouse hover, satisfying that part of the original scoping without any extra media query) on each account chip in the Forecast page reads "Lowest projected: ₱X on <date>", or "Not projected to dip below ₱X" when the account's own starting balance is already its lowest point across the whole horizon (mirroring `findLowestBalancePoint`'s own starting-balance-as-candidate convention). Computed over the full, unfiltered forecast, matching the "filters narrow what's displayed, not what's real" convention Total Balance and Peaks and Drops already established.<br>214/214 tests (8 new, covering connected/unlinked attribution, the "follow the money" re-evaluation after a balance shift, fee deduction, an unknown balanceId falling back correctly, and the zero-accounts edge case), tsc, build, eslint clean.*
- [x] **T186.** Add/Take/Move funds for accounts, instead of the Balances page's blind "set a new absolute amount" edit. *Mirrors the Budget ledger's Add funds/Take funds pattern (T75/`FundsModal.tsx`) but goes further two ways: a taggable comment (this task's own explicit ask - Budget's own version has no note field at all), and a third "Move funds" operation transferring between two of the user's own accounts.<br>**Migration 0036**: `balance_transactions` (mirrors `budget_entries`' shape - `entry_date`/`amount`/`direction`/`note`), full CRUD RLS matching `budget_entries` exactly (this is a correctable ledger, not an append-only audit table like T162's `activity_log`). Answered the same "source of truth" question T57 already settled for budgets: `balances.amount` stays the authoritative cached current value, updated in the same read-then-write step as the ledger insert (the same style `applyToBalance` already uses for settlements), not recomputed from this table on every read.<br>**A "move" writes two ledger rows**, not a third "transfer" direction - an outgoing entry on the source account and an incoming one on the destination, exactly the way a real transfer between two bank accounts shows up as two separate lines on two separate statements. A blank comment defaults to "Moved to/from {other account}" rather than a bare blank note.<br>**The actual behavior change asked for**: `updateBalance` no longer touches `amount` at all - editing an existing account is now metadata-only (name/comments/fee). `BalanceModal`'s Amount field is now read-only in edit mode (the create form is untouched - setting a brand-new account's starting balance was never the "blind update" being complained about), with its `FormTip` rewritten to point at the three new buttons instead of describing a field that no longer accepts input.<br>New `AccountFundsModal.tsx` (one component for all three modes, matching `FundsModal.tsx`'s own "one component, not three near-identical files" precedent), three new buttons on each Balances page row (Add funds / Take funds / Move funds, Move hidden when there's only one account to move between).<br>214/214 tests (unchanged - server actions against Supabase, this project's Vitest is engine-only, same position as T151/T172/every other action-only change this session), tsc, build, eslint clean.<br>**Three follow-ups requested the same session, added directly rather than as new T-numbers**: (1) the source account of a Move now also pays its own T172 transaction fee, the same "silently folded into the real balance update, never shown as its own ledger line" convention `applyToBalance` already established - a new `adjustBalanceWithOwnFee` helper, used only for a move's "from" leg (Add/Take and a move's destination side don't incur it, matching the user's own framing). (2) `BalanceModal`'s T179 grouped connected-items list gained a collapse toggle (expanded by default, not persisted - a transient per-open preference on a modal that forgets its own state anyway). (3) A new "History" button per account opens `AccountHistoryModal.tsx`, a read-only list over that account's own `balance_transactions` rows ("quickly see edits on them") - `accounts/page.tsx` now also queries that table and groups it by `balance_id`. All three: tsc, build, eslint clean, 215/215 tests (unchanged by these three - no engine/action-shape changes, just a fee calculation, a UI toggle, and a read-only list).*
- [x] **T182.** Extend Scenarios (T174) to include budgets, reversing T174's deliberate v1 exclusion now that the user asked for it directly. *Deliberately **not** full parity with the real Budgets page - no allocation, no own replenish schedule, no linked-income auto-replenish. A scenario budget is a plain named "pot" the user logs planned future incoming/outgoing entries against, merged into the forecast as `budget_entry`-sourced rows exactly like a real manual budget's future entries already work (`futureBudgetLedgerEntries`). Reproducing the full replenish-schedule/income-link machinery for a hypothetical, throwaway scenario is exactly the scope-widening T174's own note warned about - kept to the same "one creation modal per item type" shape T174 established for scenario bills/income/debt/savings and misc, rather than cloning the real Budgets page's richer Entries/Log spend/Add funds/Take funds split.<br>**Migration 0037**: `scenario_budgets` (id/name only) and `scenario_budget_entries` (mirrors `budget_entries`' shape), separate tables not a tag column - same leak-prevention architecture T174's own migration 0033 chose. Full CRUD RLS, matching the real `budget_entries`' own policy shape (this is a correctable ledger like that table, not append-only like T162's `activity_log`).<br>**Engine**: `Budget` gained `fromScenario?: true`, passed through on both `budget_replenish` and `budget_entry` rows in `forecast.ts` from `budget.fromScenario` - no new click-guard code needed anywhere, since ForecastClient/CalendarClient's existing `!row.fromScenario` checks (T174) already cover these rows automatically once the flag is set, the same way they already cover recurring/one-off rows.<br>**`loadForecast()`** fetches `scenario_budgets`/`scenario_budget_entries` when a scenario is active and pushes mapped rows into the same `budgets`/`budgetEntries` arrays already being built - additive, the same integration point T174 established, no new merge logic needed.<br>**`activateScenarioPermanently`** extended to also copy scenario budgets/entries into real ones: budgets are inserted one at a time (not bulk) specifically to capture each new real id and pair it with its old scenario id, since `budget_entries` need the *new* `budget_id` remapped, not the scenario one.<br>215/215 tests (1 new, confirming a scenario budget's own `budget_entry` rows carry `fromScenario` while a real budget's don't), tsc, build, eslint clean. Applied to both Supabase projects.*
- [ ] **T183.** Redesign the Scenarios toggle UI and reverse T174's "one active scenario at a time" constraint - **unlimited scenarios can be active simultaneously**, all merged into the same forecast at once. Replaces `preferences.active_scenario_id` (a single FK) with a many-to-many activation state. New Forecast-page UI: a "Scenarios" button beside the existing "Filter" button, carrying a count badge (number of currently-active scenarios); opening it lists every saved scenario with its own on/off toggle, each one immediately updating the forecast table when flipped. The single amber `ScenarioModeBar` banner shrinks and becomes a summary ("2 scenarios active" with a way to see which) rather than naming one scenario. Scenario-sourced rows in the forecast table stay visually highlighted (T174's `fromScenario` badge plus a stronger row treatment, e.g. a background tint, so they read as clearly hypothetical at a glance, not just badge-marked).

### Completed work (T1-T147)
Full build write-ups live in **ARCHIVE.md**, in this same order. This index exists so a task number can be found quickly without opening it.

**Done**

- **Phases 0–4 (T1–T21)**: v1 built and deployed to Vercel — schema, pure engine + tests, all CRUD pages, Forecast/History/Dashboard, set...
- **Phase 5 (T22–T23)**: integer-multiplier monthly totals (Bug #1); greeting name fallback (Bug #2).
- **T24–T25**: budgets v1 tables + engine (monthly-only, no rollover) — the baseline Phase 6B converts.
- **T41**: sample data seed (`supabase/seed.sql`).
- ~~T26–T27~~ **cancelled** — superseded by Phase 6B; never build them.

**Phase 6A — Flexible recurrence (done)**

- T32 - Migration 0004: recurrence columns + enums + constraints + backfill; legacy-column drop split out...
- T33 - Engine: day/week/month(days)/year expansion + ends rules; port existing tests; add 6A cases (list...
- T34 - Engine: nth-weekday resolution (incl. last-X) + generalized `monthlyEquivalent`; tests.
- T35 - Recurrence picker wired into all four CRUD forms; human-readable rule summary per row.

**Phase 6B — Budgets v2 (after 6A)**

- T36 - ALTER migration on budgets (allocation, carryover, linked income, recurrence columns). Existing r...
- T37 - Engine rework: boundary/cycle/carryover model + tests (list above).
- T38 - Budgets page: CRUD + replenish-source UI + entries + log spend.
- T39 - Forecast: budgets panel + budget rows + quick log-spend.
- T40 - Dashboard card + History budget tags + empty states; verify summary stats ignore budgets except t...
- T42 - Editable transactions + stale-override cleanup, logged 2026-07-21 (user request, after T39), both...
- T43 - Future-dated spends as a first-class feature (user decision 2026-07-21, upgrading Bug #3 from dis...

**Phase 7 — Notion-style redesign (done)**

- T28 - Foundation: tokens, Inter, full-width shell, sidebar, base components — incl. recurrence picker...
- T29 - Restyle Dashboard + Forecast (list, chips, modals, reminders, budgets panel) — incl. the edited-o...
- T30 - Restyle all CRUD pages (Balances, Bills, Income, Debt, Savings, Extras, Budgets).
- T31 - Restyle Auth, Settings, History; consistency pass; screenshot review with the user before closing.
- T44 - Collapsible sidebar (added 2026-07-22, user request).

**Phase 8 — Forecast & Dashboard clarity (scoped 2026-07-22, not yet built)**

- T45 - Budget rows in the Forecast table read as confusing non-transactions (user's own question: "shoul...
- T46 - "Lowest balance ahead" indicator.
- T47 - Seed data negative/near-negative scenario.
- T48 - Peaks and Drops reformatted as a year × month grid (user request 2026-07-22) instead of the curre...
- T49 - Fixed-height, independently-scrollable containers instead of ever-growing page sections (user req...
- T50 - Forecast table filter bar (user request 2026-07-22): date (range), name (text match), type (multi...
- T61 - Forecast table density (user request 2026-07-23): shrink the row font and vertical padding as sma...
- T62 - Balance-range color highlight scoped to just the running-balance cell, not the whole row (user re...
- T63 - Peaks and Drops reflows instead of needing horizontal scroll (user request 2026-07-23, follow-up...
- T64 - Nav order: moved "Forecast" up to directly below "Dashboard" in the sidebar (user request 2026-07...
- T65 - Reminders panel becomes a full-height collapsible right sidebar on the Forecast page, mirroring t...
- T66 - Follow-up to T65 (user request 2026-07-23): move both sidebars' collapse/expand toggle onto the m...
- T67 - Peaks and Drops color-coded by `balance_ranges`, matching the Forecast table (user question 2026...
- T68 - Budget entries get their own scrollable, lazy-loading container on the Budgets page (user follow...
- T69 - Budget progress bar switched from time-based to money-based (user follow-up 2026-07-23/24) — the...

**Phase 9 — Finance category page enhancements (scoped 2026-07-22, not yet built)**

- T51 - Edited-occurrence indicator on finance category pages (user request 2026-07-22), extending T29's...
- T52 - Filters on each finance category page (user request 2026-07-22): Bills/Income/Debt/Savings/Extras...

**Phase 10 — Budgets rewritten as a running ledger (scoped 2026-07-23, in progress)**

- T53 - Migration 0009: `budget_entries.direction` (`'incoming'`/`'outgoing'`, not null, no default).
- T54 - New engine module `budgetLedger.ts`: `computeBudgetBalance` (Σ incoming − Σ outgoing up to a date...
- T55 - Budgets page rework: remove the carryover checkbox and "on a schedule" replenish option from `Bud...
- T56 - Wire replenishment: `settleOccurrence` (`forecast/actions.ts`) checks whether the settled item is...
- T57 - Forecast cleanup: delete `BudgetsPanel` and the boundary-row branch of `EditSettleModal`; simplif...

**Phase 11 — Budget replenish schedules + forecast deductions (scoped 2026-07-23, done)**

- T58 - Migration + engine only, nothing wired into the UI yet. Migration `0011_budget_replenish_schedule...
- T59 - Forecast + settle wiring. `forecast.ts` now projects a real deduction row (`sourceType: "budget_r...
- T60 - UI: `BudgetModal` gets "Replenish every" back as a third `SegmentedControl` option; Dashboard's B...

**Phase 12 — Filter modal, connected accounts, goal progress (scoped 2026-07-24, not yet built)**

- T70 - Forecast filter bar moves into a modal (user request 2026-07-24): a "Filter" button replaces the...
- T71 - Optional connected account per finance item (user request 2026-07-24): every settleable type — bi...
- T72 - Per-item progress bars for Debt/Savings (user request 2026-07-23, scoped 2026-07-24).
- T73 - Rename "Balance" → "Account" across the UI (user decision 2026-07-24 — they weighed that calling...
- T74 - Per-item transactions view on Debt/Savings (user request 2026-07-24): clicking an item (not its E...
- T75 - Budgets page card redesign (user request 2026-07-24).
- T76 - Lowest Balance Ahead card shows more than a negative/positive binary (user request 2026-07-24, mo...
- T77 - Direction-aware connected-account label (user request 2026-07-24, moved from REMINDER.md 2026-07...
- T78 - `RecurrencePicker`'s "Ends" control needs a better UX pass (user request 2026-07-24).
- T79 - Every date, including form date *inputs*, should follow the human-readable format Phase 7 already...
- T80 - Make the "lowest balance" tier labels (T76's "Comfortable low point", "Healthy low point", etc.)...
- T81 - Forecast Insights container (user request 2026-07-24). The "lowest balance ahead" line lived as p...
- T82 - Amount sign auto-inferred by finance category when editing/settling a forecasted transaction, so...
- T83 - Dashboard's Accounts/Remaining Debt/Savings/Budgets cards each get a "Manage" button linking to t...
- T84 - Reminders get a "completed" state, distinct from delete (user request 2026-07-25, moved from REMI...
- T85 - Free-tier 5-year tracking cap (user request 2026-07-25): "every user should have a max of only up...
- T86 - Dashboard's Accounts list gets divider lines between entries (user request 2026-07-25).
- T87 - "Highest" balance tier's green pill had low text contrast (user request 2026-07-25).

**Phase 13 — Sample data reset (scoped 2026-07-24, done)**

- T88 - Full wipe of the test account's data + a rewritten canonical `supabase/seed.sql`.

**Phase 14 — Full mobile compatibility (scoped 2026-07-24, not yet built)**

- T89 - Responsive navigation: the left sidebar becomes a hamburger-triggered drawer below `lg`; the Fore...
- T90 - Forecast page mobile: 3-column table (Name / Amount / Balance) at narrow widths — proposal to con...
- T91 - Dashboard mobile pass: card stacking/order, Peaks and Drops (already reflows since T63) verified...
- T92 - CRUD pages + shared components mobile pass: all six category pages, every modal, and `DatePicker`...
- T93 - Settings / History / Auth mobile pass + a full-app sweep at phone (375px) and tablet (768px) widt...

**Phase 15 — Branding (scoped 2026-07-24, not yet built)**

- T94 - Logo: 2–3 minimalist SVG directions mocked up for the user to choose from (pure SVG, no image ass...
- T95 - Branded login / signup: the four auth pages (+ `AuthCard`) get the logo and a light branded treat...

**Phase 16 — Onboarding (scoped 2026-07-24 — the user's main selling point for new users; the precision/UX bar is deliberately high)**

- T96 - Settings Preferences UI separation (user request 2026-07-24): currency, balance color thresholds...
- T97 - New-user sample data + reset: signup seeds the sample dataset into the new account; an always-fin...
- T98 - Spotlight tour component (hand-built overlay: dim + highlight + step text + next/skip) + the Dash...
- T99 - "Getting started" checklist: a persistent, dismissible panel tracking the add-data steps in order...
- T100 - Onboarding polish pass: exact copy, empty states, skip/replay ("Review the tour again"), and a fu...
- T101 - "First goes negative" as a second, distinct stat alongside Lowest Balance Ahead (user feedback 20...
- T102 - End-of-tour "keep or reset sample data?" prompt (user request 2026-07-25, part 1 of a larger onbo...
- T103 - Opt-in "preview with sample data" mode, available to any user regardless of whether they already...
- T104 - Dashboard card reorder (user request 2026-07-26): Lowest Balance Ahead and Peaks and Drops moved...
- T105 - Getting Started checklist (T99) tweaks (user request 2026-07-26): debt/savings marked optional al...
- T106 - "Extras" renamed to "Misc" everywhere in the UI (user request 2026-07-26).
- T108 - Forecast intro tour (T98) gets a line about settling transactions to keep the forecast accurate (...
- T107 - Date guardrails (user request 2026-07-26): a new bill/income/debt/savings/misc item can't start o...
- T109 - Sample dataset personalized to the user's actual family, and long em dashes removed from every se...
- T110 - "How to use" guide: walk the user through the whole app in one smooth flow, start to end. (User r...
- T114 - Lightbulb "How to use" icon in the main nav, directly below Settings - the part of T110's origina...
- T111 - Rename the route `/extra` to `/misc`, to match T106's user-facing "Misc" rename. (User request 20...
- T115 - Required onboarding wizard for completely new users, distinct from T110's skippable tour (user re...
- T116 - Guided tour rebuilt from scratch as a single short walkthrough, and guided setup (T115) changed f...
- T119 - First-login onboarding gets a "welcome" choice screen (tour vs. guided setup), and progress throu...
- T120 - Onboarding audit and rebuild of the short tour, plus explainer tips across every add/edit form, p...

**Phase 18 - Onboarding, rebuilt (scoped 2026-07-26; flow agreed 2026-07-26, T122)**

- *Tour feedback (user, 2026-07-26):* (1) the Next button should show a loading state before the step's card disappears, so a click doesn...
- *Structural fragility found during T114 (2026-07-26):* the `preferences` row is created by app code at exactly two call sites (`login()...
- *First-run finding from T121's own testing (2026-07-26):* a brand-new, empty account immediately shows a red "Goes negative by ₱0.00" w...
- *Surfaces to rationalize:* welcome modal, spotlight tour, guided-setup wizard, post-tour prompt, getting-started checklist, sample-data...
- **One question, asked once.** A new account sees a single welcome modal: show me around / help me set up / explore on my own. The answe...
- **Nothing blocks.** `(app)/layout.tsx`'s hard gate (T115) is removed outright. The wizard keeps a quiet "Exit setup for now" on *every*...
- **Show me around** runs the 6-step tour (T120). It no longer offers guided setup mid-tour and no longer ends in a prompt - it simply ends.
- **Help me set up** walks accounts, then income, then bills, offering debt/savings/budgets/misc afterwards as optional. Its starting scr...
- **Explore on my own** goes straight to the app.
- **One progress surface.** The Getting Started checklist (T99) on the Dashboard becomes the single place tracking what's left. It alread...
- **Deleted, because each re-asks a question already answered at the welcome modal:** the post-tour prompt and its three options (T120), ...
- **Empty is a designed state**, not an accident - starting with the Dashboard's Lowest Balance Ahead card, which currently greets a bran...
- T121 - Dev-only "fresh account" route, so onboarding can be tested end to end without hitting Supabase's...
- T122 - Onboarding design pass: agree the whole flow end to end before building any of it. What a brand-n...
- T123 - Stop the repeats and stop the blocking. Remove the post-tour prompt (`OnboardingNextStepPrompt`...
- T124 - Tour polish - the four issues the user reported 2026-07-26 while testing: (1) the Next button nee...
- T125 - Real empty states. The Dashboard's Lowest Balance Ahead card currently shows "Goes negative by ₱0...
- T126 - Prune the onboarding state left unused once T123-T125 land. Nine `preferences` columns currently...
- T127 - Four small follow-ups from live testing after T126 (user request 2026-07-26): (1) the short tour...
- T128 - Replace the "enter it as a positive number" style form tips with a keyboard-level guard, so a neg...
- T129 - Five items left in REMINDER.md from earlier tour/guided-setup testing (user request 2026-07-26)...

**Phase 17 - UI polish and Dashboard customization (reordered 2026-07-26)**

- T112 - All buttons everywhere get a hand-cursor (`cursor-pointer`) and a slight color change on hover. (...
- T113 - Style rule: never use long em dashes anywhere in the app - user-facing strings, seed/sample data...
- [x] **T117+T118.** Dashboard customization panel plus reordering, built together at the user's request (2026-07-27) rather than in the ...
- T130 - A dismissed "Quick tour" or "Guided setup" sidebar shortcut (T127) used to disappear outright, re...
- T131 - Tour bug from REMINDER.md (user report 2026-07-27): the highlighted Bills nav item during the sho...
- T132 - Signup confirmation by typed 6-digit code instead of an emailed link, ahead of giving the app to...
- T133 - Loading feedback on every button that needed it, ahead of giving the app to real users (user requ...

**Phase 19 - Pre-launch stress test, bug sweep & polish (scoped 2026-07-28, not yet built)**

- T134 - Full manual stress test pass across every page and flow, at both desktop (1280px) and mobile (375...
- T135 - Fix: recurring items collapsing to "today's day-of-month" regardless of the date or frequency act...
- [x] **T136+T137.** Fix: in guided setup, clicking "Add another X" then Cancel/Close left the add-item modal stuck open with no way out,...
- T139 - Fix: Add Debt's date picker gets clipped on its right edge inside the modal - the calendar is nev...
- T140 - Fix: Misc still shows at least one raw `YYYY-MM-DD` date, missing Phase 7's "Jun 1, 2025" human-r...
- T141 - Misc copy cleanup: remove the redundant "info" wording on the Money in / Money out fields (should...
- T142 - Verify: editing a Misc one-off transaction from the Forecast table should update what's shown on...
- T143 - Feature: ascending/descending sort by amount on every finance list (Bills, Income, Debt, Savings...
- T144 - Feature: a "varying twice-monthly" recurrence option (e.g. 16th & 31st, 5th & 25th) distinct from...
- T145 - Feature: user-orderable finance lists (Bills, Income, Debt, Savings, Budgets, Misc) - not the Das...
- T146 - Verify/clarify: REMINDER.md asked for "ends on date should limit to 5 years from the start date."...
- T147 - Fix: `EditSettleModal`'s "Delete this entry" (for a future-dated budget ledger entry) closed the...

### Out of scope
Payments/subscriptions, mobile app, notifications, bank sync, multi-user families.

**Two of these are actively contested and this list is out of date (noted 2026-07-31).** *Notifications* has now been requested four separate times (phone notifications, a 12am due-today email, past-due alerts, and the "one email with multiple toggles" shape) and is very likely to be lifted. *Multi-user families* is contested from two directions: the partner-sharing item in "Before MVP launch", and T163's updates feed, which is deliberately scoped to a single shared account precisely so it does not cross this line. Nothing here should be treated as settled without the user saying so; the exclusions stay in force until then, but they should be revisited as a group rather than eroded one task at a time.
