# Orium — Product Spec

**The single source of truth.** Product definition, data model, engine rules, and the active roadmap live here. Working rules for Claude live in CLAUDE.md; open bugs in BUGS.md; build write-ups for finished tasks in ARCHIVE.md (indexed at the bottom of this file); the production-safety and staging plan in STAGING.md. (This file absorbed and replaced SPEC2.md in July 2026; task numbers are continuous across both.)

*Split 2026-07-31: this file had reached ~318KB, almost entirely completed-task write-ups, which made a file that must be read before every session too large to read in one pass. The history moved to ARCHIVE.md verbatim and nothing was reworded. Finished tasks are still findable here through the index in "Completed work" at the bottom.*

## What Orium is

Orium is a family cash-flow forecasting app. Users manually enter their account balances, recurring bills, income, debt payments, savings goals, one-off expenses, and spending budgets. Orium projects every upcoming transaction in chronological order and shows the **running future balance** at each date, color-coded by safety level. The core promise: *know exactly how much money you'll have on any future date, and never miss a bill.*

v1 (shipped) rebuilt the original Orium (github.com/nathanelcorpuz/orium) on a cleaner engine. The current work replaces its fixed 4-frequency recurrence with calendar-style rules (Phase 6A), restyles everything Notion-like (Phase 7), and adds Forecast/Dashboard clarity features (Phase 8/9). Budgets converted to replenishing cycles in Phase 6B, then — after that model proved confusing in practice — got rebuilt again as a simple running ledger in Phase 10 (in progress); see "Budgets v3" below for why.

## Core purpose (user, 2026-07-26)
**Orium exists to show you how much money you'll have at any point in the future.** Everything else - accounts, bills, income, debt, savings, budgets, misc, settling - is machinery serving that one question; the forecast's running balance *is* the product, not one page among several. User-facing copy should lead with that promise (a future balance you can point at) rather than generic budgeting or expense-tracking language, and settling should be explained as what keeps that number honest. Applied to the tour's opening/forecast steps and the welcome modal in T120.

## Writing style
- **No long em dashes (—) anywhere** - not in UI copy, seed/sample data, commit messages, or this file's own prose (code comments in this file are the one exception, to keep entries readable). Use a plain hyphen `-` instead. (User rule, 2026-07-25/26; enforced retroactively in seed data by T109, going forward by T113.) **Automatically enforced** by `src/lib/writingStyle.test.ts` (T113), which fails `npm run test` naming the exact file and line. That check covers `src/` and the re-runnable `supabase/*.sql` scripts; `supabase/migrations/` is deliberately outside it (an applied migration records what really ran, so it isn't rewritten to satisfy a later style rule), as are this file and the other project docs, whose historical entries still contain them.

## Tech stack (fixed — do not add alternatives)

- Next.js (App Router) + TypeScript + Tailwind CSS
- Supabase: Postgres, Auth (email/password), Row Level Security
- `@supabase/supabase-js` + `@supabase/ssr` per official Next.js patterns
- Vitest for unit tests (engine only)
- No other dependencies without asking the user first

## Core design rules (non-negotiable)

1. **Money is integer centavos.** Never floats. `₱1,500.00` = `150000`. Format only at the UI layer. Default currency `₱`, user-configurable.
2. **Due dates are calendar dates** — `YYYY-MM-DD` strings / Postgres `date`. Never UTC timestamps for due dates.
3. **Rules, not pre-generated rows.** Recurrence is stored as rules; occurrences are computed on the fly.
4. **The forecast engine is pure TypeScript** in `src/lib/engine/` — no database or network calls, fully unit-tested.
5. **Every table has `user_id`** with owner-only RLS.
6. **Make sure any edge cases are handled properly when fixing bugs or adding/editing new features.**
7. **Always keep UI and UX clean on any changes made, responsive on all devices.**
8. **Mobile-first, not responsive-as-an-afterthought** (user rule, 2026-08-04, sharpening rule 7 above). Every UI change is designed for the smallest viewport first, then scaled up with `sm:`/`md:`/`lg:` overrides - a new component's unprefixed Tailwind classes are its mobile styling, not its desktop styling narrowed down.

## Data model (Postgres — migrations in `supabase/migrations/`)

### `balances`
Real money accounts. `id, user_id, name, amount (bigint centavos), comments, created_at, updated_at`. Plus (T172, migration 0032): `transaction_fee_centavos (bigint >= 0, default 0)` - a flat cost auto-deducted from every forecasted transaction connected to this account, both incoming and outgoing.

### `recurring_items`
One row per recurring rule (bills, income, debt, savings). Amount sign convention: income positive; bills/debt/savings negative (DB-enforced).

Identity: `id, user_id, name, type (bill|income|debt|savings), amount, comments`. Plus (T232, migration 0050): `auto_debited (bool, default false)` - marks a bill/debt/savings item as paid automatically by its connected account, shown as a pill wherever that item's own name renders (its category page row, and the Accounts page breakdown, T236). Not meaningful for income; never set on `one_off_items`.

**Recurrence rule (new model, live from migration 0004):**
| column | type | notes |
|---|---|---|
| `interval` | int ≥ 1 | repeat every N units |
| `unit` | enum | `day` \| `week` \| `month` \| `year` |
| `weekdays` | int[] nullable | 0=Sun…6=Sat; required when unit=week (multi-select) |
| `days_of_month` | int[] nullable | 1–31 for unit=month; days beyond a month's length clamp to its last day |
| `ordinal` + `ordinal_weekday` | int nullable pair | nth-weekday monthly rules: ordinal 1–4 or −1 (last); (3,2)=third Tuesday, (−1,5)=last Friday. unit=month uses **either** `days_of_month` **or** the ordinal pair |
| `start_date` | date | anchor; first possible occurrence |
| `ends_type` | enum | `never` \| `on_date` \| `after_count` (`never` stays available except for Debt/Savings, T72 — as of T85, Phase 12, it's implicitly bounded by the forecast horizon, which is itself capped at `MAX_TRACKING_YEARS` from today; an explicit `on_date`/`after_count` end is separately rejected app-level if it would resolve past that same cap, no DB constraint) |
| `end_date` | date nullable | set iff ends_type=on_date |
| `occurrence_count` | int nullable | set iff ends_type=after_count |

**Legacy columns `frequency`/`day_of_month`/`weekday` are gone** — dropped by migration 0005 (2026-07-21) once T35 shipped. Backfill mapping used to populate the new columns before the drop: monthly → (1, month, days=[day_of_month, falling back to start_date's day]) · weekly → (1, week) and biweekly → (2, week), both with weekdays=[dow(start_date)] — **not** the legacy `weekday` column, which the v1 engine never read and which may have been null or disagreed with the real schedule · semi_monthly_15_30 → (1, month, days=[15,30]); existing rows got `ends_type='on_date'` + their then-current end_date.

### `occurrence_overrides`
Per-instance edits to a recurring rule (calendar-exception style). `id, user_id, recurring_item_id (fk), original_date, new_date, new_amount, new_name, skipped (bool)`. Unique on (`recurring_item_id`, `original_date`). Plus (Bug #21, migration 0051): `new_balance_id (uuid nullable, references balances on delete set null)` and `balance_id_overridden (bool, default false)` - the Forecast Edit tab's connected-account field, now a per-occurrence override like the other three fields (T193 originally made it a permanent change to the item's own `balance_id`; reversed as a bug). `balance_id_overridden` exists because `new_balance_id` alone can't tell "never touched" apart from "explicitly set to no account" - both are plain SQL null.

### `one_off_items`
"Extras" — single dated transactions. `id, user_id, name, amount (signed), due_date, comments`.

### `settlements`
"History" — what actually happened, written when the user settles an occurrence or logs a budget spend. `id, user_id, source_type (recurring|one_off|budget), source_id, name, type (bill|income|debt|savings|extra|budget), forecasted_amount, actual_amount, forecasted_date, actual_date, forecasted_balance`. Settling a recurring occurrence also writes a `skipped` override so it leaves the forecast. Plus (T217, migration 0043): `balance_id (uuid nullable, references balances on delete set null)` and `budget_account_id (uuid nullable, references budget_accounts on delete set null)` - which real account this specific settlement moved money in or out of, so History can show it. At most one is ever set on a given row (a bill/income/debt/savings/extra settlement can carry `balance_id`; a budget-type settlement can carry `budget_account_id`); either can be null when the underlying item/budget wasn't connected to any account.

### `budgets` (T36 columns live; app still on the pre-6B baseline until T37/T38)
`id, user_id, name, created_at`, plus (still pre-6B, current app): `monthly_allocation (bigint ≥ 0)`. Plus (T36, migration 0006, added 2026-07-21): `allocation (bigint ≥ 0, NOT NULL, backfilled 1:1 from monthly_allocation)`, `carryover_enabled (bool, default true)`, `linked_income_id (uuid nullable, REFERENCES recurring_items ON DELETE SET NULL — app-level rule: must point at a type=income item, not DB-enforced)`, and the full recurrence rule shape (all nullable — `start_date, interval, unit, weekdays, days_of_month, ordinal, ordinal_weekday, ends_type, end_date, occurrence_count`; `interval`/`unit`/`start_date`/`ends_type` are constrained to move together as a complete-or-nothing group). `monthly_allocation` is dropped by migration 0007 once T38 ships and the app no longer reads it — same additive-then-drop pattern as 0004/0005. Plus (T204, migration 0040): `budget_account_id (uuid nullable, REFERENCES budget_accounts ON DELETE SET NULL)` - optional link to a `budget_accounts` row (below).

### `budget_accounts` (T204, migration 0040)
`id, user_id, name, amount (bigint), comments (nullable), created_at`. Separate storage for budgets - never read by `generateForecast`/`loadForecast`, never counted toward Total Balance. A linked budget's ledger activity (replenish, spend, manual add/take) moves this account's `amount` the same way a connected main account moves on settle.

### `budget_budget_accounts` (T218, migration 0043)
Many-to-many link between a budget and however many budget accounts it draws from - replaces `budgets.budget_account_id` (single optional FK, dropped by migration 0045 once T218 shipped and every caller moved to this table; same additive-then-drop pattern as 0004/0005). `id, user_id, budget_id (fk cascade), budget_account_id (fk cascade), replenish_amount (bigint, not null, default 0), created_at`. Unique on (`budget_id`, `budget_account_id`).

`budget_entries` also gained its own nullable `budget_account_id` (migration 0044) so a single ledger row records which one account it actually touched, mirroring the entry's matching `settlements.budget_account_id` - previously implicit via the budget's one linked account, genuinely ambiguous once a budget can have several.

`replenish_amount` is this account's configured share of the budget's own automatic replenishment (income-linked or own-schedule) - meaningless/ignored for a budget with no such schedule. When a budget has 1+ connected accounts, `budgets.allocation` (the "primary amount") is *derived*: the sum of every connected account's `replenish_amount`, recomputed server-side on every create/update rather than taken from a submitted top-level field. A budget with zero connected accounts is unaffected - `allocation` stays a directly-entered number, exactly as before T218.

### `budget_entries`
The budget ledger. `id, user_id, budget_id (fk cascade), entry_date, amount (bigint > 0, always a positive magnitude), note, created_at`. Unchanged by 6B. Plus (Phase 10, migration 0009, added 2026-07-23): `direction (text, 'incoming' | 'outgoing', not null, no default — every insert must say which)`. Existing rows backfilled `'outgoing'` (they're all past logged spends, so their meaning is unchanged).

### `reminders`
`id, user_id, text, created_at`. Plus (T84, migration 0015): `completed (bool, default false), completed_at (timestamptz, nullable)`. Plus (T190, migration 0039): `due_date (date, nullable)` - when set, the reminder also plots on the Forecast page's calendar view as a green dot on that day; null behaves exactly as before (list-only).

### `preferences`
One row per user, created on signup. `user_id pk, currency (default '₱'), balance_ranges (bigint[] — 5 ascending centavo thresholds: [danger, low, medium, high, higher]; balance ≤ ranges[0] = danger, above ranges[4] = highest)`. Plus (T174, migration 0033): `active_scenario_id (uuid nullable, references scenarios, ON DELETE SET NULL)` - the scenario currently merged into the Forecast/Dashboard, if any. Plus (T239, migration 0052): `email_notifications_enabled (bool, default false)`, `notification_time (time, default '08:00:00')`, `notification_timezone (text nullable, an IANA zone name e.g. "Asia/Manila")`, `last_notified_date (date nullable)` - the daily due-today email's own settings, set from the Settings page. `last_notified_date` prevents a duplicate send within the same day (compared in the user's own timezone, not the server's) if the scheduled check runs more than once inside a matching window.

### `scenarios`, `scenario_recurring_items`, `scenario_one_off_items` (T174, migration 0033)
A "what-if" scenario is a named container (`scenarios`: `id, user_id, name, created_at`) for hypothetical bills/income/debt/savings (`scenario_recurring_items`) and one-offs (`scenario_one_off_items`) - genuinely separate tables mirroring `recurring_items`/`one_off_items`'s own columns and constraints exactly, not a shared `scenario_id` tag column on the real tables (see the Roadmap write-up for why). Deleting a scenario cascades to both child tables. Never touched by any existing query in the app - the only integration point is `loadForecast()`, which merges the active scenario's rows into its own arrays when `preferences.active_scenario_id` is set.

### `scenario_budgets`, `scenario_budget_entries` (T182, migration 0037; full parity added by T221, migration 0046)
A scenario's own budgets, mirroring `budgets`/`budget_entries`. Originally (T182) deliberately a plain named pot - no allocation, no replenish schedule, no linked income. T221 reversed that: `scenario_budgets` now carries `allocation`, `linked_scenario_income_id` (fk to `scenario_recurring_items`, never a real income - see T221's own write-up below for why), and the full recurrence-rule column set `budgets` has. One difference from a real budget stays deliberate: no budget-account link - a budget account is real storage for real money, and nothing about a scenario touches real money until "Activate permanently" copies it into real tables. `loadForecast()` pushes each active scenario's budgets into the exact same `Budget`-shaped array real budgets use (`fromScenario: true`), so the engine's existing income-linked/own-schedule replenish projection (Phase 11/T59, `forecast.ts`) already renders a scenario budget's projected deductions with zero new engine code - it was pre-anticipated for exactly this (see `fromScenario` threading in that loop, present since T182). A scenario budget's actual forecast effect still only ever comes from its own future-dated `scenario_budget_entries` rows, identical to how a real budget's income link/schedule alone never fabricates a settled amount either.

## The forecast engine (`src/lib/engine/`)

Pure functions. `generateForecast({ balances, recurringItems, overrides, oneOffs, budgets?, budgetEntries?, today, horizon }) → ForecastRow[]` where each row is `{ sourceType, sourceId, originalDate, name, amount, dueDate, type, runningBalance }`.

Pipeline: expand each recurring rule from `max(today, start_date)` to the rule's end (capped at horizon) → apply overrides (move/change/skip) → merge one-offs dated today or later → merge budget rows (below) → sort by due date, then incoming-before-outgoing within a date (stable beyond that) → running balance = sum of `balances.amount` plus cumulative signed amounts, always integer centavos. Occurrences strictly before `today` are excluded (they belong in settlements).

**Same-day ordering (T148):** rows sharing a `dueDate` put incoming amounts (`amount >= 0`) ahead of outgoing ones, so a salary landing on the 5th is credited before a bill due the same day is deducted - the running balance never shows a dip that wouldn't really happen. Rows with the same date *and* the same sign keep insertion order (`Array.prototype.sort` is stable from ES2019).

**Past-due occurrences (T150, replaces the old "strictly before today" exclusion):** an occurrence whose date has passed and which was never settled **stays in the forecast** and keeps counting against the running balance. It carries `pastDue: true` (omitted, not false, for ordinary rows). Recurring rules expand from their own `start_date` rather than from `today`, and past-dated one-offs are no longer skipped. The lookback is **unbounded** - the user chose this explicitly over a fixed window (2026-07-31) - and stays sane because settling writes a `skipped` override (recurring) or deletes the row outright (one-offs), so anything still showing is genuinely outstanding. Past-due is judged on a row's *effective* date, so moving an occurrence forward with an override clears it.

The backlog runs through the same cumulative running balance as everything else, before today's rows: the account balances the user maintains are what they hold *now*, and an unsettled obligation has not left the account yet. `splitPastDue(rows, startingBalance)` divides the two and reports `balanceAfterPastDue`; forward-looking stats (lowest balance ahead, first danger point, Peaks and Drops) take `upcoming` plus that balance, so a past-due dip is never mistaken for a future event while the numbers still start from a realistic position.

Two surfaces deliberately opt out. The Debt and Savings pages filter past-due out of their per-item "upcoming" lists, since a missed payment from months ago showing as the *next* one would read as a bug. The preview-mode sample fixture (`sampleFixture.ts`) drops past-due entirely: it backdates every rule 180 days so the recurrence summaries look established, nothing in preview can be settled, and the new-user tour would otherwise open on ~60 red rows.

**Known follow-up:** `seed_sample_data` (migration 0016) uses hardcoded January 2026 dates, so an account that restores sample data now shows about seven months of past-due. It is reached only through Settings > Restore sample data (typing "RESTORE"), never automatically at signup, so this is a wart rather than a blocker - and the hardcoded dates were already going stale on their own. Tracked as **T169**.

### Recurrence expansion (target semantics, T33–T34)
- **day**: start_date, then every `interval` days.
- **week**: weeks anchored to the week containing start_date; every `interval` weeks emit each selected weekday; skip dates before start_date.
- **month**: months anchored to start_date's month, stepping by `interval`; emit each clamped day in `days_of_month` (day > month length → last day of month), or the resolved nth-weekday date (`ordinal` 1–4 or −1=last + `ordinal_weekday`).
- **year**: start_date's month/day every `interval` years; Feb 29 → Feb 28 in non-leap years.
- **ends**: `never` → generate to horizon; `on_date` inclusive; `after_count` → stop after N emitted occurrences.
- `ends_type=never` items are excluded from finite "remaining total" stats (e.g. total remaining debt).

The old fixed 4-frequency expansion (monthly/weekly/biweekly/semi_monthly_15_30) is gone — removed along with its columns by migration 0005 once T35 shipped. Every `recurring_items` row now uses this rule shape exclusively; `RecurringItem` has no legacy fields.

### Monthly-equivalent summary stat
Summary cards ("Total Monthly Income/Bills") use integer multipliers, never fractional math (a displayed `86,666.7` means float leakage — bug). Generalized form: `occurrencesPerMonth = round(f)` where f = day: 30/interval · week: (4 × len(weekdays))/interval · month: (len(days_of_month) or 1)/interval · year: 1/(12 × interval); minimum 0. `monthlyEquivalent = amount × occurrencesPerMonth` (integer × integer). Old presets yield ×4 (weekly), ×2 (biweekly), ×2 (semi-monthly), ×1 (monthly). The *forecast* keeps real occurrence dates (a 5-Saturday month genuinely shows 5 incomes) — only summary stats use multipliers.

### Budgets v2 engine (T37, superseded by Budgets v3 below as of T57 — `budgetCycles.ts` and this whole model are deleted; kept here only as a historical record of how it worked)
A budget replenishes at each **cycle boundary**; everything derives from logged spends — no manual resets. `budgets.ts`'s T24/T25 monthly-only functions (`currentMonthBudgetStatus`, `expandBudgetOccurrences`) are kept exactly as they were rather than rewritten in place — they're still what the currently-deployed Budgets page calls, and per the T35→0005 incident this session isn't taking chances with a caller it can't fully verify mid-task. T38/T39 do the actual cutover once the UI can produce v2 data; `budgets.ts` gets deleted then, mirroring how `monthly.ts`/`interval.ts`/`semi-monthly.ts` were deleted only once nothing called them.

**Boundary source:** linked income set → that income's *effective* occurrence dates (after overrides — moved dates move the boundary; skipped occurrences produce no reset, the cycle extends). Else own recurrence set → its occurrences. Else (e.g. linked income deleted) → fallback monthly on the 1st + a "needs a schedule" badge in the UI. After the final boundary, the last cycle extends to the horizon.

**Cycle math:** cycle k spans [boundaryₖ, boundaryₖ₊₁). Entries belong to the cycle containing entry_date; boundary-date entries belong to the **new** cycle; entries before the first boundary count toward the first cycle. `available₀ = allocation`; `availableₖ = allocation + (carryover_enabled ? availableₖ₋₁ − spentₖ₋₁ : 0)` — carryover may be negative. `remaining = max(available_current − spent_current, 0)`; `over = max(spent_current − available_current, 0)`.

**Forecast integration:** current cycle → one row per budget, "{name} — remaining this cycle", amount −remaining, dated today, omitted when remaining = 0. Each future boundary within the horizon → "{name} — allocation", amount −allocation, dated the boundary. Budget rows are type `budget` (teal), never settleable (logging a spend covers that). The current-cycle row also stays uneditable — it's a live status snapshot always dated today, not a discrete future transaction. Future boundary rows *are* editable (T42): move date, change amount, or skip, via `budget_occurrence_overrides` (keyed `budget_id`+`original_date`, mirroring `occurrence_overrides`), applied in `forecast.ts`'s merge loop the same way recurring-item overrides are.

**Future-dated spends (T43):** an entry dated after today counts toward `spent`/`remaining`/`over` for whichever cycle it actually falls in — current or a later one — everywhere those appear (Budgets page, Dashboard card, current-cycle forecast row); the entries list and the totals now always agree (was Bug #3). `computeBudgetCycleStatus` gained `currentCycleEnd` (the next boundary, resolved via `horizon` — defaulted to a ~1yr lookahead for callers with no real forecast horizon, e.g. `BudgetCard.tsx`/`BudgetsPanel.tsx`/the Dashboard card; `expandBudgetCycleOccurrences` passes the real one) so a future-dated entry can be bounded to the correct cycle instead of leaking into whichever one is "current". Each future-dated entry also renders as its own Forecast row (sourceType `budget_entry`, sourceId = the entry's own id) on its `entry_date` — name `"{budget} - {note}"` matching the settlement-naming convention, amount −entry amount — so the balance dips on the day the money actually leaves. Whichever boundary row (current-cycle "remaining" or a future "allocation") the entry would otherwise have inflated is reduced by exactly its amount (floored at 0) so the two never double-count: `futureBudgetEntries(entries, budgetId, today)` in `budgetCycles.ts` is the shared helper both `expandBudgetCycleOccurrences` (for the subtraction) and `forecast.ts` (for the standalone rows) use. `budget_entry` rows are editable/deletable directly from the Forecast (user decision 2026-07-21, consistent with T42's boundary-row editability) — `EditSettleModal` gets an amount+date+note form reusing the *existing* `updateBudgetEntry`/`deleteBudgetEntry` actions from `budgets/actions.ts` rather than new ones. `BudgetCard.tsx`'s current-cycle entries list is now bounded above by `currentCycleEnd` too — previously unbounded, so an entry from a *further* future cycle used to leak into the wrong card even though the totals never counted it (part of the Bug #3 root cause).

**Logging a spend** writes a `budget_entries` row AND a settlement (source_type `budget`, type `budget`, name = budget name + note, actual_amount = −amount, actual_date = entry_date, forecasted fields mirror actuals, forecasted_balance = 0). History renders budget rows with a "budget" tag, no forecast-vs-actual comparison.

### Budgets v3 — running ledger (Phase 10, live as of T57 — this is the current, only budget model)
User feedback 2026-07-23: the cycle-boundary model above was confusing in the Forecast table (reservation rows that aren't real transactions) and didn't match how people actually think about a budget — a pool of money, not a recurring reset. Rebuilt as a simple ledger in `budgetLedger.ts` (T54), staged in alongside the untouched `budgetCycles.ts` at first (same "build alongside, cut over once the UI can produce/consume it" pattern T37 used for `budgets.ts`) and fully cut over by T57, which deleted `budgetCycles.ts` and everything that depended on it.

**The model:** no cycle boundaries, allocation targets, or carryover math. Every `budget_entries` row is `'incoming'` (a replenishment) or `'outgoing'` (a logged spend, or a manual take) against a budget's running total. `computeBudgetBalance(entries, budgetId, asOf) = Σ incoming − Σ outgoing` for every entry dated `asOf` or earlier — carryover is implicit (nothing ever resets) and overspending is allowed to go negative, no clamping, exactly like a real account. A budget optionally links to an income (`linked_income_id`) with an `allocation` amount (how much gets added when it replenishes); **replenishment fires when that linked income's occurrence is actually settled** (T56) — an `'incoming'` entry for `allocation` gets written then, dated at the actual settle date, not a projected one. A budget with no linked income instead gets manual "Add funds"/"Take funds" actions (T55), each just another `'incoming'`/`'outgoing'` entry. There's no "replenishes on its own schedule" option anymore — every non-income budget is manual. `carryover_enabled` and the own-schedule recurrence columns are gone from `budgets` entirely (migration 0010, T57).

**Forecast integration:** budget rows in the Forecast are *only* future-dated ledger entries (`futureBudgetLedgerEntries` — same shape T43 established for future spends, now covering incoming entries too, e.g. a future-dated manual add or a future replenishment), never a projected boundary/reservation row — there's nothing to project, since the running total simply *is* whatever's been entered so far. No `budget_occurrence_overrides`, no uneditable "remaining this cycle" row, no T45 reservation-badge/grouping logic (all gone with the boundary rows they existed to soften). The Forecast sidebar's `BudgetsPanel` is gone — budget status and quick actions live on the Budgets page (T55) instead.

**Logging a spend / adding / taking funds** all write a `budget_entries` row *and* a matching settlement (`actual_amount` sign follows direction — incoming positive, outgoing negative) so History keeps a full record regardless of which kind of entry it is.

**Multiple budget accounts per budget (T218):** a budget can now connect to more than one `budget_accounts` row via `budget_budget_accounts` (above), resolving the "budget linked to more than one budget account" discussion item. Two distinct places this matters, using two different resolution rules:

- **Manual actions** (Log spend, Add funds, Take funds) apply their full entered amount to exactly one account, chosen at the moment of entry. 0 connected accounts: unchanged, no picker, plain ledger-only entry. 1 connected: auto-used silently, no picker shown (identical UX to before T218). 2+: a required account picker appears in the form; the chosen account's id is stamped on both the `budget_entries` row's effect (`applyToBudgetAccount`) and the matching settlement's `budget_account_id`.
- **Automatic replenishment** (income-linked settle, or an own-schedule budget's `settleBudgetReplenish`) fires with no interactive moment, so it can't ask. 0 or 1 connected accounts: unchanged (one `budget_entries` row + one settlement for the full amount; the single account, if any, gets credited and stamped). 2+ connected: the settled total is split across accounts *proportional to each account's configured `replenish_amount` share* (so a per-occurrence override amount, T168/Bug#15, still splits by the same ratios rather than the plain default allocation) - integer centavos throughout, any leftover from proportional rounding goes to the last account in the split so the legs always sum exactly to the total. Each account's leg writes its own `budget_entries` row and its own settlement (note tags which account, e.g. "Replenished from {income} - {account name}"), mirroring the existing two-row-per-leg shape `moveBudgetFunds` already established for its own two legs.

A budget with zero connected accounts keeps replenishing exactly as before T218 (ledger-only, `allocation` entered directly) - connecting accounts, and configuring their replenish shares, can happen at any time after a budget (and its income link, if any) already exists. Budget accounts still never appear in the Forecast, unchanged from T204's original decision - only the budget's own ledger rows do.

The Budgets page shows a live per-account breakdown line (each connected account's current `amount`) whenever a budget has 2+ connected accounts; with exactly 1 it keeps the single "Budget account: {name}" line T211 added.

### Peaks and Drops
One row per year in the horizon, one column per calendar month (Jan–Dec); each cell shows that month's max (peak) and min (drop) running balance, color-coded by `balance_ranges`.

### Required test coverage (Vitest, all must stay green)
v1 core: day-31 monthly across Feb/Apr (clamping) · leap-year Feb 29 · semi-monthly across February · biweekly anchored mid-week across a month boundary · overrides move/change/skip · running balance with mixed signs · end_date cutoff and future start_date.
6A (T33–T34): every-2-weeks multi-weekday · days=[15,30] in February · interval-3 months · after_count · never→horizon · nth-weekday incl. last-X · generalized monthlyEquivalent.
6B (T37): linked-income boundaries incl. moved + skipped occurrence · own-schedule weekly cycles · fallback · income-ends extension · carryover on/off/negative · remaining/over · boundary-date entry · integer math.
6B (T43): future entry within the current cycle counts toward spent/remaining · a future entry in a LATER cycle doesn't leak into the current one · currentCycleEnd correctness (incl. null when the schedule ends) · a future boundary row reduced by known future spend in that specific cycle, floored at 0 · a future-dated entry renders as its own Forecast row without double-counting the running balance · a future-cycle entry reduces that boundary's row while still appearing as its own row.

## Pages & features

All pages require auth; unauthenticated users go to `/login`. Type colors: income green, debt orange, savings blue, extra purple, budget teal, bill default text.

1. **Auth**: sign up (email verification), log in, log out, password reset.
2. **Dashboard** (`/`): Total Balance, Total Monthly Bills, Total Monthly Income cards; per-balance breakdown; Remaining Debt + debt-free date + days until; a "Budgets" card (name + running balance, red when negative — T57); Peaks and Drops grid. Greeting shows profile name, else email local part.
3. **Forecast** (`/forecast`): total balance + editable balance chips, each also showing that account's own lowest projected balance ahead (T180); the full occurrence list color-coded by `balance_ranges` (danger = dark, low = red tint, medium = white, high→highest = deepening green); every row is clickable, opening Edit/Settle for recurring/one-off rows or a direct amount/date/note edit for a budget ledger entry (T57 — every budget row is now a real transaction, never a projection). A Table/Calendar toggle (T190, folding in the old standalone `/calendar` page from T164) switches the same content area between the occurrence list and a month grid - each day shows a count badge for forecast transactions and a green dot for any reminder due that day, click a day to list both, click a listed transaction to open the same Edit/Settle modal. Right sidebar: Reminders CRUD, each with an optional due date (T190) that plots it on the calendar view (the old Budgets panel moved to the Budgets page itself, T55).
4. **Balances / Bills / Income / Debt / Savings / Extras** (`/balances`, `/bills`, `/income`, `/debt`, `/savings`, `/extra`): CRUD pages; each shows its summary total; all four recurring forms use the shared recurrence picker with a human-readable rule summary per row ("Every 2 weeks on Sat · until Apr 2030"). Bills/Debt/Savings also carry an optional "Auto-debited" flag (T232) for items pinned to their connected account. The Balances page also shows each account's own monthly received/deducted/net breakdown (T236), expandable into a full per-frequency + one-time list.
5. **Budgets** (`/budgets`): CRUD + a running balance per budget (red when negative) + a total-monthly-estimate line (T234) + a month-filterable entries list + Log spend, plus Add funds/Take funds for budgets not linked to an income (T55, Phase 10).
6. **History** (`/history`): settlements table — forecasted vs actual amount/date, forecasted balance, type (budget rows shown as a pill tag).
7. **Scenarios** (`/scenarios`, `/scenarios/[id]`, T174, extended by T182/T183): create/rename/delete named "what-if" scenarios (bills/income/debt/savings/misc items, and budgets as a plain named pot - T182); turn any number of them on at once (T183) and their items merge into the Forecast and Dashboard together (an amber banner states the count everywhere it applies, click to see which; a "Scenarios" button beside Forecast's Filter button lists every scenario with its own independent toggle). Turn one off and only its items drop back out. "Activate permanently" copies a scenario's items into real data and deletes the scenario - irreversible, typed confirmation required. Scenario rows are never clickable in either Forecast view (edit them from the scenario's own page instead), and are tinted on the Forecast table to read as clearly hypothetical.
8. **Updates** (`/updates`, T163): a reverse-chronological, day-grouped feed over `activity_log` (T162) - every create/edit/delete/settle/toggle anywhere in the app, so a couple sharing one account can see what changed since they last looked. Entries newer than the viewer's own `activity_log_seen_at` watermark are marked new; opening the page updates that watermark for next time. The sidebar nav link carries an unseen-count badge.
9. **Settings** (`/settings`): profile, preferences (currency, balance ranges, T239's daily due-today email toggle/time/timezone), log out, delete account (removes all user data).

### Recurrence picker (shared component, T35)
Used by Bills, Income, Debt, Savings, Budgets. Select with contextual presets computed from the chosen start date — "Monthly on the 21st", "Weekly on Tuesday", "Every 2 weeks on Tuesday", "Every 15th and 30th", "Monthly on the third Tuesday", "Custom…". Custom panel: Repeat every [N] [unit]; weekday chips (week); day list or nth-weekday (month); Ends: Never / On [date] / After [N] occurrences.

### Phase 7 — Notion-style redesign (restyle only, no behavior changes)
Notion palette (`#37352F` text, `#E9E9E7` hairlines, `#2383E2` accent, soft pill backgrounds), Inter, 14px base, full-width shell with 240px sidebar, tables with hairline dividers and hover `#F1F1EF`. Budget teal `#0B6E99`. Peaks and Drops keeps the v1 year×month pill grid.

**Edited-occurrence indicator** (added 2026-07-21, user request after verifying T42): any Forecast row whose occurrence differs from its rule — i.e. it has an `occurrence_overrides` or `budget_occurrence_overrides` row with a moved date or changed amount/name (skips never render, so they need no indicator) — shows a small icon next to the row name (pencil glyph or similar, muted slate, with a `title` tooltip like "Edited from its usual schedule"). Requires the forecast pipeline to expose an `edited` flag on `ForecastRow` at the merge layer where overrides are applied. Implemented as part of T29's Forecast restyle.

**Date display format**: replace every raw `YYYY-MM-DD` shown to the user with a human-readable format — full dates as "Jun 1, 2025" (Month Day, Year), month-only dates (e.g. a recurrence rule's "until" suffix, a debt-free date rounded to the month) as "Jun 2025" (Month Year). Storage/computation stays `YYYY-MM-DD` everywhere per the core design rules — this is purely a UI formatting change, which is why it's scoped to Phase 7 rather than done ad hoc. `recurrenceSummary.ts`'s internal `formatMonthYear` already produces the "Jun 2025" style; export it (or a shared equivalent) for reuse instead of duplicating. Applies wherever T28–T31 touch a page showing dates: Bills/Income/Debt/Savings row summaries and start/end dates, Forecast due dates, History forecasted/actual dates, Dashboard's debt-free date.

## Operations

- **`npm run build` is always run by Claude, never the user** (2026-07-24 — mirrored in CLAUDE.md's session workflow). If a dev server is already active on :3000, Claude stops it, runs the build, then restarts the dev server afterward rather than asking the user to run it themselves.
- **Migrations are written as files** in `supabase/migrations/` and applied by Claude directly via the connected Supabase MCP integration (falls back to the user pasting into the SQL editor if that connection isn't available in a session — see CLAUDE.md "Hard rules"). Back up first via `pg_dump` only when real data is at stake.
- **The recurrence migration was split across two files**, each paste-and-run whole: `0004_recurrence_rules.sql` (add columns + backfill, non-destructive) and `0005_recurrence_drop_legacy_after_t35.sql` (enforce NOT NULL + drop legacy columns). Both are applied — 0004 on 2026-07-21, 0005 the same day once T35 was live in production.
- **Sample data**: `supabase/seed.sql` fills every feature with a realistic family dataset (run after 0004; re-runnable; all seed rows share the id prefix `00000000-0000-4000-a000-` for easy wiping).
- ~~**`/api/dev-login` is currently disabled**~~ (noted 2026-07-31, **no longer true as of 2026-08-02**): the route now works - hitting it returns a 307 redirect and lands on an authenticated `/`, so `DEV_LOGIN_EMAIL` has since been filled in. The note is kept rather than deleted because the failure mode is worth remembering: the route 404s with a bare "Not found" unless *both* `DEV_LOGIN_EMAIL` and `DEV_LOGIN_PASSWORD` have values in `.env.local`, giving no hint as to which is missing.
- **The Browser pane's "stuck on Loading..." problem - root cause found 2026-08-02.** Several tasks (T205, T206, T217, T218, T222) recorded that browser verification was impossible because the pane reported "not displayed, so the page is not compositing frames" and/or the app sat on "Loading..." forever with no console or server errors. **This is not an app bug and never was.** When the Browser pane is not on screen, the page's `document.visibilityState` is `"hidden"`, and a hidden document never fires `requestAnimationFrame` (measured: 0 rAF ticks in a full second). Next.js emits an inline `<script>requestAnimationFrame(function(){$RT=performance.now()})</script>` whose callback sets the reveal timestamp its Suspense boundaries gate on - so with rAF frozen, the streamed page content is parsed into the DOM but the fallback is never swapped out for it. The app is fully rendered and correct underneath; only the reveal is stuck.
  - **The real fix is to display the Browser pane** before verifying - then rAF resumes and everything works normally, screenshots included.
  - **The workaround when it can't be displayed**, which is how T231 was verified: the content is already in the DOM inside a `hidden` wrapper, so `javascript_tool` can walk up from any rendered element removing `hidden`, hide the `<main>` fallback, and then measure real geometry with `getBoundingClientRect()`/`getComputedStyle()` at any viewport `resize_window` sets. That gives genuine computed layout (flex direction, element widths, `document.documentElement.scrollWidth` for overflow) - weaker than a screenshot for spotting visual ugliness, but far stronger than "build and lint passed," which is all the earlier tasks could claim.
- **Dev auto-login for browser verification** (added 2026-07-21): `GET /api/dev-login` signs in the dedicated test account so automated Browser-pane sessions reach a logged-in state without a password ever being typed. Dev-only twice over: it 404s unless `NODE_ENV` is development **and** `DEV_LOGIN_EMAIL`/`DEV_LOGIN_PASSWORD` exist in `.env.local` (gitignored; never set on Vercel). The route is listed in the middleware's `PUBLIC_PATHS` so a logged-out hit isn't bounced to `/login` first.
- **Email notifications (T239, 2026-08-03) need two env vars this project didn't have before, in both `.env.local` and Vercel's project settings - not yet set on Vercel as of this write-up.** `RESEND_API_KEY`: sign up free at resend.com, create an API key, paste it in. `CRON_SECRET`: any random string (already generated and set locally for testing) - must be the *identical* value in both places, since Vercel sends it as a bearer token on every cron-triggered request and the route checks it matches. Without both set, `/api/cron/daily-notifications` runs (Vercel Cron will still hit it on schedule) but every user's send attempt fails gracefully with a clear "RESEND_API_KEY is not set" error, logged per-user rather than crashing the whole run.

### How to test the onboarding flow (recipe - hand this to the user verbatim whenever they ask)
Added 2026-07-26 at the user's request, so the steps live here instead of in their head. Both routes are dev-only and send no email, so this can be repeated as often as needed (see T120/T121, and the "email rate limit exceeded" problem that made this necessary).

1. **Become a brand-new user:** open `http://localhost:3000/api/dev-new-account`. This creates a real, already-confirmed account and signs into it. The **Welcome to Orium** box should appear, with a "0 of 6" Getting started checklist and zero balances behind it.
2. **Pick a path to test.** The modal offers exactly three. Re-open the same link before each one so every run starts clean:
   - *Take the short tour* - the 6-step walkthrough: "Welcome to Orium" (centred, no highlight), then Accounts, Bills, Forecast, Settings, and back to the Dashboard's Peaks and Drops. It simply ends on the last step. **There is no post-tour prompt** - T123 deleted it, along with the tour's "Prefer step-by-step setup?" link, because between them the app was asking the same question up to four times.
   - *Start guided setup* - goes to `/setup`, a normal page (not a gate), stepping through accounts, bills and income, then debt, savings, budgets and misc as optional. Every step has "Exit setup for now", and the nav stays usable throughout.
   - *Maybe later* - skips onboarding; check it stays skipped while navigating, and that the modal does not return.
3. **To test that progress survives a logout:** start the tour, click Next once or twice, log out, then log back in with the account's own address (shown in the URL bar after step 1, and in the sidebar) plus `DEV_LOGIN_PASSWORD`. The tour must resume on the same step, not restart (T119).
4. **Clean up afterwards:** open `http://localhost:3000/api/dev-new-account?cleanup=1`. It deletes only accounts this route created (matched on the `orium-dev+` prefix), so the real test account can never be caught by it.
5. **Return to the normal seeded account:** open `http://localhost:3000/api/dev-login`.

Quicker alternative when a full fresh account isn't needed: `http://localhost:3000/api/dev-reset-onboarding` resets the *current* account's onboarding columns only (add `?wipe=1` to also clear its financial rows). Not a faithful new user - currency, balance thresholds, tier labels, dismissed form tips and the sample-data stamp all survive it - so prefer step 1 when testing the real first-run experience.

## Roadmap

**Task order is non-negotiable: work strictly top to bottom through this section — always the topmost unchecked (`- [ ]`) item.** Never skip ahead or cherry-pick a later task without the user explicitly saying so. (Mirrored in CLAUDE.md's session workflow.)

### Before MVP launch (needs discussion)
- ~~Interactive onboarding experience for new users~~ — discussed and scoped 2026-07-24. Decisions locked with the user: seeded sample data + reset/restore actions rather than a separate sandbox for v1 (a switchable demo sandbox is a possible follow-up if real-user testing shows demand — deliberately not scoped now); spotlight tours for the Dashboard/Forecast intros plus a persistent "Getting started" checklist for the add-data steps; all hand-built, no tour library without explicit approval. Now **Phase 16 (T96–T100)**.
- ~~A fully rewritten, forced-sequence onboarding narrative.~~ Raised 2026-08-04 (REMINDER.md), verbatim: "Welcome to Orium! ... <We show them the quick tour>. <We guide them in the step by step adding of accounts, bills, income, debts, savings, budgets, and misc>. <We guide them on what this generated (Forecast page)>. <We guide them on the dashboard sections>. <We guide them on the History and Updates tour>. <We guide them with scenarios>. <We refresh them with how they can use the tool to keep track of all their personal finances>." Reads as a direction reversal of Phase 18's "nothing blocks, one question asked once" design - **resolved 2026-08-04 as T251** (Phase 47): kept the three welcome choices, but "Show me around" now flows into the full narrative described here rather than staying a short tour, so nothing is forced on someone who picks a different choice.
- **Post-build milestones (logged 2026-07-24 — milestones, not tasks):** after Phases 13–16 ship: (1) the user's wife uses the app for 1–2 weeks and feedback is collected; (2) iterate on that feedback; (3) then a launch discussion — landing page, first users, marketing, and whether a mobile app comes before or after launch.
- ~~Per-item progress bars for Debt/Savings goals~~ — scoped 2026-07-24 and moved to the roadmap as **T72** (Phase 12). The open "what does progress mean" question was resolved by the user: progress is occurrence-count-based (occurrences settled / total occurrences), made computable by requiring every debt/savings item to have an ending.
- ~~A more insightful History page.~~ Flagged by the user (2026-07-23): History should help users understand past spending patterns and cash flow, not just list settlements. Reaffirmed 2026-07-25 ("history - graphs insights"). Two more specific ideas folded in 2026-08-04 (REMINDER.md): a cash-flow pie chart, and a per-budget entry-log history. **Resolved 2026-08-04 as T252** (Phase 47): a pie chart by settlement type, a month-over-month spend trend, and a per-budget ledger view - all built on `settlements`/`budget_entries`, no new tables, no new chart-library dependency.
- ~~Investment tracking (stocks, bonds, mutual funds, retirement accounts).~~ Raised 2026-08-04 (REMINDER.md): "I want to see if we can implement a way to appeal to people with investments like stocks, bonds, mutual funds, retirement accounts." A new asset class Orium doesn't model at all today - `balances` assumes a spendable cash account, not a security with a fluctuating market value. Distinct from "Financial goals / milestones reminders (FIRE-style)" below (that's about flagging a milestone that doesn't exist yet; this is about tracking a real holding's value). **Resolved 2026-08-04 as T253** (Phase 47): user-entered running values, not live market data (avoids a new external-data dependency); shown as a separate "net worth outside cash flow" figure, not merged into Total Balance or the forecast itself.
- ~~Phone notifications.~~ Raised 2026-07-25, explicitly informational only at the time ("don't set them as T yet"), directly in tension with the "Out of scope" list below. **Resolved 2026-08-04 as T254** (Phase 47): lifts the exclusion for push specifically (same precedent T239 already set for email), via Web Push (VAPID + service worker) rather than a native app.
- **Beta launch strategy.** Raised 2026-07-27, moved here from REMINDER.md 2026-07-28: sizing how many free beta users the current Supabase free-tier project can actually support; how to vet sign-ups as legitimate (currently leaning toward posting to the user's own Facebook page for friends/family, wary of Reddit's strict/unfamiliar-with-Filipino-subreddits self-promotion norms); the overall sequencing (beta ~3 months → act on feedback and build paid features 1-2 months → public launch with paid features → mobile app timing, possibly after a browser-only launch). A landing page draft already exists (`landing-page.html`, repo root). **The signup-gating half is resolved 2026-08-04 as T255** (Phase 47, folding in REMINDER.md's "free until 50 users"): open signup, hard-capped at 50 accounts, a waitlist form beyond that. The rest of the sequencing below stays a business timeline, not something converted into a task.
  - **The user's own concrete sequencing, given 2026-08-02** (supersedes the rough "beta ~3 months → 1-2 months → launch" sketch above, which was written before this): finish every "before MVP" feature first, then
    1. **Closed beta, 3 months** - the user plus 5 others.
    2. **Gather feedback, then iterate on it - ~3 months.**
    3. **Open beta, 3 months** - up to ~50 users, the exact ceiling depending on how much the free infrastructure can actually take (the user's own caveat, and the sizing question already open in the bullet above - it stops being hypothetical here).
    4. **Collect and iterate again - ~3 months.**
    5. **Business setup - ~2 weeks or less**: registration, tax, landing page.
    6. **Profitability discussion, then build the subscription setup (plans and pricing) - ~3 weeks to 1 month.**
  - That is roughly **13-14 months from the end of "before MVP" work to a paid product**, with the two 3-month iteration blocks being the least predictable parts (they're sized by what the feedback actually turns out to be, not by scope known today). Three things worth settling when this is properly discussed rather than assumed: (a) 50 concurrent users on Supabase's free tier is the single riskiest number here and should be load-checked before step 3 rather than discovered during it; (b) steps 5 and 6 are estimated in weeks against multi-month steps elsewhere - registration and tax in particular are external-dependency work, not engineering work, so they may not compress the way the estimate assumes; (c) nothing in this sequence names *when* the mobile app and local-only versions below land relative to it, which matters because both change what "the product" being priced in step 6 even is.
- ~~Imports and exports.~~ Raised 2026-08-02. Load-bearing for the two items directly below - the user's own framing makes export the backup mechanism for a local/privacy-first version. **Resolved 2026-08-04 as T256** (Phase 47): JSON as the primary, faithful full-account export; CSV-per-table as a secondary, view-only convenience. Whole-account export only for v1; import is *restore* (wipe-and-replace) only for v1, *merge* deferred as its own harder follow-up. Every id regenerated on import.
- ~~A fully local version of the entire web app.~~ Raised 2026-08-02: "a fully local version of the entire web app, give me options." Four options were sketched (IndexedDB behind the existing data-access interface; a desktop app; self-hosted Supabase; a local-first-with-sync hybrid) - kept below for reference. **Resolved 2026-08-04 as T257** (Phase 47): option 1, IndexedDB behind the same data-access interface, the only one staying a single codebase - depends on T256 (export/import) shipping first as the safety net for data an uninstalled browser profile would otherwise lose.
  1. **Browser-local storage (IndexedDB) behind the same data-access interface.** No install, no server, runs from the same deployed site; data lives in one browser profile only and is lost if the user clears site data. Needs the export/import item above to be real before this is safe to offer. Cheapest by a wide margin, and it shares almost all of its work with the offline budget-logging item below.
  2. **A desktop app (Electron/Tauri) bundling a local database.** A real installable app with real file-backed storage the user can back up themselves; costs a new build/release pipeline per platform and a new dependency of exactly the kind CLAUDE.md says to ask about first.
  3. **Self-hosted: the user runs their own Supabase/Postgres.** Zero app changes, maximum privacy, and completely unrealistic for the non-technical audience this product is aimed at - worth naming only so it's explicitly ruled in or out rather than silently assumed.
  4. **A hybrid: local-first with optional sync.** What most people actually mean by this, and by far the most expensive - it means conflict resolution, which is a genuinely hard problem class and not something to drift into by accident.
- ~~A mobile app version, with a local-only option for privacy-sensitive users.~~ Raised 2026-08-02: "need a mobile app version of this where it will also have a local option for more privacy-sensitive users, which will have the imports and exports most beneficial for backing up their own data." Directly contested the "Out of scope: mobile app" line below. **Resolved 2026-08-04 as T258** (Phase 47): PWA first (manifest + service worker, home-screen install, offline support, zero new codebase), overlapping almost entirely with T267's offline budget-log entry - native/app-store stays explicitly deferred rather than ruled out, revisited only if PWA proves insufficient once real usage exists.
- **2024 pre-build survey (supplementary context only, not a mandate).** A Google Form the user sent to friends, family, and their extended network in March 2024, before Orium existed: 31 responses. Analysed 2026-08-01 at the user's request. The raw CSV lives only on the user's machine and **must never be committed** - it carries 24 real names and email addresses. **Weight this as one soft data point, not a spec:** warm-network sample of 31, the feature question asked "will this help?" rather than "would you pay?", no willingness-to-pay question at all, and it is ~2.5 years stale. Nothing here overrides a decision the user has already made.
  - *Feature demand, as ranked by respondents:* property/what-if visualisation 26/31, assign funds and see the future impact 25/31, total funds once all debts are paid 23/31, retirement age 13/31. The top two are already built (T174 Scenarios; Budgets + forecast) and the last-placed one was never built, so build order happened to match demand. The debt figure is the one real gap - 74% wanted it, the underlying data already exists, but there is no single "what you will have once every debt is cleared" number anywhere in the app.
  - *The finding that cuts against the product thesis:* asked what would improve their situation, 18 of 31 said more income, 11 said their own spending behaviour, and exactly 1 named a better tool. Most respondents do not believe an app solves their problem. That is a positioning input for the GTM/landing-page discussion, not an engineering one.
  - *Competitive set:* every app named was a wallet, bank, or backward-looking expense tracker (GCash, Maya, and the major PH banks; Spendee, Money Manager, Lista, Timely Bills, Excel, phone notes). None forecast. That is the gap Orium occupies, and it also means prospective users have no existing mental model to hang it on.
  - *Audience shape, supporting the family framing:* 26/31 financially support someone else (kids, parents, siblings); only 2 support just themselves. Most check finances weekly or twice a month, on the 15th/30th payday rhythm. 77% have savings, yet mean happiness with their finances is 3.2/5, with 17 of 31 sitting exactly at 3. The 4 who said they are not proactive are the same 4 who listed no tools at all - non-users use nothing, not a competitor. Two dislikes were device complaints ("not mobile accessible", "need WiFi or Data"), a small data point for the mobile-app timing question above.
  - *Three verbatims worth keeping:* one respondent disliked their app's "account balance being reset monthly" (Orium's continuous running balance is exactly that fix); another valued "yung feeling na hindi ako mabibitin hanggang susunod na payday" (Lowest Balance Ahead / Peaks and Drops, described by someone who had never seen them); a third complained their tool "doesn't notify me of the upcoming bills/payment due date" - a **fifth** independent request for notifications, on top of the four already logged against the "Out of scope" line below.
  - *One actionable asset:* 24 of the 31 left email addresses - a pre-warmed beta list of people who already said they want this. Feeds the Beta launch strategy bullet above and is gated by the same custom-SMTP blocker tracked in `STAGING.md`.
- **GTM playbook.** Raised 2026-07-27, moved here 2026-07-28: the user has a private GTM playbook they'll share for an ongoing planning discussion (explicitly **not** to be included in the public GitHub source — keep it out of any committed file). **Still genuinely blocked, not converted 2026-08-04 with the rest of this section** - there's nothing to plan without the document itself; ask the user to send it when starting this discussion.
- ~~Future pre-launch feature list.~~ Raised 2026-07-27, moved here 2026-07-28 — the user asked to spec four things before public launch: (1) notifications - resolved, see Phone notifications (T254) and Email notifications below, (2) import / templates / a guided first-time build so setup isn't solo - the ongoing-portability half is **T256**, the onboarding-template half folds into **T251**'s rebuilt narrative, (3) sharing the app with a partner - **resolved 2026-08-04 as T259** (Phase 47): an invite onto the same account, lifting "Out of scope: multi-user families" for this one case, (4) ~~"what-if" scenarios~~ - built 2026-07-31 as **T174**.
- **Production safety, staging, and inviting real users.** Raised 2026-07-30, triaged 2026-07-31. **Step 1 done 2026-08-01**: a second free Supabase project ("Orium Staging") now exists with all 33 migrations applied, and local `.env.local` points `npm run dev` at it instead of production - full details and the one remaining manual step (pasting the staging service-role key) in `STAGING.md`'s "Progress" section. **Signup gating resolved 2026-08-04 as T255** above. Custom SMTP stays deferred (2026-08-04 decision, see BUGS.md Bug #24/#25 and `STAGING.md`) - no verified domain yet. Building the one-way production-to-staging data refresh (decided to be on-request only, not automatic) is still not built - a `STAGING.md` task, not converted to a T-number here since that file already owns it.
- ~~Raise the tracking horizon from 5 years~~ - resolved as **T171** (below): the user gave a flat "final decision" of 50 years on 2026-07-31, superseding both this discussion and its earlier 25-year version, and superseding **T146**'s 2026-07-28 "keep 5" confirmation in turn. No split between a validation cap and a smaller rendered horizon - the single `MAX_TRACKING_YEARS` constant governs both, exactly as before, just larger.
- ~~Email notifications (five asks total).~~ Raised 2026-07-27 through 2026-08-03. Ask (1) resolved - see **T239** below (Phase 42). **The other four resolved 2026-08-04**: (2) a past-due alert → **T260** (Phase 47), folded into T239's existing daily email as a new section rather than a new toggle - which also resolves (3)'s "one email, multiple toggles" preference. (4) a periodic financial-stats digest → **T261** (Phase 47), its own opt-in toggle, weekly by default. (5) phone/push → **T254** above, a genuinely separate channel. The Operations note about custom SMTP for signup email volume is a related but distinct problem (Supabase Auth's own emails, not these) - still deferred, see Bug #24/#25.
- ~~User-defined categories.~~ Raised 2026-07-30: alongside the built-in types (bills, income, debt, savings) the user wants their own - birthdays, occasions, events - for money that is set aside and predictable but is not a bill. **Resolved 2026-08-04 as T262** (Phase 47): a label/tag on existing items, not a new `type` - dramatically cheaper, and `type`'s enum/colors/pages/goal-logic all stay untouched.
- ~~Deactivate a scenario that was already "Activated permanently."~~ Raised 2026-08-03 (REMINDER.md), **withdrawn by the user the same day** ("disregard undo scenario") before any design question was answered. Kept here only as a record that it was asked and explicitly dropped, not built - see git history for the original write-up (the code-level finding still stands if this ever comes back: `activateScenarioPermanently` copies scenario rows into the real tables with no link back to the source scenario, so "deactivate" would need new tracking to be buildable at all).
- ~~A "mock run" sandbox mode on the Forecast page itself~~, distinct from Scenarios. Raised 2026-08-04, first as "just thinking out loud," then the same session: "actually let's implement the mock run, for now" - scoped down to a v1 (Accounts' Add/Take/Move funds only, previewed on Forecast, "make this real" replaying the same actions for real) and built same day. **Reverted the same session**: the user reconsidered ("actually scratch the mock run. remove that") and asked for something simpler instead - real (not sandboxed) Add/Take/Move funds reachable from the Forecast page too, the same way they already work on Accounts. Built as **T249** (below, Phase 45). Kept here as a record that the sandbox idea was tried and explicitly walked back, not silently dropped - the code-level finding still stands if a preview-before-committing mode is wanted again later: the forecast engine already takes plain in-memory data as input, so a client-side clone-and-replay sandbox is the cheap path, and "save as a scenario" still has no clean home for edits to *existing* real data (Scenarios only ever adds new hypothetical items).
- ~~Fund-distribution planning: which account should pay which bill/debt/savings/misc item~~ - resolved as **T236** (below, Phase 39): the user waived the "needs a discussion" gate 2026-08-03 ("do the fund distribution without discussing... you can decide on the design and implement it"). First iteration was a standalone `/allocation` page (T235) with a per-account reassign control; the user rejected that shape same-day ("remove the fund distribution page completely") and specified a different one directly - no separate page, a monthly received/deducted/net breakdown built into the Accounts page instead, with an expandable full-frequency view. T236 is that second iteration.
  - *Original framing, kept for context:* "need a way to better visualize and plan for distributing which bills/debts/savings/misc should be for each accounts. I am having a hard time trying to make them all not have a negative projection in the future... Because I can see we're not negative in the entirety of the forecast but seeing negative lowest projected on some accounts despite that is uncomfortable. i need to be able to quickly know how to best distribute the accounts to the payables." The combined forecast (all accounts summed) can stay positive while an individual account's own projection (T180's per-account lowest-balance stat) still dips negative - the user wanted help deciding which connected account each payable should draw from so no single account goes negative, not just visibility into the fact that one does.
- ~~"Auto-debited" indicator for payables that can't be reassigned to a different account~~ - resolved as **T232** (below, Phase 39): scoped 2026-08-03, split out of the fund-distribution item above as its own concrete, buildable piece rather than waiting on that larger discussion.
- ~~Auto-move a portion of a settled income to another account~~ - resolved as **T212** (below, Phase 30): discussed 2026-08-01, the user chose the rule-on-income shape over rule-on-destination.
- ~~Financial goals / milestones reminders (FIRE-style).~~ Raised 2026-08-01. The user wants the app to proactively flag milestones that don't exist yet - starting with an Emergency Fund, HMO, Life Insurance, and Long Term Investing, parked as plain Reminders for now. **Resolved 2026-08-04 as T263** (Phase 47): reuses T262's new label - a milestone is a Savings item tagged with a recognized milestone label; the Forecast/Dashboard flags whichever recognized labels have no matching active item yet.
- ~~A budget linked to more than one budget account~~ - resolved as **T218** (below): discussed 2026-08-02, the user confirmed the dropdown-at-transaction-time shape for Log spend/Add funds/Take funds, and settled the harder "no interactive moment" case (automatic replenishment) as a per-account configured share (`replenish_amount`) that the settled total splits across proportionally - not a single "default account." Budget accounts still never appear in the Forecast, so the earlier reconciling concern doesn't apply; connecting accounts (and shares) can be done any time, including after a budget/income link already exists.
- ~~Undo toast on every action.~~ Raised 2026-08-02 (REMINDER.md): "I want an undo message on all actions... it can pile up if not closed... a quick message of what was done that will be undone like 'undo add funds to BDO Tatay'." **Resolved 2026-08-04 as T264** (Phase 47): real undo only for cheap-to-reverse actions (toggles, reminder deletes); money-moving actions get a display-only toast instead of a fake undo, since reversing them correctly needs real financial logic, not a UI feature.
- ~~A shareable "anyone with the link" view of Updates.~~ Raised 2026-08-01: the user wants to send their wife a link to the Updates feed (T163) that works like Google Drive's link-sharing - no login required. **Resolved 2026-08-04 as T265** (Phase 47): read-only, revocable via a regenerable token, shows the full feed (not a scoped subset), rate-limited per-IP since it bypasses normal auth.
- ~~A budget's start date when income-linked.~~ Raised 2026-08-02 (REMINDER.md): "need to add a start date in adding a budget, and it should be based on when an income is received, if its connected to an income." **Resolved 2026-08-04 as T266** (Phase 47) toward the cheaper option: display/default the linked income's own start date on the form, no change to how replenishment actually projects - the heavier floor-date behavior change stays deferred as its own follow-up if this proves insufficient.
- ~~Offline budget-log entry.~~ Raised 2026-08-02: "I will use it when I'm outside... it needs to inherit everything that's active in the budgets details... I want it to work smoothly even when I'm on data and offline." **Resolved 2026-08-04 as T267** (Phase 47, builds on T258's PWA): scoped to Log spend only for v1; cached budget/account list refreshed opportunistically online with a visible "as of" staleness note; offline entries queue in IndexedDB and auto-submit on reconnect with a "N waiting to sync" indicator; a queued entry whose budget/account was since changed surfaces as a failed sync rather than silently dropping.
  - **PWA basics, for reference:** a PWA is the existing Next.js app plus two small additions - a manifest file (name/icon, so the browser offers "Add to Home Screen" and it opens full-screen like a real app) and a service worker (a background script that can cache pages/data so they still load with no signal). No separate codebase, no app store, no new backend - it deploys with everything else already on Vercel.
  - **Mobile data vs. offline is not a special case to design for separately.** A service worker and its cache work identically regardless of *how* the connection exists (WiFi, mobile data, or none) - the browser only distinguishes "online" vs "offline" as a single signal. The normal path (data or WiFi present) hits Supabase directly exactly like the rest of the app already does; only the offline path needs the fallback described below. "Works smoothly on data too" is the default, not extra work.
  - **The "inherit everything active" requirement splits into two different problems, reads vs. writes:**
    - *Reads* (which budgets exist, their connected accounts (T218/T222), current balances) have to be cached ahead of time, refreshed opportunistically whenever the app is opened online - so the form can still render a real, recent picker even with zero bars. This is a snapshot, not live data; genuinely new/deleted budgets since the last sync won't appear until the next online refresh - worth being upfront about, not hidden.
    - *Writes* (the actual "log a spend" submission) go straight to Supabase when online; when offline, the entry queues in the browser's own local storage (IndexedDB) and is submitted automatically the next time the app detects it's back online. The user needs some visible "N entries waiting to sync" state so a queued entry doesn't feel like it vanished.
- ~~Account-wide deletion/reset completeness.~~ Raised 2026-08-02, noticed while fixing Bug #18/T226: `wipeFinancialData.ts`'s `FINANCIAL_DATA_TABLES` list - used by Settings' "Delete account," "Reset data," and "Restore sample data" - was last updated before T204/T209/T212/T218 added `budget_accounts`, `budget_budget_accounts`, `budget_account_transactions`, `balance_transactions`, and `income_auto_moves`. **Resolved 2026-08-04 as T268** (Phase 47): a concrete audit against every `user_id`-bearing table, closing whatever's missing and adding a check so a future new table can't silently repeat the gap.

### Carried over from Phase 19
Everything from Phases 6A-19 is done and archived.

- [x] **T138.** Fix: guided setup throws a network error after entering 10+ bills and advancing to the next step. *Never reproduced across four investigation passes (2026-07-28 through 2026-08-02) - see git history of this file for the full investigation record. Closed 2026-08-03 on the user's explicit instruction, without further reproduction attempts.*

### Phase 27 - triaged from REMINDER.md 2026-08-01, not yet built
Bug reports from the same batch went straight to BUGS.md (Bug #15) instead - this phase is feature requests only, in the order raised. "Prioritize budgets" (the user's own instruction, 2026-08-01) means T199/T200 (budgets-related) go first among these once work resumes here, ahead of top-to-bottom order.

- [x] **T193.** Edit a forecasted transaction's connected account directly from the Forecast/Calendar's own Edit tab, not only at settle time. *`EditSettleModal`'s Edit form gets the same account `<select>` the Settle form already had, for both recurring and one-off rows. For a recurring item this is a permanent change to `recurring_items.balance_id` (not a per-occurrence override - there's no such column, and "connected to the wrong account" is a lasting fix), applied alongside the existing occurrence_overrides write; for a one-off, `balance_id` just joins the existing name/amount/date update since a one-off item is itself the single occurrence. Both actions now also revalidate `/accounts` so the Balances page's connected-items list picks up the change. 222/222 tests (unchanged - server actions), tsc, build, eslint clean.*
- [x] **T194.** The Settle form's account transaction fee (T172) becomes a real optional field, auto-populated from the connected account's own fee setting rather than only shown as an informational "Plus a X fee" line. *`readSettleForm`/`applyToBalance` (forecast/actions.ts) gain an explicit fee parameter - blank keeps the original behavior (read whatever the account is currently set to), a typed value overrides it for this one settlement only (a waived fee, a higher-than-usual one). Scoped to the account this row was already connected to; doesn't live-update if a different account is chosen in the settle form's own picker (T193) - not asked for, and would need the `balances` prop widened with each account's fee plus a controlled select to do properly. 222/222 tests, tsc, build, eslint clean.*
- [x] **T195.** Budgets' Add funds / Take funds get an optional comment field, matching what Accounts' own Add/Take/Move funds (T186) already has. *`FundsModal.tsx` gets the same "Comment (optional)" text input `AccountFundsModal.tsx` already has - the server action (`writeLedgerEntry`, budgets/actions.ts) already read `note` from the form (it's what `LogSpendModal.tsx`'s own comment field already fed), only the Add/Take form itself was missing the field. 222/222 tests, tsc, build, eslint clean.*
- [x] **T196.** Dashboard stat cards restructure: Total Balance gets its own section breaking out each account's individual balance (not just the combined total); Total Monthly Bills/Income/Debt/Savings each become their own category rather than sharing one card layout. *The old shared 3-card "stats" widget (T117) split into two: "Total Balance" (its own card, each account's own balance listed underneath) and "Monthly totals" (four cards - Bills/Income/Debt/Savings, all via `monthlyEquivalent`; Debt/Savings never had a monthly figure before, only "Remaining" further down, a different question). `DashboardWidgetsPanel`'s reconcile logic (already built to handle added/removed widget keys) means existing users' saved order/hidden state degrades gracefully - the old "stats" key just drops out, the two new ones append visible by default.*
- [x] **T197.** New Misc stat cards on the Dashboard: total money in (incoming one-offs) and total money out (outgoing one-offs), as their own cards alongside T196's restructure. *Misc has no "monthly" concept (one-offs, not recurring), so these total every not-yet-settled Misc forecast row instead, split by sign - the full `forecast` array (past-due included, same "not actually gone yet" convention T150 established), not just `upcoming`. 222/222 tests (unchanged - presentational), tsc, build, eslint clean.*
- [x] **T198.** Show a linked income's recurrence frequency (e.g. "Every 2 weeks on Tue") when adding or editing a budget on the Budgets page. Same `summarizeRecurrence` helper (`src/lib/recurrenceSummary.ts`) T199 below would also use. *The income `<select>` is now controlled (was `defaultValue`-only) so picking a different income updates the frequency line live. `BudgetModal`'s `incomes` prop widened to an `IncomeOption` type with the rule fields optional rather than required - the onboarding wizard's own income list is a bare `{id, name}[]`, and a runtime `hasRecurrenceRule` guard means the frequency text simply doesn't render there instead of forcing that unrelated list to carry data it's never had. 222/222 tests, tsc, build (confirms the wizard's narrower list still satisfies the widened prop), eslint clean.*
- [x] **T199.** Two follow-ups to T191 raised the same day, before T191's own commit: (1) show a forecasted transaction's recurrence frequency when it's clicked, for any recurring-sourced row (bill/income/debt/savings, and a budget replenishment via its linked income's own rule). (2) an explicit "Goes to budget: X" label on a budget replenishment's modal, distinct from the account-balance line T191 already shows. *New `budgetReplenishRuleSummary` (`recurrenceSummary.ts`) resolves a budget's effective rule - its linked income's, or its own schedule - mirroring how `forecast.ts` itself already decides which occurrence dates a budget's replenishment follows. `forecast/page.tsx` now threads `recurringItems`/`budgets` (already returned by `loadForecast()`, just not passed down before) through `ForecastClient`/`CalendarGrid`, each computing the same small `frequencyForRow` lookup independently (matching how each already computes its own `accountBalanceAfterRow` map, T191). `EditSettleModal`'s up-front banner (T191) now shows up to three independent lines - account balance, "Goes to budget," frequency - each rendering only when it applies. 222/222 tests, tsc, build, eslint clean.*
- [x] **T200.** Fix: the sidebar's purple help/tip icons don't lay out correctly when the sidebar is collapsed - they should stack vertically like everything else in the collapsed rail, not keep their expanded-width arrangement. *`AppShell.tsx`'s dismissed-shortcuts row (two 32px squares, Quick tour/Guided setup) used a plain horizontal `flex gap-2` regardless of collapsed state - two squares plus the gap (72px) don't fit in the 64px collapsed rail. Now `flex-col` when collapsed, same as every other collapsed-state element already is.*
- [x] **T201.** Fix: the Updates nav item's unseen-count badge (T163) is oversized when the sidebar is collapsed - should shrink to fit the narrow rail like every other collapsed-state element. *The collapsed badge shared the expanded version's `px-1.5 py-0.5` pill sizing, which has no size cap - a 2-digit count or "99+" grew wider than the rail. Collapsed now uses a fixed `h-4 min-w-4` circle, the same shape RemindersPanel's own bell badge already established for the same reason.*
- [x] **T202.** Add a loading state for the Dashboard - clicking into it currently shows nothing while its data fetches. *New `src/app/(app)/loading.tsx` (the one route-group level missing one - every sub-page already had its own). Reuses the existing `LoadingScreen` component, same as every other page's loading state. 222/222 tests, tsc, build, eslint clean (same pre-existing 6-problem baseline, none in the touched files).*
- [x] **T203.** Budgets get a Move funds action too, matching Accounts' own Add/Take/Move funds (T186) - moving money from one budget to another, logged the same two-leg way T186 already does for accounts. *New `moveBudgetFunds` (budgets/actions.ts) writes two `budget_entries` rows (outgoing on the source, incoming on the destination) plus paired `settlements` rows for History, same two-leg shape T186/T209 already established. Distinct from T209's `moveBudgetAccountFunds` - this moves money between two *budgets*, that one between two *budget accounts* (T204's separate storage layer); if either budget involved is itself linked to a budget account, that account's balance moves too on the same leg via `applyToBudgetAccount`, so the two layers stay in sync. `FundsModal.tsx` widened from add/take-only to a third "move" mode mirroring `BudgetAccountFundsModal.tsx`'s own pattern (a `budgets` prop for the move-to picker); `BudgetCard.tsx` gets a "Move funds" button, shown only when there's more than one budget to move to. 222/222 tests, tsc, build, eslint clean.*
- [x] **T205.** Mobile Forecast table should occupy nearly the full viewport height when scrolled to it, with a small space above for Filter/Scenarios and room to scroll back up. *`ForecastClient.tsx`'s table container was capped at `max-h-[50vh]` below `md`, leaving a lot of empty page beneath the table before the user even started scrolling rows. Now `max-h-[80vh]` on mobile (desktop's `md:max-h-[70vh]` unchanged) - the Table/Calendar toggle and Filter/Scenarios buttons stay visible above it, and the table keeps scrolling within its own container rather than the page, so scrolling back to its top doesn't lose the user's place on the page. Single Tailwind class change; not verified in-browser this session - the Browser pane wasn't compositing frames (same environment issue noted against T138), so this is confirmed by build/test/lint only. 222/222 tests (unchanged - presentational), tsc, build, eslint clean.*
- [x] **T206.** The account filter already on Bills/Income/Debt/Savings/etc. becomes a dropdown (matching whatever pattern the other filters on those pages already use), rather than however it's currently presented. *New `AccountFilterDropdown.tsx` replaces the always-expanded `MultiSelectChips` row on Bills, Income, Debt/Savings (`MonthlyGoalsClient.tsx`), and Misc - a closed-by-default button styled like the filter bar's existing `<select>` dropdowns (AmountRangeFilter's operator, AmountSortControl), showing "All accounts" / the one selected account's name / "N accounts", opening a small checkbox panel on click (closes on an outside click). Multi-select is kept (a real thing to want - items spread across several accounts), just no longer always visible in the bar; `accountFilterOptions`/`matchesAccountFilter` (`src/lib/accountFilter.ts`) and every page's own `selectedAccounts`/`toggleAccount` state are unchanged, only the rendering swapped. Not verified in-browser this session - same Browser-pane rendering issue as T205 (stuck on "Loading..." with no console/server errors, chunks loading fine) - confirmed by build/test/lint only. 222/222 tests (unchanged - presentational), tsc, build, eslint clean.*
- [x] **T210.** A manually-replenished budget should say so in the compact "quick" views - the Budgets page's own list and the Dashboard's budgets section - the same way an income-linked or scheduled budget already states its source (`replenishLabel`, BudgetCard.tsx). Label text: "Replenished manually." *`BudgetCard.tsx`'s pill text changed from bare "Manual" to "Replenished manually" - same information, worded as an action. The Dashboard's own budgets widget (`page.tsx`) had no equivalent at all for a manual budget (no linked income or schedule also means no progress bar/countdown, so a manual budget's row previously showed nothing beyond name and balance) - now shows the same "Replenished manually" text under the name. 222/222 tests (unchanged - presentational), tsc, build, eslint clean.*

### Completed work (T1-T192, plus T204/T208/T209 - see Phase 28 below, completed out of order)
Full build write-ups live in **ARCHIVE.md**, in this same order. This index exists so a task number can be found quickly without opening it.

**Done**

- **Phases 0–4 (T1–T21)**: v1 built and deployed to Vercel — schema, pure engine + tests, all CRUD pages, Forecast/History/Dashboard, set...
- **Phase 5 (T22–T23)**: integer-multiplier monthly totals (Bug #1); greeting name fallback (Bug #2).
- **T24–T25**: budgets v1 tables + engine (monthly-only, no rollover) — the baseline Phase 6B converts.
- **T41**: sample data seed (`supabase/seed.sql`).
- ~~T26–T27~~ **cancelled** — superseded by Phase 6B; never build them.

**Phase 6A — Flexible recurrence (done)**

- T32 - Migration 0004: recurrence columns + enums + constraints + backfill; legacy-column drop split out...
- T33 - Engine: day/week/month(days)/year expansion + ends rules; port existing tests; add 6A cases (list...
- T34 - Engine: nth-weekday resolution (incl. last-X) + generalized `monthlyEquivalent`; tests.
- T35 - Recurrence picker wired into all four CRUD forms; human-readable rule summary per row.

**Phase 6B — Budgets v2 (after 6A)**

- T36 - ALTER migration on budgets (allocation, carryover, linked income, recurrence columns). Existing r...
- T37 - Engine rework: boundary/cycle/carryover model + tests (list above).
- T38 - Budgets page: CRUD + replenish-source UI + entries + log spend.
- T39 - Forecast: budgets panel + budget rows + quick log-spend.
- T40 - Dashboard card + History budget tags + empty states; verify summary stats ignore budgets except t...
- T42 - Editable transactions + stale-override cleanup, logged 2026-07-21 (user request, after T39), both...
- T43 - Future-dated spends as a first-class feature (user decision 2026-07-21, upgrading Bug #3 from dis...

**Phase 7 — Notion-style redesign (done)**

- T28 - Foundation: tokens, Inter, full-width shell, sidebar, base components — incl. recurrence picker...
- T29 - Restyle Dashboard + Forecast (list, chips, modals, reminders, budgets panel) — incl. the edited-o...
- T30 - Restyle all CRUD pages (Balances, Bills, Income, Debt, Savings, Extras, Budgets).
- T31 - Restyle Auth, Settings, History; consistency pass; screenshot review with the user before closing.
- T44 - Collapsible sidebar (added 2026-07-22, user request).

**Phase 8 — Forecast & Dashboard clarity (scoped 2026-07-22, not yet built)**

- T45 - Budget rows in the Forecast table read as confusing non-transactions (user's own question: "shoul...
- T46 - "Lowest balance ahead" indicator.
- T47 - Seed data negative/near-negative scenario.
- T48 - Peaks and Drops reformatted as a year × month grid (user request 2026-07-22) instead of the curre...
- T49 - Fixed-height, independently-scrollable containers instead of ever-growing page sections (user req...
- T50 - Forecast table filter bar (user request 2026-07-22): date (range), name (text match), type (multi...
- T61 - Forecast table density (user request 2026-07-23): shrink the row font and vertical padding as sma...
- T62 - Balance-range color highlight scoped to just the running-balance cell, not the whole row (user re...
- T63 - Peaks and Drops reflows instead of needing horizontal scroll (user request 2026-07-23, follow-up...
- T64 - Nav order: moved "Forecast" up to directly below "Dashboard" in the sidebar (user request 2026-07...
- T65 - Reminders panel becomes a full-height collapsible right sidebar on the Forecast page, mirroring t...
- T66 - Follow-up to T65 (user request 2026-07-23): move both sidebars' collapse/expand toggle onto the m...
- T67 - Peaks and Drops color-coded by `balance_ranges`, matching the Forecast table (user question 2026...
- T68 - Budget entries get their own scrollable, lazy-loading container on the Budgets page (user follow...
- T69 - Budget progress bar switched from time-based to money-based (user follow-up 2026-07-23/24) — the...

**Phase 9 — Finance category page enhancements (scoped 2026-07-22, not yet built)**

- T51 - Edited-occurrence indicator on finance category pages (user request 2026-07-22), extending T29's...
- T52 - Filters on each finance category page (user request 2026-07-22): Bills/Income/Debt/Savings/Extras...

**Phase 10 — Budgets rewritten as a running ledger (scoped 2026-07-23, in progress)**

- T53 - Migration 0009: `budget_entries.direction` (`'incoming'`/`'outgoing'`, not null, no default).
- T54 - New engine module `budgetLedger.ts`: `computeBudgetBalance` (Σ incoming − Σ outgoing up to a date...
- T55 - Budgets page rework: remove the carryover checkbox and "on a schedule" replenish option from `Bud...
- T56 - Wire replenishment: `settleOccurrence` (`forecast/actions.ts`) checks whether the settled item is...
- T57 - Forecast cleanup: delete `BudgetsPanel` and the boundary-row branch of `EditSettleModal`; simplif...

**Phase 11 — Budget replenish schedules + forecast deductions (scoped 2026-07-23, done)**

- T58 - Migration + engine only, nothing wired into the UI yet. Migration `0011_budget_replenish_schedule...
- T59 - Forecast + settle wiring. `forecast.ts` now projects a real deduction row (`sourceType: "budget_r...
- T60 - UI: `BudgetModal` gets "Replenish every" back as a third `SegmentedControl` option; Dashboard's B...

**Phase 12 — Filter modal, connected accounts, goal progress (scoped 2026-07-24, not yet built)**

- T70 - Forecast filter bar moves into a modal (user request 2026-07-24): a "Filter" button replaces the...
- T71 - Optional connected account per finance item (user request 2026-07-24): every settleable type — bi...
- T72 - Per-item progress bars for Debt/Savings (user request 2026-07-23, scoped 2026-07-24).
- T73 - Rename "Balance" → "Account" across the UI (user decision 2026-07-24 — they weighed that calling...
- T74 - Per-item transactions view on Debt/Savings (user request 2026-07-24): clicking an item (not its E...
- T75 - Budgets page card redesign (user request 2026-07-24).
- T76 - Lowest Balance Ahead card shows more than a negative/positive binary (user request 2026-07-24, mo...
- T77 - Direction-aware connected-account label (user request 2026-07-24, moved from REMINDER.md 2026-07...
- T78 - `RecurrencePicker`'s "Ends" control needs a better UX pass (user request 2026-07-24).
- T79 - Every date, including form date *inputs*, should follow the human-readable format Phase 7 already...
- T80 - Make the "lowest balance" tier labels (T76's "Comfortable low point", "Healthy low point", etc.)...
- T81 - Forecast Insights container (user request 2026-07-24). The "lowest balance ahead" line lived as p...
- T82 - Amount sign auto-inferred by finance category when editing/settling a forecasted transaction, so...
- T83 - Dashboard's Accounts/Remaining Debt/Savings/Budgets cards each get a "Manage" button linking to t...
- T84 - Reminders get a "completed" state, distinct from delete (user request 2026-07-25, moved from REMI...
- T85 - Free-tier 5-year tracking cap (user request 2026-07-25): "every user should have a max of only up...
- T86 - Dashboard's Accounts list gets divider lines between entries (user request 2026-07-25).
- T87 - "Highest" balance tier's green pill had low text contrast (user request 2026-07-25).

**Phase 13 — Sample data reset (scoped 2026-07-24, done)**

- T88 - Full wipe of the test account's data + a rewritten canonical `supabase/seed.sql`.

**Phase 14 — Full mobile compatibility (scoped 2026-07-24, not yet built)**

- T89 - Responsive navigation: the left sidebar becomes a hamburger-triggered drawer below `lg`; the Fore...
- T90 - Forecast page mobile: 3-column table (Name / Amount / Balance) at narrow widths — proposal to con...
- T91 - Dashboard mobile pass: card stacking/order, Peaks and Drops (already reflows since T63) verified...
- T92 - CRUD pages + shared components mobile pass: all six category pages, every modal, and `DatePicker`...
- T93 - Settings / History / Auth mobile pass + a full-app sweep at phone (375px) and tablet (768px) widt...

**Phase 15 — Branding (scoped 2026-07-24, not yet built)**

- T94 - Logo: 2–3 minimalist SVG directions mocked up for the user to choose from (pure SVG, no image ass...
- T95 - Branded login / signup: the four auth pages (+ `AuthCard`) get the logo and a light branded treat...

**Phase 16 — Onboarding (scoped 2026-07-24 — the user's main selling point for new users; the precision/UX bar is deliberately high)**

- T96 - Settings Preferences UI separation (user request 2026-07-24): currency, balance color thresholds...
- T97 - New-user sample data + reset: signup seeds the sample dataset into the new account; an always-fin...
- T98 - Spotlight tour component (hand-built overlay: dim + highlight + step text + next/skip) + the Dash...
- T99 - "Getting started" checklist: a persistent, dismissible panel tracking the add-data steps in order...
- T100 - Onboarding polish pass: exact copy, empty states, skip/replay ("Review the tour again"), and a fu...
- T101 - "First goes negative" as a second, distinct stat alongside Lowest Balance Ahead (user feedback 20...
- T102 - End-of-tour "keep or reset sample data?" prompt (user request 2026-07-25, part 1 of a larger onbo...
- T103 - Opt-in "preview with sample data" mode, available to any user regardless of whether they already...
- T104 - Dashboard card reorder (user request 2026-07-26): Lowest Balance Ahead and Peaks and Drops moved...
- T105 - Getting Started checklist (T99) tweaks (user request 2026-07-26): debt/savings marked optional al...
- T106 - "Extras" renamed to "Misc" everywhere in the UI (user request 2026-07-26).
- T108 - Forecast intro tour (T98) gets a line about settling transactions to keep the forecast accurate (...
- T107 - Date guardrails (user request 2026-07-26): a new bill/income/debt/savings/misc item can't start o...
- T109 - Sample dataset personalized to the user's actual family, and long em dashes removed from every se...
- T110 - "How to use" guide: walk the user through the whole app in one smooth flow, start to end. (User r...
- T114 - Lightbulb "How to use" icon in the main nav, directly below Settings - the part of T110's origina...
- T111 - Rename the route `/extra` to `/misc`, to match T106's user-facing "Misc" rename. (User request 20...
- T115 - Required onboarding wizard for completely new users, distinct from T110's skippable tour (user re...
- T116 - Guided tour rebuilt from scratch as a single short walkthrough, and guided setup (T115) changed f...
- T119 - First-login onboarding gets a "welcome" choice screen (tour vs. guided setup), and progress throu...
- T120 - Onboarding audit and rebuild of the short tour, plus explainer tips across every add/edit form, p...

**Phase 18 - Onboarding, rebuilt (scoped 2026-07-26; flow agreed 2026-07-26, T122)**

- *Tour feedback (user, 2026-07-26):* (1) the Next button should show a loading state before the step's card disappears, so a click doesn...
- *Structural fragility found during T114 (2026-07-26):* the `preferences` row is created by app code at exactly two call sites (`login()...
- *First-run finding from T121's own testing (2026-07-26):* a brand-new, empty account immediately shows a red "Goes negative by ₱0.00" w...
- *Surfaces to rationalize:* welcome modal, spotlight tour, guided-setup wizard, post-tour prompt, getting-started checklist, sample-data...
- **One question, asked once.** A new account sees a single welcome modal: show me around / help me set up / explore on my own. The answe...
- **Nothing blocks.** `(app)/layout.tsx`'s hard gate (T115) is removed outright. The wizard keeps a quiet "Exit setup for now" on *every*...
- **Show me around** runs the 6-step tour (T120). It no longer offers guided setup mid-tour and no longer ends in a prompt - it simply ends.
- **Help me set up** walks accounts, then income, then bills, offering debt/savings/budgets/misc afterwards as optional. Its starting scr...
- **Explore on my own** goes straight to the app.
- **One progress surface.** The Getting Started checklist (T99) on the Dashboard becomes the single place tracking what's left. It alread...
- **Deleted, because each re-asks a question already answered at the welcome modal:** the post-tour prompt and its three options (T120), ...
- **Empty is a designed state**, not an accident - starting with the Dashboard's Lowest Balance Ahead card, which currently greets a bran...
- T121 - Dev-only "fresh account" route, so onboarding can be tested end to end without hitting Supabase's...
- T122 - Onboarding design pass: agree the whole flow end to end before building any of it. What a brand-n...
- T123 - Stop the repeats and stop the blocking. Remove the post-tour prompt (`OnboardingNextStepPrompt`...
- T124 - Tour polish - the four issues the user reported 2026-07-26 while testing: (1) the Next button nee...
- T125 - Real empty states. The Dashboard's Lowest Balance Ahead card currently shows "Goes negative by ₱0...
- T126 - Prune the onboarding state left unused once T123-T125 land. Nine `preferences` columns currently...
- T127 - Four small follow-ups from live testing after T126 (user request 2026-07-26): (1) the short tour...
- T128 - Replace the "enter it as a positive number" style form tips with a keyboard-level guard, so a neg...
- T129 - Five items left in REMINDER.md from earlier tour/guided-setup testing (user request 2026-07-26)...

**Phase 17 - UI polish and Dashboard customization (reordered 2026-07-26)**

- T112 - All buttons everywhere get a hand-cursor (`cursor-pointer`) and a slight color change on hover. (...
- T113 - Style rule: never use long em dashes anywhere in the app - user-facing strings, seed/sample data...
- [x] **T117+T118.** Dashboard customization panel plus reordering, built together at the user's request (2026-07-27) rather than in the ...
- T130 - A dismissed "Quick tour" or "Guided setup" sidebar shortcut (T127) used to disappear outright, re...
- T131 - Tour bug from REMINDER.md (user report 2026-07-27): the highlighted Bills nav item during the sho...
- T132 - Signup confirmation by typed 6-digit code instead of an emailed link, ahead of giving the app to...
- T133 - Loading feedback on every button that needed it, ahead of giving the app to real users (user requ...

**Phase 19 - Pre-launch stress test, bug sweep & polish (scoped 2026-07-28, not yet built)**

- T134 - Full manual stress test pass across every page and flow, at both desktop (1280px) and mobile (375...
- T135 - Fix: recurring items collapsing to "today's day-of-month" regardless of the date or frequency act...
- [x] **T136+T137.** Fix: in guided setup, clicking "Add another X" then Cancel/Close left the add-item modal stuck open with no way out,...
- T139 - Fix: Add Debt's date picker gets clipped on its right edge inside the modal - the calendar is nev...
- T140 - Fix: Misc still shows at least one raw `YYYY-MM-DD` date, missing Phase 7's "Jun 1, 2025" human-r...
- T141 - Misc copy cleanup: remove the redundant "info" wording on the Money in / Money out fields (should...
- T142 - Verify: editing a Misc one-off transaction from the Forecast table should update what's shown on...
- T143 - Feature: ascending/descending sort by amount on every finance list (Bills, Income, Debt, Savings...
- T144 - Feature: a "varying twice-monthly" recurrence option (e.g. 16th & 31st, 5th & 25th) distinct from...
- T145 - Feature: user-orderable finance lists (Bills, Income, Debt, Savings, Budgets, Misc) - not the Das...
- T146 - Verify/clarify: REMINDER.md asked for "ends on date should limit to 5 years from the start date."...
- T147 - Fix: `EditSettleModal`'s "Delete this entry" (for a future-dated budget ledger entry) closed the...

**Phase 20 — Post-production backlog (done 2026-07-31)**

- **T148.** Forecast: same-day rows sort income before deductions.
- **T149.** Budgets: "Add funds"/"Take funds" show on every budget, not just income-linked ones.
- **T150.** Fix: Forecast silently dropped past-dated unsettled transactions (Bug #11).
- **T151.** Fix: settling an income linked to a budget double-counted the allocation (Bug #14).
- **T152.** Fix: Forecast's balance chips opened an account without its connected finances (Bug #12).
- **T153.** Fix: "First goes negative" was the wrong label above a nonzero danger threshold (Bug #13).
- **T154.** Engine test coverage for T148's same-day ordering rule.
- **T155.** Forecast row polish: bold dates, comment-bubble icon on hover.
- **T156.** Start date shown on Bills/Income/Debt/Savings list rows.
- **T157.** Recurrence picker: "Yearly" preset; "Ends: never" default for Bills/Income.
- **T158.** Forecast: expand a clicked transaction to show its full detail.
- **T159.** Comprehensive filters on finance records (account filter was the real gap).
- **T160.** Completed debts/savings goals get their own archived section.
- **T161.** Forecast desktop adopts the mobile date-grouped layout.
- **T162.** Activity log foundation (every create/edit/delete/settle).
- **T163.** "What's changed since you last logged in" Updates feed.
- **T164.** Family calendar view built on the forecast.
- **T169.** `seed_sample_data`'s dates made relative to the seeding date, not hardcoded 2026.
- **T170.** ~~Leftover sample data in production~~ - invalid finding (cross-account query bug), retracted.
- **T171.** Hard tracking limit raised from 5 to 50 years - the user's final decision.

**Phase 21 — Budgets v4: budget accounts (done 2026-07-31)**

- **T165.** Combined running total across all budgets.
- **T166.** Verified: auto-replenish on linked income settle already worked as designed.
- **T167.** Budget display: allocation shown, frequency shown, "replenishes today" no longer goes stale.
- **T168.** Budget replenishments editable per instance from the Forecast table.

**Phase 22 — Second reminder batch (done 2026-07-31)**

- **T172.** Per-account transaction fee, auto-deducted from every connected forecasted transaction.
- **T173.** "Due today" amber tag in the Forecast, alongside the past-due red one.
- **T174.** "Run possible scenario" - a toggleable what-if layer over the forecast.
- **T175.** Temporarily disable a bill/misc/income/debt/savings/budget to preview its forecast impact.
- **T176.** History page widened to match Forecast's table width.

**Phase 23 — Third reminder batch (done 2026-08-01)**

- **T177.** Reminders are draggable (pointer-based drag-and-drop reorder).
- **T178.** Date-range filter added to Bills/Income/Debt/Savings.
- **T179.** Connected-items list in `BalanceModal` grouped by type, with a collapse toggle.
- **T180.** Per-account forecasted balance (hover tooltip on each account chip).
- **T181.** History page gets its own filter bar.
- **T182.** Budgets added to Scenarios.
- **T183.** Unlimited simultaneously active scenarios, redesigned Forecast toggle UI.
- **T184.** Toggled-off finance items read more clearly disabled (grayscale).
- **T185.** Hide an Updates entry from view without touching the underlying log row.
- **T186.** Add/Take/Move funds for accounts, replacing blind balance edits.
- **T187.** Updates stays unread until explicit action; its own filters and lazy loading.
- **T188.** View and edit a finance item's modified future transactions from its own page.

**Phase 24 — Accounts page UX pass (done 2026-08-01)**

- **T189.** Connected-items redesign (icon-based disconnect + confirm step), decluttered account row (icon buttons for History/Edit/Delete), and the per-account forecast stat surfaced visibly here too, not just on Forecast's hover tooltip.

**Phase 25 — Calendar folded into Forecast, reminder due dates (done 2026-08-01)**

- **T190.** Calendar is now a Table/Calendar toggle inside the Forecast page (the old `/calendar` route and nav item are gone), and reminders gained an optional due date that plots as a green dot on that calendar view. Also fixes a reported T177 drag-reorder bug (dragging a reminder forward could land it one slot past the intended target).

**Phase 26 — Per-transaction account balance, budget replenishments attributed to an account (done 2026-08-01)**

- **T191.** Clicking a forecasted transaction connected to an account now shows what that account's own balance will be right after this transaction, in both the Forecast table and calendar views - resolves the "Forecasted balance per account" discussion item, scoped down to a single transaction rather than a general per-date lookup. An income-linked budget's replenishment is now attributed to that income's own connected account (money really does leave it, per T151/Bug #14's settle-time behavior) and shows up in the Forecast table's Account column accordingly; an own-schedule budget's replenishment stays unattributed, since it isn't funded from any particular account in this model.
- **T192.** A forecasted transaction's amount can now be exactly 0 wherever it's entered - creating/editing a Bill/Income/Debt/Savings/Misc item or a scenario version of one, settling or editing a single occurrence, and a budget replenishment/ledger entry (real or scenario) - previously rejected outright as "not a valid amount" in seven places, only some of which even had a stated reason (the two that did were guarding against a negative, not a zero).

**Phase 28 — Budget accounts (done 2026-08-01, completed ahead of Phase 27)**

- **T204.** "I want to create accounts specified for budgets as well. The main accounts used in the cash flow will be separate, and I need another set of accounts that will be used as storage for the budgets." New `budget_accounts` table (migration 0040), separate from `balances` and never counted toward Total Balance or the forecast. A budget optionally links to one (same "optional connection" shape bills/income already use for a main account); once linked, every ledger change on that budget - replenish (auto from a settled income, or its own schedule), spend, manual add/take - moves the linked account's balance too, the same way settling a bill moves a connected main account. Managed from a collapsible "Budget Accounts" sub-section on the Budgets page itself, per the user's own answer, not a new nav item.
- **T208.** Two small follow-ups raised right after T204/T198 shipped: (1) an income-linked budget's card now also shows the main account that income is connected to ("From account: X"), one hop further than the existing "Connected to {income}" pill - `IncomeItemRow` gained `balanceId`, and `budgets/page.tsx` now also fetches `balances` (id/name only) to resolve it. (2) the "Storage account" field on a budget's own create/edit form was renamed "Budget account" for consistency with the Budgets page's own "Budget Accounts" section header.
- **T209.** "Budget accounts should have almost identical functionality with main accounts, but they don't have to have projected total balance." New `budget_account_transactions` table (migration 0041), mirroring `balance_transactions` (T186) exactly - Add funds / Take funds / Move funds for a budget account, each logged with an optional comment, same three-mode `BudgetAccountFundsModal` shape `AccountFundsModal.tsx` already established. No fee concept (a budget account was never part of the forecast/T172 fee model) and no separate History view (not asked for this time).

**Phase 29 — Reminders UX (done 2026-08-01)**

- **T207.** "Adding a new reminder should have an add button instead that shows the form instead of showing it outright." The add-reminder form is now collapsed behind a dashed "+ Add reminder" button by default, opening (with autofocus) on click and collapsing again on a successful add or Cancel - same "click to reveal a form" shape `ReminderItem`'s own edit/delete modes already use. Bundled with **Bug #17** (BUGS.md) - a long reminder name's edit-mode text input lacked `min-w-0`, so it refused to shrink and pushed the Cancel/Save buttons out of view.

**Phase 30 — Budget account label, income auto-move (done 2026-08-01)**

- **T211.** A budget item now shows which budget account it's linked to (T204's `budget_account_id`) - a "Budget account: {name}" line on `BudgetCard.tsx`, matching the existing "From account:" line for a linked income's own main account (distinct things: one is the income's main account, the other is the budget's own separate storage account). Raised via a pasted cloud-sandbox session transcript (see below) rather than REMINDER.md. No budget in production/staging was actually linked yet when this was built - the "(Maya Nanay)" text visible in some budget names was a manual naming workaround, not a real link; the label will show once one is set from the Edit form.
- **T212.** "When an income arrives to a certain account connected to it, it needs to auto move a portion of the income amount received to another main account, and this auto move should also reflect in the projected total balance." Raised 2026-08-01 (see the now-resolved "Before MVP launch" discussion note above) and discussed the same day: the rule lives on the income, not the destination account, so one income can define several "on settle, move ₱X to account Y" rules. New `income_auto_moves` table (migration 0042). The forecast engine projects each rule as a paired debit/credit `income_auto_move` row on every one of the income's effective occurrences (mirroring `budget_replenish`'s own "borrow the income's occurrence dates" pattern) - the debit carries the source account's own T172 fee (a genuinely separate transaction, unlike a same-money budget carve-out), the credit does not, matching `moveAccountFunds` (T186). `settleOccurrence` nets the total auto-move amount into the same single write that already nets budget allocations out of the landing income (T151/Bug #14's precedent, to avoid a second read-write race on the same account), then credits each destination account and writes both legs to `balance_transactions` so both accounts' own History view shows the transfer. Original display shape (two visible rows per occurrence) revised the same day by T214 below - see that entry for the current shape.

**Phase 31 — Lazy loading, auto-move display revision (done 2026-08-01)**

- **T213.** "Implement lazy loading in Peaks and Drops, [and] opening a finance item (views upcoming and paid transactions)." Two independent performance fixes, both deferring/narrowing work that used to happen unconditionally on page load:
  - *Peaks and Drops* (Dashboard): with T171's 50-year tracking horizon, this card could render up to 50 year-groups (600 month-cards) into the DOM at once, for a card that only ever shows a ~260-420px scrollable window. Now reuses the Forecast table's own incremental-reveal shape (`INITIAL_VISIBLE_YEARS`/`YEARS_PER_BATCH`/`IntersectionObserver` + sentinel, `PeaksAndDropsCard.tsx`) - 3 years up front, 3 more as the user scrolls the card's own container, for both the Grid and Graph views.
  - *Item transaction modals* (Bills/Income/Debt/Savings): each page used to precompute every item's upcoming occurrences (a full `loadForecast()` run) and every item's full settlement history, for every item, on every page load - even though only one item's data is ever shown, only when its own "view transactions" modal (`ItemTransactionsModal.tsx`) actually opens. Replaced with a server action (`itemTransactions.ts`'s `getItemTransactions`) fired on that click instead, narrowed to one item. `loadForecast()` itself isn't cheaper to run (an item's occurrences aren't independently computable without the full engine - duplicating that logic was ruled out as a repeat of Bug #12's mistake), so this defers and narrows the payload rather than reducing server compute. Debt/Savings' `goalProgress` still needs a *count* of paid settlements eagerly (the active/completed split and progress bar aren't behind a click) - kept, but now a lightweight `source_id`-only query (`getSettlementCountsByItemId`) instead of the old full-row fetch. Currency, previously piggybacked off the same `loadForecast()` call on all four pages, now comes from a new dedicated `getCurrency()`.
- **T214.** Follow-up to T212, same day: "the auto move doesn't have to appear as a forecast transaction, it just needs to indicate that in the transaction of the income itself... maybe somewhere around the account column? a tag? that when hovered shows this? and when the income forecast transaction is clicked it should show it there as well." The two `income_auto_move` rows T212 added are now `hidden: true` on the engine side (`ForecastRow.hidden`) - they still count toward `runningBalance` and per-account attribution (`accountBalances.ts`), so Total Balance and the destination account's own projected balance stay correct, but `ForecastClient.tsx`/`CalendarGrid.tsx` filter them out of every visible list. In their place: the income's own row gets a small "auto-move" tag next to its Account-column entry, hover-titled with the destination(s) and amount(s) (resolved client-side from `GenerateForecastInput.incomeAutoMoves`, not carried on the row itself); `EditSettleModal.tsx` shows the same detail as an "Auto-moves:" line when that income row is actually clicked - reused by `ItemTransactionsModal.tsx` too (IncomeClient.tsx threads its own already-built map through), so clicking an income's upcoming occurrence from the Income page itself shows it as well. New engine test confirming both legs carry `hidden: true` while the income's own row doesn't.
- **T215.** "Add a '# days until # transaction(s) to settle' in the forecast insights." New `findNextTransactionBatch` (`lowestBalance.ts`) - the earliest upcoming date with at least one real transaction, and how many share it, so a payday and a bill due the same day count as 2. Excludes `hidden` rows (T214's auto-move legs - bookkeeping, not their own transaction) and `fromScenario` rows (hypothetical). Renders as a third line in the Forecast page's Insights card, right after "Lowest balance ahead" and the danger-point line, singular/plural handled for both the day count and the transaction count. New engine tests covering the date/count logic, order-independence, and both exclusions.
- **T216.** "The accounts section in the dashboard is now redundant, you can remove that" - raised via REMINDER.md. The Dashboard's separate "Accounts" widget (each account's name/balance plus a "Manage" link to `/accounts`) duplicated the per-account breakdown T196's "Total Balance" widget already shows above it; removed outright rather than merged, since the user asked for removal specifically. `DashboardWidgetsPanel`'s existing reconcile logic (the same mechanism T196's own comment describes) drops the now-invalid `"accounts"` key from any user's saved widget order/hidden state automatically - no migration needed. Nothing else referenced that widget's `data-tour="dashboard-accounts"` anchor.

**Phase 32 — Settlement account tracking, multiple budget accounts per budget (done 2026-08-02)**

- [x] **T217.** "Need to see accounts used (main and budgets if there are) in the history." History had no record of which account a settlement actually moved money in or out of, even though the underlying item/budget usually knew it (`balance_id` on recurring/one-off items, a budget's connected account(s)). `settlements` gained nullable `balance_id`/`budget_account_id` (migration 0043), stamped at every settle/spend/replenish/move call site across `forecast/actions.ts` and `budgets/actions.ts`; History's table gained an "Account" column resolving whichever one is set against `balances`/`budget_accounts`, "-" when neither is (including every pre-existing settlement row, which has no way to be attributed retroactively).
- [x] **T218.** "Budgets logging spend to have an option [for] what budget account to log that spend with, same with add funds and take funds, so the correct budget account auto calculates if there are multiple connected to it..., and a breakdown should show in the budget list per budget item if it has multiple budget accounts..., this should allow multiple budget accounts connected to a budget." Resolves the "budget linked to more than one budget account" discussion item above. New `budget_budget_accounts` join table (migration 0043, each row an account plus its own `replenish_amount` share) replaced `budgets.budget_account_id` entirely - backfilled 1:1 from every real link that existed (5 budgets on production shared one account, "Maya Nanay"; "Pocket Money" had its own, "GCash Tatay" - both preserved exactly, verified by an exact-match query with 0 mismatches immediately before dropping), then the now-dead column dropped by migration 0045 on both staging and production (the auto-mode classifier correctly blocked the unattended production drop first, applied only after the user's explicit go-ahead). `budget_entries` also gained its own `budget_account_id` (migration 0044, similarly backfilled) so a single ledger row states which one account it touched directly, rather than through a join that could no longer resolve to just one answer.
  - **Manual actions** (Log spend/Add funds/Take funds): 0 connected accounts unchanged; 1 auto-used silently; 2+ shows a required picker (`LogSpendModal.tsx`/`FundsModal.tsx`), and the chosen account is stamped on both the ledger entry and its settlement.
  - **Automatic replenishment** (income-linked settle, or an own-schedule budget's `settleBudgetReplenish`) has no interactive moment to ask at, so a per-occurrence total splits *proportional to each connected account's configured `replenish_amount` share* - new pure helper `splitAmountByShares` (`src/lib/budgetSplit.ts`, unit-tested), integer-safe with any rounding remainder going to the last account so legs always sum exactly. Discussed and confirmed with the user 2026-08-02 over the alternative of a single "default" account. Each account's leg writes its own `budget_entries` row and settlement (shared helper `writeBudgetReplenishLegs`, `forecast/actions.ts`), mirroring the two-row-per-leg shape `moveBudgetFunds` already used. A budget's `allocation` becomes *derived* (the sum of connected accounts' shares) once 1+ accounts are connected - the plain allocation field disappears from `BudgetModal.tsx` in that state, replaced by one amount input per connected account plus a live computed total.
  - `BudgetCard.tsx` shows a live per-account breakdown line once 2+ accounts are connected (e.g. "GCash Tatay ₱300 · Cash Tatay ₱1,000", matching the user's own example), the single-line "Budget account: X" (T211) kept for exactly 1.
  - `moveBudgetFunds` (moving between two *budgets*, distinct from moving between two budget accounts) originally left the 2+-account case unresolved on each side - closed the same day by **T220** below.
  - Verification this session: `npm run test` (240/240 passing, incl. new `budgetSplit.test.ts`), `npm run build` (clean TypeScript + production build), and a read-only structural check of the running dev server against staging (History's new "Account" column renders; Budgets page renders real allocations/schedules correctly; server logs show clean 200s, no console errors). **Full interactive browser verification (clicking through the new account pickers, watching a live multi-account breakdown) could not be completed this session** - the Browser pane reported "not displayed, so the page is not compositing frames" for every screenshot/click attempt, a tooling-side issue distinct from the app itself (same class of issue noted against T205/T206). Recommended manual check next session: on the Budgets page, create a second budget account, connect it alongside an existing one to a budget (e.g. "Pocket Money" already has "GCash Tatay" on production - add a second), confirm the picker appears on Log spend and the breakdown line shows both accounts.
  - **Production incident, same session:** migration 0045 (below) dropped `budgets.budget_account_id` on production before the matching code was pushed/deployed, so the live Vercel site briefly errored on the Budgets page ("column budgets.budget_account_id does not exist") until the commit was pushed. Caught immediately from a live screenshot, fixed by pushing the already-built/tested code (commit `18ce176`). Lesson applied going forward: when a migration and its code land in the same session, push before considering the migration "done," not after a separate later checkpoint.

**Phase 33 — Dashboard budgets total, Move funds account split, scenario budget parity (raised via REMINDER.md 2026-08-02, done same day - user granted standing executive authority to build and continue through the Roadmap without asking first)**

- [x] **T219.** "I want a monthly budgets total in the dashboard as well, you can decide on how to best display it that is consistent across the app." New "Total Monthly Budgets" card (`src/app/(app)/page.tsx`) alongside the existing Total Monthly Bills/Income/Debt/Savings cards - same `monthlyEquivalent` formula the other three already use, resolved against whichever schedule actually drives each budget's replenishment (`budgetReplenishRule`, the same resolver the Budgets widget's own progress bars already call). A manual budget contributes 0 - no recurring rhythm to average into a monthly figure. Grid widened `sm:grid-cols-3 lg:grid-cols-5` to fit the fifth card.
- [x] **T220.** "Moving funds in budgets should also state which accounts connected to the receiving budget will receive corresponding amounts." Closes the 2+-account gap T218 deliberately left open in `moveBudgetFunds`. New shared `writeBudgetMoveLeg` helper (`budgets/actions.ts`, direction-agnostic, used for both the "from" and "to" side) - 0/1 connected accounts unchanged, 2+ splits proportional to configured shares (`splitAmountByShares`, same helper T218's replenish path uses), one `budget_entries`/settlement leg per account. `FundsModal.tsx`'s move mode now states this live as the user types an amount and picks a destination budget - "Also moves ₱X into {account}" for one connected account, "Also splits into: {a} ₱x, {b} ₱y" for 2+ - the amount field became a controlled input to make the preview possible. Also hardened `writeBudgetReplenishLegs` (T218) with the same zero-leg guard this uncovered: `budget_entries.amount` is DB-constrained strictly positive, so a leg that rounds down to exactly 0 from a tiny split is now skipped entirely rather than attempted.
- [x] **T221.** "Adding a budget in a Scenario should allow me to get all functionalities of adding a new budget in the real thing, not just add a budget name." Reverses part of T182's deliberate scope cut. `scenario_budgets` gained `allocation`, `linked_scenario_income_id` (fk to `scenario_recurring_items` - never a real income, since a hypothetical budget replenishing from *real* settled income would blur the "leak into real data must be structurally impossible" line T182's own migration comment already drew) and the full recurrence-rule column set `budgets` has (migration 0046) - one deliberate exception from real parity: no budget-account link, since a budget account is real storage for real money and a scenario never touches real money until activated. New `ScenarioBudgetModal.tsx` mirrors `BudgetModal.tsx`'s own form (replenish-source `SegmentedControl`, `RecurrencePicker`, an income picker scoped to the scenario's own income items) in place of the old bare-name inline form. The genuinely interesting part needed no new engine code at all: `loadForecast()` already pushed a scenario budget into the exact same `Budget`-shaped array a real budget uses (`fromScenario: true`) - it was hardcoding `allocation: 0`/nulls everywhere, so the engine's existing income-linked/own-schedule replenish projection (Phase 11/T59) was silently disabled for every scenario budget the whole time, even though `forecast.ts`'s own loop already had a comment anticipating exactly this case (`fromScenario` pass-through, present since T182). Populating the real values was the entire fix - the projection, the scenario-tint, and the unclickable-in-Forecast behavior all just work now, unchanged code. `activateScenarioPermanently` also updated to carry the configuration into the new real budget it creates - recurring items are now inserted one-by-one (was a bulk insert) to capture each new real id, so a linked scenario income resolves to that income's own newly-real id rather than being silently dropped. Same-day follow-up ("this also should show in the Updates"): create/update/delete on a scenario budget now call `logActivity` - previously no scenario item type logged activity at all (a bare-name pot wasn't worth one); scenario bills/income/debt/savings/misc items still don't, out of scope for this specific ask.

**Phase 34 — Per-account budget attribution, redesigned Move funds, scenario real-data linking (raised live in chat 2026-08-02, done same day)**

- [x] **T222.** "if i move some of the pocket money funds from gcash tatay to cash tatay, how should i reflect that in the budgets? [...] i need to be able to easily identify how much of the GCash Tatay amount only belongs to Pocket Money [...] i want to move pocket money funds from gcash tatay to cash tatay, and i want that to reflect in the app easily." Closed a real gap T218 left open: a budget account can hold more than one budget's money at once (nothing stops two budgets sharing one), but nothing showed which budget's money was actually sitting where.
  - **Engine:** new `computeBudgetAccountBalance(entries, budgetId, budgetAccountId, asOf)` (`budgetLedger.ts`) - the same running-total math as `computeBudgetBalance`, filtered to one connected account too. Needed no new data: `budget_entries.budget_account_id` (T218/migration 0044) already recorded which account every entry touched, just unused for this until now - only had to thread `budgetAccountId` onto the engine's own `BudgetEntry` type and through `BudgetCard.tsx`'s/`page.tsx`'s row mappings.
  - **BudgetCard's own per-account breakdown (T218) was quietly wrong and is now fixed:** it showed each connected account's *raw total balance*, not this budget's own share of it - correct only by coincidence when a budget was the sole thing drawing from that account. Now shows the attributed amount via the function above.
  - **New, inverse breakdown on the Budget Accounts section:** each account now lists which budgets have money in it and how much (e.g. "GCash Tatay ₱20,000 - Pocket Money ₱5,000 · Orius Needs ₱6,000 · Unallocated ₱9,000"), computed in `BudgetsClient.tsx` by summing every connected budget's own attributed share per account; "Unallocated" is the account's raw total minus every budget's attributed share - money added directly to the account (T209's own Add/Take/Move funds) rather than through any one budget's ledger, not an accounting error.
  - **Move funds redesigned** into a new dedicated `MoveBudgetFundsModal.tsx` (pulled out of `FundsModal.tsx`, which is add/take-only again) with a single "Move to" picker grouped into two kinds of destination: this budget's *other* connected accounts (a same-budget relocation - new `moveBudgetOwnAccountFunds` action, net zero on the budget's own ledger total since it's still the same budget's money, two `budget_entries`/settlement legs with the same `budget_id` but different `budget_account_id`), or a *different budget* (the existing `moveBudgetFunds`, T203/T220). Picking a different budget that itself has 2+ connected accounts opens a second picker for which of *that* budget's accounts receives it.
  - **T220's proportional auto-split for `moveBudgetFunds` replaced with an explicit picker**, per this same request ("moving funds should explicitly ask me which budget to send it to and which of the connected account it should be sent to") - a manual, deliberate move is exactly the kind of interactive moment where the user should choose, unlike automatic replenishment (`writeBudgetReplenishLegs`, forecast/actions.ts), which still splits proportionally since it has no such moment. `writeBudgetMoveLeg` simplified back to always writing a single leg, now taking an explicit `chosenAccountId` instead of computing a split.
  - Verification: `npm run test` (240/240), `npm run build` (clean), `npx eslint` (clean - caught and fixed a real `setState`-in-`useEffect` issue in the new modal, replaced with an inline reset on the select's own `onChange`). No migration needed - reused the `budget_account_id` column T218 already added. Same Browser-pane "not displayed" limitation as T217/T218 blocked click-through verification; a `read_page` structural check confirmed the Budgets page renders correctly (real budget data, Move funds buttons present) with no compile errors in the current file state.
  - **Follow-up bug, raised via REMINDER.md 2026-08-02, fixed same day:** "taking funds directly from a budget account should simply just adjust that budget account, not create an 'Unallocated'." First pass (code-only, not interactive-tested) concluded the *math* was already right - `takeBudgetAccountFunds`/`addBudgetAccountFunds` (T209) only ever touch `budget_accounts.amount`, never `budget_entries`, so a direct Take/Add correctly leaves every budget's attributed share untouched and the derived Unallocated figure moves exactly as expected. The user confirmed this was still wrong in practice: the real problem was the *display*, not the math - `BudgetAccountsSection.tsx`'s breakdown line rendered any time `unallocated !== 0`, which for an account with **no budget attached at all** is unconditionally true (its entire balance is "unallocated" by definition) - so a plain, budget-less account showed "Unallocated ₱X" merely restating its own already-visible total, and a direct Take/Add on it looked like this line had "appeared" from nowhere. Fixed by only showing the breakdown at all when `attributed.length > 0` - a real mix of budget money worth explaining, not an account that's simply never been linked to a budget.
- [x] **T223.** "adding budgets in scenarios should inherit all actual data i.e. all active income should be available as options in the scenario budget. that goes with accounts as well. both main and budget accounts. [...] if a real data is removed from the actual financial data [...] it will be removed from there as well." Bills/income/debt/savings/misc scenario items already offered every real main account as a `balance_id` option with `on delete set null` (T174, migration 0033, confirmed unchanged against production) - this extends the same "real data as options, gracefully cleared if that real row is deleted" pattern to scenario budgets, which T221 had only linked to the scenario's own hypothetical incomes.
  - `scenario_budgets` gained `linked_income_id` (fk `recurring_items`, `on delete set null`) and `budget_account_id` (fk `budget_accounts`, `on delete set null`) (migration 0047), alongside the existing `linked_scenario_income_id` - a three-way mutual-exclusion constraint replaces T221's two-way one (at most one of real income / scenario income / own schedule).
  - `ScenarioBudgetModal.tsx`'s income picker now offers both in one select, grouped by `<optgroup>` ("Real income" / "This scenario's income"), values prefixed to tell them apart (`readScenarioBudgetForm`, actions.ts) - same encoding pattern `MoveBudgetFundsModal.tsx` already established for its own two-kind picker. A new single "Budget account" select offers every real budget account.
  - Linking to a real income needed no new engine code, same story as T221's scenario-income case: `forecast.ts`'s existing replenish projection only cares about `budget.linkedIncomeId` resolving in `incomeEffectiveOccurrences`, agnostic to whether that income is real or scenario-sourced - `forecastData.ts` just picks whichever of the two id columns is set. Deliberately does **not** make settling a real income write into `scenario_budget_entries` - a scenario stays inert with respect to real data in that direction, same as always; only the Forecast projection is shared, exactly like the real-vs-scenario income case.
  - `budget_account_id` is a pure "where would this live" reference - logging a scenario budget entry never calls `applyToBudgetAccount`, so a scenario still can't move real money through it. Only `activateScenarioPermanently` turns it real, inserting a `budget_budget_accounts` row (`replenish_amount` = the budget's allocation, matching migration 0043's own backfill convention) for the newly-real budget - real budgets don't have a direct `budget_account_id` column since T218, so this couldn't just carry through on the `budgets` insert itself the way `linked_income_id` does.
  - Verification: `npm run test` (240/240), `npm run build` (clean), `npx eslint` (clean). Same Browser-pane limitation as T217/T218/T222 - build/test/lint only this session.

**Phase 35 — per-occurrence income auto-move overrides (done 2026-08-02)**

- [x] **T224.** "need to be able to manually set an 'auto-move' individually. for example, I want to auto move a specific point in time with a specific amount just for that date, so it does go low, because moving half of an income to another account for the rest of time is not a good practice as it decreases flexibility." An `income_auto_moves` rule (T212) used to apply to every future occurrence of the linked income with no exceptions - this adds a per-occurrence override, mirroring `budget_replenish_overrides`'/T168's own pattern.
  - New `income_auto_move_overrides` table (migration 0049, applied to both staging and production): `income_auto_move_id`, `original_date`, `skipped`, `new_date`, `new_amount` - one difference from the budget table it mirrors: `skipped` defaults to `false` here, not `true`. A budget replenishment's `skipped` doubles as "already handled by settleOccurrence" (it's independently settleable); an auto-move has no settle path of its own, it only ever moves money alongside its income's own settle, so this table exists purely for a user's own edit and there's no second meaning to conflate.
  - Engine (`forecast.ts`): the auto-move loop now looks up an override per `(autoMoveId, originalDate)` - skips both legs entirely when `skipped`, otherwise substitutes `newAmount`/`newDate` for just that occurrence, marking `edited: true` on both hidden legs (mirroring the budget-replenish override's own `edited` convention).
  - Settle-time (`settleOccurrence`, forecast/actions.ts): reads the same override before touching cash, same Bug #15 lesson - the real transfer has to honor whatever the user edited this one date to, not blindly reapply the rule's flat amount. A skipped occurrence writes no `balance_transactions` legs and moves no money; the aggregate "Auto-moved ₱X" activity-log line is skipped entirely when every leg for that settle was skipped (no misleading "₱0.00" entry), matching the "a skipped thing leaves no trace" precedent the linked-budget loop already follows.
  - Two new actions, `editIncomeAutoMove`/`resetIncomeAutoMove`, mirroring `editBudgetReplenish`/`resetBudgetReplenish` exactly - one difference: a checked "skip this date" box is validated and upserted before amount/date, since skipping is a first-class outcome here rather than a separate settle-path marker.
  - UI: the income row's "Auto-moves:" line in `EditSettleModal` (shown in both the Forecast table and calendar views, and in Bills/Income/Debt/Savings' own "Upcoming" modal via `ItemTransactionsModal`) is no longer read-only - each auto-move is now its own `AutoMoveRow` component with an inline Edit form (amount, date, "Skip this date entirely" checkbox, Save/Reset), self-contained so each rule's `useActionState` hooks stay independent without violating the rules of hooks. New shared `resolveAutoMoves.ts` computes the per-occurrence effective amount/skip/edited state once, used identically by `ForecastClient.tsx`, `CalendarGrid.tsx`, and `ItemTransactionsModal.tsx` so the three surfaces can never disagree about what a given date's auto-move actually is (income/page.tsx now also fetches `income_auto_move_overrides` for the last of these).
  - Responsive: the edit form stacks its amount/date fields vertically below `sm` and side-by-side at `sm` and up (same pattern T231 established for row actions elsewhere), and both the read view and the button row wrap on narrow widths.
  - 244/244 tests (4 new: reduce, skip, move-date, and non-interference with the rule's other occurrence), tsc, build clean, eslint clean (same pre-existing 6-problem baseline in `SampleDataActions.tsx`/`AppShell.tsx`, none in the touched files). **Full interactive click-through could not be verified this session** - the Browser pane's known rendering issue (documented in Operations above) turned out to also block client-side hydration entirely for this run (DOM nodes had zero React-attached properties even after the documented `hidden`-attribute workaround and a forced `visibilitychange` retry), not just the visual reveal - a step further than the T217/T218/T222/T230/T231 sessions hit. Verified instead via the passing engine tests (which exercise the exact override logic end to end), a clean production build, and reading the real seeded Forecast data (`Nanay - TNIT` → `BDO Tatay`, ₱10,000) to confirm the existing auto-move tag still renders correctly against the new per-occurrence resolver. Recommended manual check next session: click that income's Aug 8/15 2026 occurrence on `/forecast`, edit its auto-move amount, and confirm it also updated the actual settle-time transfer when settled.

**Phase 36 — Peaks and Drops scenario toggle (raised live in chat 2026-08-02, done same day)**

- [x] **T225.** "I need you to allow me to select/deselect scenarios in the peaks and drops as well so i quickly see how everything will move from there." The Dashboard's Peaks and Drops card gets its own "Scenarios" button (badge shows the active count) opening the exact same list-with-independent-toggles panel `ForecastClient.tsx`'s own "Scenarios" button already uses - same `toggleScenarioActive` action, same `SubmitButton`-per-row shape. No new mechanism needed: that action's `AFFECTED_PATHS` already included `/`, so flipping a scenario from the Dashboard already revalidated the whole page (Peaks and Drops included) even before this - the only gap was that the Dashboard had nowhere to trigger the toggle from. `page.tsx` now also fetches the full `scenarios` list (not just active ones, which is all `loadForecast()` itself returns) to populate the panel.

**Phase 37 — Updates feed coverage audit (bug report 2026-08-02, fixed same day)**

- [x] **T226.** "the updates doesnt update when i update budget accounts in any way. every single action within the web app should have a corresponding update log." Root cause was a database bug, not a missing-code one: `activity_log`'s own `entity_type` CHECK constraint (migration 0029, predates T204) was never widened to allow `'budget_account'` when T204/T209 added it - every budget-account activity insert has been silently rejected by Postgres since 2026-08-01, invisible because `logActivity`'s error handling was *fully* silent (not even a server-side log line). Full write-up in BUGS.md, Bug #18. Fixed by migration 0048 (both projects, confirmed via `pg_get_constraintdef`), plus `logActivity` now `console.error`s a failed insert instead of swallowing it completely - still never surfaced to the user, but now diagnosable from server logs the same day instead of silently. Auditing every `"use server"` action file for the broader "every single action" ask turned up two more real gaps (never-written, not silently-failing): **Settings** (`updateProfile`/`updatePreferences`/`resetData`/`restoreSampleData` - the latter two logged *after* the wipe, since `wipeFinancialData` clears `activity_log` itself by design) and **every scenario bill/income/debt/savings/misc item's own create/update/delete** (scenario budgets already got this in T221; the rest hadn't - same `"(scenario)"` name-suffix convention). Deliberately left unlogged, as genuine exceptions rather than gaps: `deleteAccount` (activity_log cascades away with the account being deleted - nothing to show), Updates' own read-state actions (mark seen/dismiss/mark read - logging that you read the log is circular), auth (login/signup/logout - not financial data), and onboarding/form-tip UI state (dismiss a tip, skip a wizard step - not financial data either). 240/240 tests, build, eslint clean.
- [x] **T227.** Raised via REMINDER.md 2026-08-02: "move the running balance text in budget items above the bar, the allocated per replenishment should be below frequency." `BudgetCard.tsx` reordered - "{allocation} allocated per replenishment" now sits directly under the frequency line, ahead of the linked-account/budget-account-breakdown lines and the running balance figure, which stays above the progress bar as it already was.
- [x] **T228.** Raised via REMINDER.md 2026-08-02: "Allow to rearrange budget accounts." Built 2026-08-03 - `BudgetAccountsSection.tsx` now calls `useOrderedList("orium.budgetAccountsOrder", accounts, (a) => a.id)` and renders `ReorderButtons` next to each account's name, the exact same client-side/localStorage pattern T145 established and `BudgetsClient.tsx`'s own budget list (same page) already uses. No filter/sort exists on this particular list, so reorder is always available (no `canReorder` gate needed here).
- [x] **T229.** "when adding a new budget and it is manual replenishment, no need to set amounts for budget accounts. you can remove that. the selections can simply just be budget accounts." A manual budget never auto-replenishes, so `budget_budget_accounts.replenish_amount` (T218's proportional-split input) is genuinely unused for one - Log spend/Add/Take funds already ask which connected account a transaction affects at the moment it happens, regardless of any configured share. `BudgetModal.tsx`'s "Budget accounts" section now branches on replenish source: manual shows a plain checkbox list (connect/disconnect only, submitted with `replenish_amount: 0`); income/schedule keeps T218's existing per-account share inputs and derived-allocation total unchanged. The plain "Replenish amount (₱)" field also now always shows for a manual budget regardless of how many accounts are connected (previously hidden once 1+ were connected, matching the derived-allocation behavior that only actually applies to income/schedule).

**Phase 38 — Reminders click reorder, mobile clutter pass (raised live in chat 2026-08-02, done same day)**

- [x] **T230.** "Convert the drag reorder to a click reorder like how the bills/income etc. works." T177's pointer-based drag on the Reminders panel (a grip handle driving `useOrderedList.moveToIndex` from the pointer's Y position) is replaced by the same `ReorderButtons` up/down pair every other list already uses. One interaction to learn instead of two, and it sidesteps the `touch-action: none` / scroll-capture care a drag needs inside an already-scrollable panel. Deleted along with it, since Reminders was the only caller of either: `useDragReorder` (RemindersPanel.tsx), `useOrderedList`'s `moveToIndex` (`src/lib/useOrderedList.ts`, including its own T177 off-by-one bug fix - the bug and the code it lived in are both gone), and `GripIcon` (navIcons.tsx). The persisted order itself is untouched - same `orium.remindersOrder` localStorage key, so an order set by dragging before this change still applies after it.
- [x] **T231.** "Fix some UI issues of clutter in mobile, like the list of Income and others (thoroughly check each page)." Two structural problems, each repeated across every list page rather than being one page's mistake:
  - **The row.** Bills/Income/Debt/Savings/Misc all rendered `flex items-center justify-between` with the reorder buttons, the name/amount/pills/recurrence block, and Edit + Switch off + Delete all competing for one horizontal line. At 375px that leaves the content roughly 130px after the action buttons take their share. Now `flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between` - actions drop to their own line below the content on mobile, unchanged from `sm` up. This is the shape the Accounts page already adopted in T189; these five pages simply never got it. Edit/Delete also became icon buttons (new shared `RowIconActions.tsx`), again matching T189's own reasoning that several equally-loud text pills per row read as clutter. "Switch off"/"Switch on" deliberately stays a text button - it reports state as much as it offers an action, and no icon says "included in the forecast" unambiguously.
  - **The filter bar.** Seven pages (Bills, Income, Debt, Savings, Misc, Budgets, History, Updates) share the same `flex flex-wrap items-end gap-4` bar of 6-8 controls. On desktop that's one or two tidy lines; at 375px every control wraps onto its own line, so the bar grew to roughly half the screen and pushed the list itself - the thing the page exists for - below the fold on all of them. New shared `CollapsibleFilters.tsx` collapses it behind a "Filters" button below `md`, unchanged at `md` and up. The panel is always rendered (never conditionally mounted) so no filter loses its state when collapsed, and the button carries a count badge so a collapsed bar can't silently hide why a list looks short - each page's `filtersActive` boolean became an `activeFilterCount` number to feed it.
  - Also added `shrink-0` to the five list pages' header action buttons ("Add income", "Add bill", ...), which had no minimum width and would squash against a long "Total monthly (est.): ₱x" line beside them.
  - Verification: 240/240 tests, `npx tsc --noEmit` clean, `npm run build` clean, `npx eslint src` at the same pre-existing 6-problem baseline (SampleDataActions.tsx, AppShell.tsx - none in any touched file). **Measured in-browser this time, not just built:** at 375px an Income row computes `flex-direction: column` with the action group laid out below the content block (top 265px vs 168px) and `document.documentElement.scrollWidth === 375`, i.e. no horizontal overflow; at 750px it returns to `row`; at 1280px the mobile Filters button computes `display: none` while its panel computes `display: block`, confirming desktop is untouched. See the Operations note below for how this was measured despite the Browser pane's screenshot limitation.

**Phase 39 - raised via REMINDER.md 2026-08-03, built same day (user waived the usual "ask before building" step for this batch: "work through those and do the fund distribution without discussing... you can decide on the design and implement it")**

- [x] **T232.** "Auto-debited" indicator on bills/debt/savings items that are pinned to a specific account and can't be freely reassigned - e.g. subscriptions auto-debited from BDO Tatay, the Car Loan auto-debited monthly - so it's obvious at a glance during fund-distribution planning (T236) that these items aren't candidates for moving to a different account. New `auto_debited boolean not null default false` on `recurring_items` (migration 0050, applied to both projects) - scoped to bill/debt/savings (not income, not one-off Misc items, which are single dated transactions rather than a recurring auto-debit arrangement). A checkbox ("Auto-debited (paid automatically by the connected account)") added to `BillModal.tsx` and the shared `MonthlyGoalModal.tsx` (Debt/Savings), read via the same `formData.get(...) === "on"` convention `AutoMoveRow.tsx`'s own skip checkbox already established; `readBillForm`/`readMonthlyGoalForm` and their create/update inserts thread it through. Shown as a small slate "Auto-debited" pill next to the item's name on Bills/Debt/Savings' own list rows (`BillsClient.tsx`/`MonthlyGoalsClient.tsx`), same placement as the existing "Off"/✎ markers. Deliberately not threaded through the Forecast engine/`ForecastRow` - the category pages and the Accounts page breakdown (T236) already satisfy "at a glance" without touching the pure engine or its test fixtures.
- [x] **T233.** Filter the Forecast page's occurrence list by connected account. Raised via REMINDER.md 2026-08-03: "should be able to filter accounts in forecast page." T70's existing Forecast filter modal had date/name/type/amount/balance-range filters but no account filter, even though every settleable row has carried an optional `balance_id` since T71. `ForecastClient.tsx` gained a `selectedAccounts` multi-select (same `MultiSelectChips` component the Type filter already uses, options from `accountFilterOptions`) wired into `filteredForecast` via the existing `matchesAccountFilter` helper (`src/lib/accountFilter.ts`, built for T206) - reused rather than reimplemented, so this is the sixth call site of that same filter logic.
- [x] **T234.** Show the total monthly budgets figure on the Budgets page itself, not just the Dashboard. Raised via REMINDER.md 2026-08-03: "should be able to see total monthly budgets in budgets page." Not a duplicate of T219 (Phase 33), which only put the figure on the Dashboard. `BudgetsClient.tsx` now computes the same `budgetReplenishRule`/`monthlyEquivalent` reduction T219's Dashboard card already does (a manual budget with no resolvable schedule contributes 0, same rule), rendered as a "Total monthly (est.)" line right under the existing "Total across all budgets" running-ledger figure.
- ~~T235.~~ Fund-distribution planning page - built as a standalone `/allocation` page (see git history for the original write-up), then **removed completely the same day** on the user's explicit instruction ("remove the fund distribution page completely"), replaced by **T236** below with a different design: no separate page at all, the breakdown lives directly on the Accounts page instead. `src/app/(app)/allocation/` (page, client component, actions, loading) deleted outright, the sidebar nav entry and its icon reverted. The `auto_debited` column/migration (T232) and the Forecast account filter (T233) were unaffected - only the page built specifically to surface T232's flag was reverted, not the flag itself.
- [x] **T236.** Per-account monthly received/deducted/net breakdown, directly on the Accounts page - the user's redesign of what T235 was trying to do. Verbatim ask: "in the accounts page, i want to see... the amount each account received monthly, the amount each account is deducted monthly, the sum of those two per account... as accurate as possible... all [recurrence shapes] should be handled accordingly... you also need to list out the breakdown of other recurrence if they exist... I want to see the monthly estimate first and give me the option to see more."
  - **New pure module `src/lib/accountMonthlyBreakdown.ts`** (its own Vitest file, 8 tests): `computeAccountMonthlyBreakdown(recurringItems, oneOffItems, balanceId)` reduces every connected recurring item through the same generalized `monthlyEquivalent()` T219/T234 already use (income → `monthlyReceived`, bill/debt/savings → `monthlyDeducted`, `monthlyNet` = the sum) - accurate for any interval/unit combination since it's the same engine-tested function, not a re-derived approximation. A new `frequencyLabel(interval, unit)` generalizes the four legacy presets ("Daily"/"Weekly"/"Monthly"/"Yearly") to any combination ("Every 2 weeks", "Every 3 months", ...), used both as a group's display label and its grouping key. Connected items are grouped into `frequencyGroups` (sorted shortest-period-first via a `frequencyWeight` sort key, display/sort-only, never used for money math) so a yearly item - which rounds to ₱0 in the monthly estimate, same as `monthlyTotals.test.ts`'s own documented 1/12-rounds-to-0 case - still visibly appears in its own "Yearly" group rather than silently vanishing. One-off (Misc) items are never rolled into the monthly figures (they don't recur) but are listed in their own `oneTime` group, each with its raw amount and due date.
  - **Accounts page wiring:** `accounts/page.tsx` already calls `loadForecast()` (for T180's lowest-balance stat), which already returns `recurringItems` with every recurrence field and T232's `autoDebited` flag - no new query needed there. One new query added for one-off items with `due_date` (`loadConnectedItems()`, used elsewhere on this same page, deliberately omits it - not worth widening that shared shape for one caller). Both thread into `BalancesClient.tsx`, which computes each account's breakdown via `computeAccountMonthlyBreakdown` in the render loop (cheap - a handful of items per account, no memoization needed).
  - **UI, per account row:** a compact always-visible line - "+₱X/mo" (green, omitted if zero) · "-₱Y/mo" (red, omitted if zero) · "= ₱Z/mo net" - answers the headline ask at a glance, no click required. Clicking it (only rendered when the account has any connected item at all) expands the full breakdown: one block per frequency group (label + each item's name/type-colored label/amount, with T232's "Auto-debited" pill shown inline where set) followed by a "One-time" block for connected Misc items - "give me the option to see more," not shown by default. Reuses `TYPE_COLOR`/`TYPE_LABEL` (`forecastLabels.ts`) for consistent per-type coloring, same as every other list in the app.
  - 253/253 tests (8 new), tsc, build clean (had to clear a stale `.next` cache left over from the deleted `/allocation` route's dev-mode type validator, not a real code issue); eslint at the same pre-existing 6-problem baseline. Verified against real seeded data via the Browser pane's DOM-text-extraction workaround: UB Nanay correctly showed "+₱88,000.00/mo -₱54,669.61/mo = ₱33,330.39/mo net", Wise Nanay "+₱162,000.00/mo -₱117,908.04/mo = ₱44,091.96/mo net", BDO Tatay (no connected income) correctly omitted the "+" figure entirely rather than showing "+₱0.00/mo", and Cash (nothing connected) correctly showed no breakdown line at all. **Not verified this session:** clicking to expand a single account's grouped breakdown - the same pane limitation that blocked T235's modal click-through blocked this toggle too (`aria-expanded` stayed `"false"` after a dispatched click event). The toggle itself is the same `useState` + conditional-render pattern already live and working elsewhere in this exact codebase (`BudgetAccountsSection.tsx`'s collapsible section, `BalanceModal.tsx`'s own connected-items collapse), and the 8 new unit tests cover the underlying grouping/sum logic the expanded view renders - but the click interaction itself has not been driven end-to-end in a browser. Worth a real click-through next session before calling this fully done.
  - **Follow-up, raised live in chat 2026-08-03, fixed same day: T212 income auto-moves were completely invisible to this breakdown.** "Accounts should include the funds being auto moved to them in the monthly estimates... bdo tatay does not consider the received funds as income... when an income is being auto moved to an account, that should show transparently in the accounts page as well." The original version only ever looked at `balanceId` connections, so an auto-move's destination account showed nothing for money it genuinely receives every settle, and the source account's own income figure looked bigger than what actually stays there. `computeAccountMonthlyBreakdown` gained an `autoMoves` parameter (the same raw `income_auto_moves` rows the existing "Receives ₱X from {income} on settle" pill already used, reused rather than re-fetched) plus a `balanceNameById` lookup for display. Each move's monthly-equivalent is computed from its *linked income's own recurrence rule* (an auto-move has no schedule of its own - it only ever fires alongside that income's settle), added to `monthlyReceived` on the destination account and subtracted from `monthlyDeducted` on the income's own connected account - both real, both now counted in the headline totals, not just visible if you dig. A new `autoMoves: AutoMoveFlow[]` array on the returned breakdown (own section in the expanded view, between the frequency groups and "One-time") makes each flow individually visible too: "Auto-moved in from {income name}" / "Auto-moved out to {destination account name}" - satisfying "show transparently," not just "correct the total." An inactive linked income's auto-move is skipped, same `isActive` rule Bug #20 already established. 262/262 tests (6 new). Verified against real data: BDO Tatay's monthly received went from ₱0.00 to +₱40,000.00/mo (the "Nanay - TNIT" income's weekly ₱10,000 auto-move, ×4 via `monthlyEquivalent`); the income's own connected account - **Wise Nanay**, confirmed via direct SQL join, not UB Nanay as the user's own message named it - correspondingly dropped from -₱117,908.04/mo deducted to -₱157,908.04/mo, and its net from +₱44,091.96/mo to +₱4,091.96/mo. UB Nanay itself was untouched, correctly, since its own ₱88,000/mo income has no auto-move at all.

**Phase 40 - raised via REMINDER.md 2026-08-03, built same day**

- [x] **T237.** Show the resolved end date for a recurrence rule set to "Ends after N occurrences," not just the count - "there must be a quick way to find out the end date even if the ends selection is after n times in every finance item." Previously an `after_count` rule only ever displayed "· N times" (`endsSuffix`, `recurrenceSummary.ts`) - the actual calendar date it stops on required mentally counting forward yourself. `ruleEndDate()` (`engine/remaining.ts`) already computed exactly this - it's what drives the Dashboard's debt-free/savings-goal dates - just never surfaced through `summarizeRecurrence`, the single shared function every Bills/Income/Debt/Savings/Budgets row already calls to show its recurrence line. Now appends `· until {Month Year}` (same format/wording the `on_date` case already used) whenever the Nth occurrence resolves to a real date, so all nine of `summarizeRecurrence`'s existing call sites picked this up automatically with no per-page change needed. 258/258 tests (3 new: the existing after_count test extended with its real resolved dates rather than just the count, a weekly-rule case proving the date comes from actually expanding the rule rather than a naive interval×count estimate, and a no-`occurrenceCount` edge case), tsc, build clean, eslint at the same pre-existing 6-problem baseline. Not browser-verified interactively this session (a pure display-formatting change reused by every existing caller, already covered by real date-math tests) - confirmed the shared function itself is correct rather than re-verifying all nine screens it renders on.

**Phase 41 - raised via REMINDER.md 2026-08-03, not yet built**

- [ ] **T238.** "Filters across finance pages (bills debts etc) should be more visually appealing and smooth to the eyes. It should also contain more options for the filter, like the standard sorting by amount. Exhaust all possible filters for all possible lists." Two different kinds of work bundled in one ask - a visual/UX polish pass (subjective, "more appealing," needs an actual look at the current `CollapsibleFilters`/`MultiSelectChips`/`AmountRangeFilter`/`AmountSortControl` treatment across pages before deciding what changes) and a completeness audit (what filter/sort dimensions exist per list, what's missing). **Partially checked already, so a future session doesn't re-derive this:** amount sorting (`AmountSortControl`) is already live on Bills, Income, Debt/Savings, Misc, and Budgets - the "standard sorting by amount" example the user gave already exists everywhere it'd apply; the real gap is elsewhere (which fields *aren't* filterable/sortable per list - e.g. no date-added or name sort on any page, Debt/Savings has no "days until due"/progress filter, Budgets has no balance-range filter distinct from allocation). Needs a real per-page audit (Bills, Income, Debt, Savings, Misc, Budgets - History/Updates are read-only feeds, likely out of scope) before scoping into something buildable, plus the user's own read on what "more visually appealing" means concretely (spacing, color, layout, or the collapsed-vs-inline decision itself) - not assumed unilaterally given the subjective half of the ask.

**Phase 42 - raised live in chat 2026-08-03, built same day (email provider and scope questions confirmed with the user via AskUserQuestion before writing any code, per CLAUDE.md's "ask before new dependencies / product decisions" rule)**

- [x] **T239.** Daily "transactions due today" email. Verbatim ask: "implement an email notification system that notifies me when a forecasted transaction is detected for the day? it should give me a list of the transactions in question. I should be able to set the time of when in the day I will receive it and it should ask for a timezone." Resolves ask (1) of the "Email notifications" discussion item above; the other four asks there are still open.
  - **Two decisions confirmed with the user first, not assumed:** which email service (Resend, chosen over the alternative of the user already having one) - called directly via `fetch` against Resend's plain REST API, no `resend` npm package, so this adds zero new dependencies to `package.json`; and that this formally lifts "Out of scope: notifications" for *email* specifically (phone push notifications stays excluded, a separate decision).
  - **Migration 0052** (applied to both projects): `preferences` gains `email_notifications_enabled`, `notification_time`, `notification_timezone`, `last_notified_date` (see the data model section above for the exact shapes and why `last_notified_date` exists).
  - **Settings UI** (`PreferencesForm.tsx`, same shared `updatePreferences` action every other preference already saves through): a toggle, a `type="time"` picker, and a timezone `<select>` populated from `Intl.supportedValuesOf("timeZone")` - the browser's own authoritative IANA list, no new dependency and no hand-maintained list to go stale. Defaults to the browser's own current zone (`Intl.DateTimeFormat().resolvedOptions().timeZone`) the first time a user ever opens the section, not a guess or a hardcoded "Asia/Manila".
  - **Scheduling:** Vercel Cron (`vercel.json`, originally `*/15 * * * *`) hits a new route, `src/app/api/cron/daily-notifications/route.ts`. Authenticates via a bearer secret (`CRON_SECRET`, which Vercel sends automatically on every cron-triggered request once that env var exists) rather than a user session - middleware's `PUBLIC_PATHS` gained `/api/cron` so it isn't redirected to `/login` first, and the route re-checks the secret itself regardless. **Corrected 2026-08-04 (Bug #26, BUGS.md):** the `*/15 * * * *` schedule violates Vercel's free Hobby plan, which only allows a cron job to run once a day - every deployment carrying it had been silently rejected since this task shipped, so production ran a stale pre-T239 build for about a day undetected. Now `0 0 * * *` (once daily, 00:00 UTC = 08:00 Asia/Manila). The user explicitly accepted the resulting precision loss (a `notification_time` far from 08:00 Manila will never actually fire) rather than paying for Vercel Pro or standing up an external cron service - see Bug #26 for the full trade-off.
  - **Per-user timezone matching, without ever converting to UTC by hand:** new `src/lib/notifications/notificationTiming.ts` (own Vitest file, 8 tests covering both an ahead-of-UTC and a behind-UTC zone, and the window-matching boundary conditions) reads the current instant's local wall-clock time directly in each user's own zone via `Intl.DateTimeFormat`, and compares that against their stored `notification_time` within a 20-minute window (wide enough to absorb normal cron jitter against the 15-minute check interval). A known, documented, deliberately-unhandled edge case: a notification time within 20 minutes of local midnight can be missed, since the comparison is same-calendar-day minutes-since-midnight, not a continuous timeline - not worth the added complexity for a personal digest email.
  - **Per-user forecast, scoped by hand:** new `src/lib/notifications/dueTodayForecast.ts` (`loadDueTodayItems`) - a deliberately separate, smaller loader from `forecastData.ts`'s `loadForecast()`, not a shared refactor of it, for two reasons: (1) this runs from the cron route with a service-role client and no signed-in session, so every query is scoped by `.eq("user_id", userId)` by hand - a service-role client bypasses RLS entirely, so getting this wrong would leak one user's forecast into another's email; (2) scenarios are deliberately excluded - a "what-if" is hypothetical by definition, and an email listing a scenario's invented transactions as if real would be actively misleading, unlike the Forecast/Dashboard UI where they're always clearly tinted and bannered. Uses the same full horizon as the real Forecast page (not just "up to today") - a rule's *original* occurrence date is what bounds expansion, before any override is applied, so a future occurrence the user dragged to today via an edit override would otherwise never even be generated.
  - **Sending:** `src/lib/notifications/sendEmail.ts` composes a plain HTML list (name, type, amount) and posts to Resend. `NOTIFICATION_FROM_EMAIL` defaults to Resend's own shared test sender (`onboarding@resend.dev`, works with zero domain setup) - swappable for a verified address on a real domain later with no code change. A quiet day (nothing due) still marks `last_notified_date`, so the window doesn't stay open re-checking for nothing on every remaining tick; a send failure deliberately does *not* mark it, so the next tick inside the same window retries.
  - **New env vars needed** (`.env.local` for local testing, Vercel's project settings for production - never committed): `RESEND_API_KEY` (from a free Resend account the user creates), `CRON_SECRET` (any random string, generated once and used identically in both places to authenticate the cron trigger).
  - 274/274 tests (8 new), tsc, build clean, eslint at the same pre-existing 6-problem baseline. **Verified end-to-end against real staging data**, not just unit-tested: saved real values through the actual Settings form (confirmed via direct query: `email_notifications_enabled: true`, `notification_time`, `notification_timezone: "Asia/Manila"` all persisted correctly); hit the live cron route with a matching bearer secret and a notification_time placed inside the current matching window, confirmed via the route's own JSON response that it correctly found the enabled user, matched the timezone window, resolved their email, and ran the real per-user forecast query - failing only at the final send step with exactly the expected "RESEND_API_KEY is not set" error, since no real Resend account exists yet. All test data reset to a clean default afterward. **Not yet exercised**: an actual delivered email, blocked purely on the user creating a Resend account and providing the API key - the entire pipeline up to that point is confirmed working against real data.

### Phase 43 - raised via REMINDER.md 2026-08-04, not yet built
- [ ] **T240.** A helper text on the Budgets page explaining that budget amounts are excluded from the Forecast, since they're treated as already "paid" out of that forecast total. Raised: "add a helper text in budgets to indicate that all amounts in the budgets are excluded from the forecast as they are considered 'paid' from that forecast table."
- [ ] **T241.** Guided setup navigation redesign. Raised as two related complaints: (1) "clicking the continue does not show a loading indicator and just changes the view to displaying the back and forward button. it shouldn't be a button anymore, and there should just be a persistent back and forward"; (2) "guided setup after setting a misc and clicking on skip it goes back to step 5. The overall flow is clunky... implement a back and forward button. There should just be an add button on the form. the forward button should be greyed out until they add at least one if the step is one of the required ones. the forward button is clickable even without an item for the optional finance categories." Bundles a real navigation redesign (persistent Back/Forward instead of the current per-step Continue button, a loading state on Forward, Forward disabled until a required step has at least one item, always enabled for optional steps) with a specific glitch (skip on the Misc step lands back on step 5 instead of advancing/finishing) that the redesign likely subsumes rather than needing a separate fix.
- [ ] **T242.** Quick tour coverage expansion. Raised as five related asks, all about the existing 6-step tour (T120/T122) being too narrow: (1) "the quick tour should highlight the entire page of the relevant step... in step 2 (accounts), the user should be able to see all the account items as well aside from just the accounts total balance. this should apply for the rest of the steps" - each step's spotlight widens to the full relevant section, not just one stat; (2) "the bills, income, debt, savings, budgets, and misc should be introduced in the quick tour" (currently only Accounts, Bills, Forecast, Settings, and the Dashboard's Peaks and Drops are covered, per the onboarding-testing recipe in this file's Operations section); (3) "add the scenarios in the quick tour as well"; (4) "add the history in quick tour"; (5) "add the updates in quick tour." A larger tour needs the existing step-count/progress UI and the spotlight-overlay mechanism (`OnboardingTour` et al.) checked for how well they scale past 6 steps, but this is additive coverage of features the tour already exists to introduce - not a structural rework, unlike the item below.
- [ ] **T243.** A manual, one-off auto-move for a single income occurrence, to an account *not* already configured as one of that income's `income_auto_moves` rules. Raised: "Need ability to add auto move manually, so for example the auto move set up for Nanay HH is currently to BDO Tatay and Cash, but I want another option to add a manual auto move to a different account not connected to the original setup of the income, only for that certain instance in the forecasted transaction of the income." Distinct from T224 (which edits the amount/date/skip of an *existing* auto-move rule's occurrence) - this adds a rule-less, one-time transfer to a destination the income has no standing rule for at all, scoped to one occurrence only. Likely needs its own override-style table (or an `income_auto_move_id`-nullable variant of T224's `income_auto_move_overrides` shape) since there's no parent rule to attach to.
- [ ] **T244.** Follow-up to T236 (Accounts page monthly breakdown): a per-category (bills/income/debt/savings, not just per-frequency) subtotal when opening an account's connected-items breakdown. Raised: "need a total amount for connected items per category when opening accounts." T236 already groups an account's connected items by frequency ("Monthly," "Every 2 weeks," ...); this asks for an additional cut by type instead of (or alongside) that.
- [ ] **T245.** On the Income page, show how much of a given income's amount is committed to the budgets it funds, broken down by frequency. Raised: "from the income page, i need to be able to quickly see the total amount of the budget funds a certain income funds. for example in TNIT, it funds gas grocery laundry etc and I need to quickly see how much those are based on the frequency it's in (weekly)." The reverse direction of T245's sibling concept - not "which account does this pay," but "which budgets draw from this income, and how much of it they take."
- [ ] **T246.** Filter the Forecast's occurrence list to modified/edited transactions only. Raised: "need to filter modified forecast transactions." T29's existing edited-occurrence indicator (`ForecastRow.edited`) already flags exactly these rows - this adds it as a filter dimension in T70's filter modal, the seventh call site of that general shape.
- [ ] **T247.** When editing a bill/income/debt/savings/budget item that has existing per-occurrence overrides on its future transactions, ask whether to also reset those modified occurrences to the newly-edited values. Raised: "when editing a finance info in any category, if it has a correspoding modified entry in the forecast page, it needs to ask 'should we update all previously modified future transactions related to this as well?' which if yes will auto update all future modified transaction to what was set in the finance info, kind of like resetting it. if no, those modified future transactions are left alone." A real, but self-contained, UX decision the user has already specified the shape of (a yes/no prompt at edit time) - buildable without a further discussion.

### Phase 45 - Add/Take/Move funds from the Forecast page (raised live in chat 2026-08-04, built same day)

- [x] **T249.** "Can you quickly make it possible to do all account actions in the account page in the forecast page as well?" then, after the mock-run sandbox idea (above) was tried and walked back the same session: "just allow me to be able to move funds in accounts in the forecast page the same as how it works in the accounts page." Real actions, not a preview - identical to clicking Add/Take/Move funds on the Accounts page itself.
  - `BalanceModal.tsx` (shared by both pages since T152/Bug #12) gained "Add funds"/"Take funds"/"Move funds" buttons next to the Amount line, shown whenever it's opened in edit mode - reuses the existing `AccountFundsModal` component and its real `addAccountFunds`/`takeAccountFunds`/`moveAccountFunds` server actions unchanged, opened on top of `BalanceModal` the same way `BalancesClient.tsx`'s own row buttons already do. "Move funds" only appears when there's at least one other account to move to/from (same guard `AccountFundsModal` itself already had).
  - A new `balances` prop on `BalanceModal` (optional, defaults to `[]`) is only needed for Move funds' own "move to" picker - both `BalancesClient.tsx` and `ForecastClient.tsx` now pass their already-loaded `balances` array through; the onboarding wizard's own `BalanceModal` call is unaffected (Move funds simply doesn't show there, since a brand-new account usually has nothing else to move to/from yet).
  - Since this modal is already how a Forecast balance chip opens for editing (T152), no new UI surface was needed on the Forecast page itself - clicking a chip now offers the same three actions the Balances page's row buttons do, in the same modal.
  - 281/281 tests (unchanged - no engine/server-action logic changed, only which UI can reach the same existing actions), tsc, build, eslint clean. Not browser-verified interactively this session - the Browser pane's documented `requestAnimationFrame`-freeze issue (Operations section) blocked hydration entirely again (same as T248 above), confirmed via a fresh check before concluding this. Verified instead via a careful manual re-read of the JSX structure (the modal's return value became a fragment wrapping `Modal` plus a conditional `AccountFundsModal` sibling) and confirming both call sites (`BalancesClient.tsx`, `ForecastClient.tsx`) pass their real `balances` array through unchanged.

### Phase 46 - Move funds transaction fee, with a payer toggle (raised live in chat 2026-08-04, built same day)

- [x] **T250.** "Can you implement a transaction fee field in moving account funds to one another?" - refined twice in the same conversation: (1) "make sure the transaction fee gets auto deducted in the receiving account, so if i send 25000 and transaction fee is 50, the receiver should get 24950 and the sender should [be debited exactly] 25000" - the opposite of T186's original design, where the fee was extra cost on top of what the sender sent; (2) "actually there should be a toggle to whoever bears the fee cost" - both behaviors kept, selectable per move.
  - `AccountFundsModal.tsx`'s Move funds form gains a "Transaction fee (₱, optional)" field - blank means "use the source account's own configured fee" (T172's `transaction_fee_centavos`), same override convention T194 already established for the Settle form; pre-filled with that account's own fee when it has one, editable/clearable for a one-time exception. Alongside it, a "Who pays the fee?" radio (Sender/Receiver, defaulting to Sender to match the original T186 behavior for anyone not touching this new control) - live-labeled with each account's actual name.
  - `moveAccountFunds` (accounts/actions.ts): resolves the fee (override, else the source account's own setting) once, then applies it to whichever side the toggle names - **Sender**: the source account is debited `amount + fee`, the destination receives the full `amount` (T186's original math, unchanged). **Receiver**: the source account is debited exactly `amount` (unaffected by the fee), the destination receives `amount - fee`. `adjustBalanceWithOwnFee` (the old always-sender-pays helper) is gone, replaced by this explicit resolve-then-branch - simpler than threading a direction flag through a shared helper for what's now a genuine two-way choice. `balance_transactions` rows on both legs still record the plain typed `amount` regardless of which side the fee comes off of, matching the pre-existing convention that the ledger shows "the amount moved," not the fee's own accounting - only the real `balances.amount` update differs.
  - 281/281 tests (unchanged - server action, no engine change), tsc, build, eslint clean. Not browser-verified interactively this session - same Browser-pane `requestAnimationFrame`-freeze block noted against T248/T249 earlier this session. Verified via a careful manual re-check of the arithmetic against the user's own worked example (send 25000, fee 50, Receiver toggle -> sender delta -25000, receiver delta +24950) before considering it done.

### Phase 47 - Before MVP launch backlog, planned 2026-08-04 (not yet built)
Every standing "Before MVP launch (needs discussion)" item, plus REMINDER.md's own pending list, converted into a real, scoped task in one pass - user instruction, verbatim: "all standing before MVP discussions should now be within scope and moved to the spec. Plan them only, no executions." Each entry below states the actual decision made (not just the open question), so building it later doesn't need this same discussion repeated. Cross-referenced back from each original "Before MVP launch" bullet, which now points here instead of restating the question.

- [ ] **T251.** Onboarding rebuild: fold guided setup into the tour as one continuous, optional walkthrough. Resolves the "fully rewritten, forced-sequence onboarding narrative" ask without reversing Phase 18's "nothing blocks" decision: the three welcome choices stay (Show me around / Help me set up / Explore on my own), but "Show me around" becomes the comprehensive path - the existing 6-step tour flows directly into step-by-step data entry (accounts → bills → income → debt → savings → budgets → misc, T241's redesigned back/forward nav), then continues into explaining what that generated (Forecast, T242's full-page-highlight steps), the Dashboard, History/Updates, and Scenarios, ending on a recap. "Help me set up" (data entry only, no narration) and "Explore on my own" stay short escape hatches for anyone who doesn't want the full walk. Builds directly on T241/T242 (Phase 43) rather than duplicating their step definitions - build order should do T241/T242 first, T251 last, since it's the integration layer over both.
- [ ] **T252.** History insights: pie chart of settled spend by type, a month-over-month trend, and a per-budget entry-log view. Resolves the open "more insightful History page" discussion with three concrete pieces: (1) a pie chart of `settlements` grouped by type (bill/debt/savings/budget/misc) over a selectable window (this month/3 months/this year/all time); (2) a total-spent-per-month bar/line trend; (3) a per-budget history view (reachable from the budget's own detail) listing its full `budget_entries` ledger with running monthly totals. No new tables - all from existing `settlements`/`budget_entries`. No charting library added - hand-rolled inline SVG, matching the precedent Peaks and Drops already set, so this needs no new-dependency conversation at build time.
- [ ] **T253.** Investment tracking - manual value entry, not live market data. Resolves the "does this track live value or user-entered" question toward the cheaper option, consistent with "no new dependencies without asking": a new `investments` table (name, type - stock/bond/mutual_fund/retirement/other, current_value centavos, comments) with a plain CRUD page and a logged "update value" action (mirroring Add/Take funds' own audit trail, not a silent edit). Does not enter the forecast at all (no predictable schedule) - shown as a separate "Net worth outside cash flow" figure on the Dashboard, deliberately not merged into Total Balance, so the core forecast promise isn't diluted by an asset class that doesn't move on a schedule.
- ~~T254.~~ Phone (push) notifications. **Withdrawn by the user 2026-08-04**: "remove phone push notifs." "Out of scope: notifications" stays in force for push - only email is lifted (T239).
- [ ] **T255.** Beta signup cap: free until 50 users, enforced. Resolves REMINDER.md's "Free until 50 users" and the Beta launch strategy's open "how does signup get gated" question together: signup stays open (no invite code) but hard-caps at 50 total accounts - once `count(auth.users) >= 50`, the signup page shows a waitlist form (collects an email, creates no account) instead of the normal signup form. Answers Production safety's "how signup gets gated" question directly. The rest of the Beta launch sequencing (closed beta → iterate → open beta → business setup → pricing) stays a business timeline, not something to convert into a T-number - only the enforcement mechanism is planned here.
- [ ] **T256.** Imports and exports. Resolves the open format/scope questions: a single JSON file as the primary export (the only format that round-trips recurrence rules, overrides, and the budget-account graph faithfully), with CSV-per-table as a secondary, view-only "for a spreadsheet" export that isn't meant to be re-imported. Export is whole-account only for v1 (per-section deferred). Import is *restore* only for v1 (wipe-and-replace into an empty/new account, the backup-recovery case) - *merge* import into an already-populated account is a distinct, harder v2 with real conflict questions, deferred rather than guessed at. Every id is regenerated on import, never reused from the source file.
- ~~T257.~~ A fully local (IndexedDB) version of the web app. **Withdrawn by the user 2026-08-04**: "remove from the plan anything about working offline. I want this to stay as a web app now because we'll be allowing for signing up without an email for privacy" - the privacy use case this was solving is now handled by T269 (no-email signup) instead.
- ~~T258.~~ Mobile app (PWA). **Withdrawn by the user 2026-08-04** alongside T257/T267, same reasoning - "stay as a web app."
- [ ] **T259.** Partner sharing - an invite flow onto the same account, not a new multi-tenancy model. Lifts "Out of scope: multi-user families" for this one case, the same way T239 lifted "notifications" for email specifically. Shape: the account owner generates an invite from Settings (Supabase Auth's own invite mechanism, or a signed token link); the invited person's login gets full read/write access to the *same* `user_id`'s data - no per-row "who did this" attribution beyond what Updates already shows. The real risk, stated plainly rather than glossed over: two Supabase Auth identities writing through one `user_id`-scoped RLS policy needs either a shared service identity or a `household_id` layered under the existing policies - the second is the correct long-term shape but a real migration, not a small change.
- ~~T260.~~ Email notifications, ask (2): a past-due alert. **Withdrawn by the user 2026-08-04**: "Keep only one email notif feature for now" - T239 (already built and live) is that one.
- ~~T261.~~ Email notifications, ask (4): a financial-stats digest. **Withdrawn 2026-08-04**, same instruction as T260.
- [ ] **T262.** User-defined categories - a label, not a new type. Resolves the discussion toward the cheaper option: a nullable `custom_label` text column on `recurring_items`/`one_off_items`, freely typed by the user, rendered as an extra pill alongside the item's existing type pill everywhere that already shows one. `type`'s enum, colors, per-type pages, and the debt/savings goal logic are all untouched - a birthday fund stays a Savings item, now optionally tagged "Birthday - Mom."
- [ ] **T263.** Financial goals / milestones reminders (FIRE-style). Resolves "doesn't exist yet" computationally by reusing T262's new label: a milestone (Emergency Fund, HMO, Life Insurance, Long-Term Investing) is a Savings item tagged with one of a small fixed set of recognized milestone labels. The Forecast/Dashboard gains a "Milestones" section listing which of those labels have no matching active Savings item yet, prompting the user to add one - once added, it's an ordinary Savings item and already gets T72's existing goal-progress bar for free.
- [ ] **T264.** Undo toast on every action - real undo where it's safe, display-only elsewhere. Resolves which actions get which: toggling an item on/off or deleting a reminder get genuine reversal (a straightforward inverse action); Add/Take/Move funds and settling get a display-only toast ("Added ₱X to {account}," no undo button) - reversing a money-moving action correctly would mean re-checking whether that balance has changed from something else since, which is real financial logic, not a UI feature, and a fake undo that gets it wrong is worse than none. Toasts stack, close only via "x," and don't persist across a reload - session-only, matching every other transient UI state already in the app.
- ~~T265.~~ A shareable, read-only "anyone with the link" view of Updates, for a partner. **Withdrawn by the user 2026-08-04**: "remove share read only access with a partner." (T259, full read/write partner sharing on the same account, is a different feature and stays planned.)
- [x] **T266.** A budget's start date when income-linked - resolved as the cheaper, non-behavior-changing option. Default/display the linked income's own start date on the budget's create/edit form; no change to how replenishment projects (still every one of the linked income's occurrence dates, unconditionally). The heavier option - a real floor-date filter - is explicitly deferred as its own follow-up if this turns out insufficient, not guessed at now. Built 2026-08-04: `BudgetModal.tsx`'s income-linked frequency line now also shows "Starts {date}" when the selected income has one. tsc/tests/build/eslint clean.
- ~~T267.~~ Offline budget-log entry. **Withdrawn by the user 2026-08-04**, same "stay as a web app" instruction as T257/T258.
- [ ] **T268.** Account-wide deletion/reset completeness audit. A concrete audit, not a discussion: enumerate every table with a `user_id` column against `wipeFinancialData.ts`'s `FINANCIAL_DATA_TABLES` list, confirm `budget_accounts`/`budget_budget_accounts`/`budget_account_transactions`/`balance_transactions`/`income_auto_moves` are each either listed or genuinely cleaned up via FK cascade, add whatever's missing, and add a check (a test, or a documented manual step) so a future new table can't silently repeat this gap.
- [ ] **T269.** Username/password signup without email, for privacy-conscious users. Raised 2026-08-04 (REMINDER.md). Supabase Auth has no native "username only" identity, so the shape is a synthetic internal email generated from the chosen username (e.g. `username@users.orium.internal`), used only as Supabase's own identity key and never actually emailed to - the user only ever sees/enters their username. Real, stated-plainly tradeoff: no real email means no password-reset-by-email either, so this path needs its own recovery story (a security question, a printed recovery code at signup, or an explicit "we cannot help you recover this account" acceptance) - not solved by this plan, flagged as the one open question before it's buildable. These accounts are excluded from T239's email notifications, exactly as the user's own framing already expects. Needs a new `preferences` (or `auth`) flag distinguishing username-only accounts from email accounts, since several existing flows assume a real email exists.
- [x] **T270.** An in-app support contact: njcorpuz01@gmail.com. Raised 2026-08-04 (REMINDER.md). A plain "Found a bug or have a feature request? Email njcorpuz01@gmail.com" line added to Settings (near the existing account-management actions) - a `mailto:` link, no new infrastructure. Built 2026-08-04.

### Phase 48 - REMINDER.md batch, planned 2026-08-04 (not yet built)
Raised after Phase 47 above was already underway - same instruction applies (plan only, no execution). One item here (T271) is important enough to flag on its own: it reopens Bug #24/#25 with a cheaper fix than the domain-purchase path that session already deferred.

- [x] **T271.** Custom, app-owned signup verification via nodemailer + Gmail - closes Bug #24/#25, and brings back a real code-entry flow. "Now that we're going to use nodemailer, we can easily implement the code stuff don't we?" Rather than routing Gmail through Supabase's own SMTP settings (still bound to Supabase's own email templates and OTP mechanics), the app owns the whole flow directly, sidestepping Supabase Auth's mailer entirely:
  - `signup()` (`src/app/auth/actions.ts`) creates the user via the **admin API** (`createAdminClient()`, `admin.auth.admin.createUser({ email, password, email_confirm: true })`, same pattern `/api/dev-new-account` already used) rather than the anon client's `signUp()` - this alone stops Supabase from sending its own confirmation email at all, since the account is created already-confirmed in Supabase's own eyes. Immediately signs in via `signInWithPassword` to establish the real session, same as before.
  - The app then generates its own 6-digit code (`src/lib/auth/signupVerification.ts`), upserts it into a new table (`signup_verification_codes`, migration 0053: one row per user, `unique(user_id)`, own-row RLS) with a 15-minute expiry, and emails it via `nodemailer` through Gmail SMTP (`smtp.gmail.com:587`, STARTTLS) - a real new dependency (`nodemailer` + `@types/nodemailer`), explicitly authorized by the user naming it directly.
  - `preferences.pending_email_verification` (migration 0053, default `false` so every existing user is unaffected) gates real app access - `middleware.ts` redirects anywhere else to `/verify-email` while it's `true`, and redirects away from `/verify-email` once it's `false`. New page `src/app/verify-email/page.tsx` (outside the `(app)` route group, matching `/login`/`/signup`) holds the code-entry form plus a "Resend code" action and a logout escape hatch (a user reaching this page has no sidebar to log out from otherwise). `verifySignupCode()` checks the submitted code against the stored one and expiry, clears the flag, and redirects to `/`.
  - "Resend" (`resendSignupVerificationCode()`, renamed again from `resendSignupEmail`) regenerates a fresh code (upsert overwrites, not a second row) and re-sends via the same nodemailer path.
  - **Migration 0053 applied to both projects** (production `fptpkrbfbtfhzxbtmgic` and staging `gwvnqvampbztirzqjuaq`). 281/281 tests (unchanged - server actions/middleware, no engine change), tsc, build, eslint clean.
  - **Needs from the user before this can be tested live**: a **Gmail App Password** for `njcorpuz01@gmail.com` (Google Account → Security → 2-Step Verification → App Passwords), set as `GMAIL_APP_PASSWORD` alongside `GMAIL_USER=njcorpuz01@gmail.com` in `.env.local` and Vercel's project settings. Without it, `sendSignupVerificationEmail` fails gracefully with a clear "GMAIL_USER/GMAIL_APP_PASSWORD are not set" error (same fail-safe convention T239's Resend integration already established) rather than crashing signup. Gmail's own sending limits (~500/day) comfortably cover a beta; personal Gmail accounts sending automated mail can trip spam filters at higher volumes, worth revisiting before a public launch rather than at beta scale. **Not yet browser-verified end-to-end** (no live send possible without the App Password) - verified instead via a clean build/type-check and a careful manual re-read of the full flow (admin creation → sign-in → code issue → middleware gate → verify → clear flag) against the exact patterns `/api/dev-new-account` and T239 already established and proved working.
- [x] **T272.** A "take-home" figure on the Dashboard. Raised: "Need to know how much is my take home every month in the dashboard." Reads as net monthly cash flow - Total Monthly Income minus Total Monthly Bills/Debt/Savings/Budgets, the same `monthlyEquivalent` figures the existing per-category cards (T196/T219) already compute, just summed into one net number rather than shown as separate cards only. Built 2026-08-04: a sixth "Take-home (est.)" card in the same Monthly totals grid, red when negative.
- [ ] **T273.** A capacity/scaling audit for the free-tier infrastructure. Raised: "Need to know how to what limits to implement in the feature so as to no[t] bloat the database or traffic." Not a feature - an audit task: check current Supabase free-tier limits (database size, row counts, monthly active users, API request volume, egress) against actual measured usage per account (this session's own read of the production account is a real data point to start from), and produce real numbers for the "50 concurrent users" ceiling the Beta launch strategy discussion already flagged as its single riskiest assumption - load-checked rather than assumed, per that discussion's own open question.
- [x] **T274.** Accounts page: rename the "Amount" label to "Running balance." Raised: "Change the wording in accounts from 'Amount' to 'Running balance' to make sure the user understands that's the running balance." A label-only change on `BalanceModal.tsx`. Built 2026-08-04.
- [x] **T275.** Account creation's transaction-fee helper text should mention it's still overridable later. Raised: "Indicate in the transaction fee helper of the account when creating one that they can still update the transaction fee when editing/settling a forecast transaction." A copy addition to the existing `FormTip` on that field, referencing T194's per-settlement override and T250's per-move override. Built 2026-08-04.
- [ ] **T276.** Bulk settle in the Forecast table. Raised: "need to be able to bulk settle in the forecast table, so in case a user gets back to the tool 2 weeks or 2 months after... they can update their forecast quickly." A real feature for the past-due backlog specifically (T150's carry-forward already keeps every unsettled occurrence visible, which is exactly what would pile up over an absence) - a multi-select on past-due rows plus a "Settle selected at forecasted amount/date" bulk action, reusing the existing single-row `settleOccurrence` action per selected row rather than a new bulk-write path, so every settlement still gets its own real row/log entry.
- [x] **T277.** Dashboard: Lowest Balance Ahead and Peaks and Drops move to the top by default. Raised as part of the "focus the whole app on enter → forecast → review → keep clean" framing. `DashboardWidgetsPanel`'s existing reorder/hide mechanism (T117/T118) already supports this - just a new default order for an account that's never customized it; anyone who's already reordered their own Dashboard keeps their own choice, unaffected. Built 2026-08-04: moved both widget definitions ahead of Total Balance/Monthly totals/Misc totals in `page.tsx`'s `widgets` array.
- [x] **T278.** Forecast page: bigger Total balance figure, bold account/balance chips. Raised in the same framing paragraph - a visual-weight pass on the page's own header, no behavior change. Built 2026-08-04.
- [ ] **T279.** Filter-button consistency check across Bills/Income/Debt/Savings/Budgets/Misc. Raised: "Make the Filter options in Bills/Income/Debt/Savings/Budgets/Misc hidden in a Filter button, like in the Forecast Page." Likely **already substantially done** - T231 (Phase 38) already built `CollapsibleFilters.tsx` and applied it to exactly this page list. Needs a real look before assuming a gap: Forecast's own filter is a full modal (T70), while T231's pattern is an inline collapsible panel - if the user means "a modal like Forecast's," that's a real, different task; if they mean "collapsed by default," T231 likely already covers it. Flagged rather than guessed at, since a confident wrong assumption here would mean redoing already-done work.
- [x] **T280.** Recurrence form: picking "Ends after N occurrences" and typing a count should show the resolved end date live, in the form itself. Raised as part of the same framing paragraph. T237 (Phase 40) already shows this on the saved item's own summary row after the fact - this extends the same `ruleEndDate()` calculation into the live form, before saving, so the date is visible while the user is still picking the count. Built 2026-08-04 in `RecurrencePicker.tsx` - reuses `ruleEndDate()` (`engine/remaining.ts`) directly, no duplicated date math.
- [x] **T281.** Account edit form: relabel the transaction-fee field "Default transaction fee." Raised in the same paragraph - clarifies (alongside T275 above) that it's a fallback, not a fixed, unchangeable cost. Built 2026-08-04.
- [ ] **T282.** Rename "Savings" to "Goals," and add "Goal accounts" mirroring "Budget accounts." The largest item in this batch. Verbatim: "Change 'Savings' into 'Goals', then create a 'Goals accounts' similar to the 'Budget accounts'... a goal can have its own Goal Account where the amount will go to when a goal forecast transaction is settled... make sure goal accounts have add take and move funds as well... goal items indicate which goal accounts they belong to, goal accounts can have multiple goal items, and goal items can have multiple goal accounts similar to budgets." Plan: a rename-only pass first (every "Savings" label, route, and `type` value's *display* text - not the underlying DB enum value, to avoid a destructive migration for a cosmetic rename), then a genuinely new `goal_accounts`/`goal_goal_accounts` table pair structurally mirroring `budget_accounts`/`budget_budget_accounts` (T204/T218) exactly, including Add/Take/Move funds (T209's pattern) and the same many-to-many, proportional-share replenish-on-settle logic. Comparable in size to the original Budget Accounts build (T204/T209/T218 combined) - plan as its own multi-task phase when actually built, not a single sitting. **Stale as written, per T284 below (2026-08-08): "Budget accounts" as a mirror target no longer exists once T284 ships** - budgets link directly to regular `balances` rows instead. If/when this is actually built, "Goal accounts" should follow T284's unified-account shape (a `used_for` tag on `balances`, not a second dedicated table) rather than the `budget_accounts` structure this bullet still describes verbatim.
- [ ] **T283.** "Balance on a future date" lookup. Raised in the same paragraph: "...how much money they'll have at any day in the future including a breakdown of all their accounts... they'll be prompted to choose a date... a breakdown of their total balance across all accounts at that point in time with a helper text that it doesn't include total balances in goals and in budgets." A new button/modal on the Forecast page: pick any future date, see the combined total plus a per-account breakdown as of that date - computed by walking the existing `forecast` rows up to the chosen date (the same data `computeAccountBalancesAfterEachRow`/per-row `runningBalance` already produce, just queried at an arbitrary date rather than per-row-click) with an explicit note that budget/goal accounts (T282) are out of scope for this figure, matching how they're already excluded from Total Balance everywhere else. **Stale as written, per T284 below (2026-08-08):** budget accounts are no longer excluded from Total Balance by default (T284 makes that opt-in via the "Cash Flow Only" toggle) - this feature's own helper text/scope should follow whatever T284 lands on, not the old always-excluded assumption.

### Phase 49 - Account model redesign: one account category, replenishment as auto-move, a "Cash Flow Only" toggle (raised live in chat 2026-08-08)

- [x] **T284.** Collapse "budget accounts" into ordinary accounts; budget replenishment becomes a real inter-account transfer (mirroring `income_auto_moves`); a "Cash Flow Only" toggle on Forecast and Peaks and Drops excludes budget-earmarked money on demand. Raised live in chat, verbatim: "remove the budget accounts, I want just one accounts category, just the main accounts. The budget accounts can now be moved to those main accounts, and the replenishment logic works like an 'auto-move'. I just want a toggle in the forecast and peaks/drops views for 'Cash Flow Only' which will exclude any running budget amounts when toggled on." Resolves REMINDER.md's "need to distinguish all types of accounts clearly... maybe we can just assign categories to accounts" and "I want to redesign accounts. I want to have just one accounts category. There will be no distinction between budget/cash flow/savings accounts anymore" (both logged below in this same triage pass). **Built out of order, ahead of the topmost unchecked roadmap item (T240), per the user's own explicit request this session** - same precedent as T204/T236/etc. **One piece of the original ask is explicitly deferred, not forgotten**: the per-account interest-rate feature ("I want the accounts to have an interest feature, where I can put a % number... the balance updates depending on the base interest and when the interest actually gets deposited") is a distinct, non-trivial feature (needs a compounding/deposit-schedule model) - not in scope for this task, logged separately below as its own "Before MVP launch" discussion item.
  - **Two design questions resolved with the user before writing code** (AskUserQuestion, 2026-08-08): (1) "Cash Flow Only" defaults **off** - everything counts unified by default, matching "just one accounts category"; toggle on to see the narrower, pre-T284-equivalent view. (2) a budget account is identified by an **explicit tag** on the account (`balances.used_for_budgets`), settable on any account independent of whether a budget currently links to it - not inferred from live links, which would silently change what's excluded whenever a link is added/removed.
  - **Backed up first**: real money in `budget_accounts` (4 rows)/`budget_budget_accounts` (10)/`budget_account_transactions` (8) was snapshotted to a local JSON file before the migration touched anything, given this is a destructive schema change against production data with real balances.
  - **Migration** (`supabase/migrations/0054_collapse_budget_accounts_into_balances.sql`, applied to both `fptpkrbfbtfhzxbtmgic` and `gwvnqvampbztirzqjuaq`): `balances` gains `used_for_budgets boolean not null default false`. The 4 real `budget_accounts` rows were copied into `balances` **re-using their existing ids** (so every `budget_budget_accounts`/`budget_entries`/`settlements` row that already pointed at one of those ids needed no data rewrite, only its FK constraint retargeted), flagged `true`. `budget_budget_accounts.budget_account_id` and `budget_entries.budget_account_id` renamed to `balance_id`, refiring their FKs at `balances`. `scenario_budgets.budget_account_id` renamed to `balance_id` the same way. `settlements.budget_account_id` was dropped outright rather than repointed - that table already had its own `balance_id` column (T217), so budget settlements now just write into the one column every other settlement type already uses, closing a redundant second dimension. `budget_account_transactions` (same shape as `balance_transactions`, just the account-id column named differently) was folded into `balance_transactions` id-for-id, then dropped. `budget_accounts` itself dropped last, once nothing referenced it.
  - **Engine** (`src/lib/engine/`): a budget's replenishment now pushes a **hidden credit-leg row** into each of its linked `balances` (split proportional to each link's `replenish_amount` share, via `splitAmountByShares` - moved from `src/lib/budgetSplit.ts` into the engine since the engine now needs it too, not just the settle-time action) - the same paired-row shape `income_auto_moves` already established for real inter-account transfers, and literally what "works like an auto-move" means here. The existing visible debit row (income-linked: attributed to the income's own connected account; own-schedule: unattributed, exactly as before T284) is unchanged - it still reduces the combined total normally. No explicit zero-contribution treatment was added for the new pair (unlike `income_auto_move`'s own Bug #19 fix): the debit and credit(s) naturally net to the same combined total regardless of order, since (unlike Bug #19's case) nothing else in this codebase reads an intermediate same-day balance for a *budget* row specifically - accepted as a narrower, documented cosmetic gap rather than replicating that whole fix for a code path with no live report against it yet. New `src/lib/engine/cashFlowFilter.ts` (`filterCashFlowOnly`) is a pure post-processing pass over an already-computed forecast - not a second engine run - dropping every `usedForBudgets` account's starting balance and every row whose `balanceId` belongs to one, then recomputing the running total. `budget_entries` (and their forecast rows) now carry `balanceId` for the first time, closing a real gap: a future-dated spend/manual-add against a budget-linked account previously had no account attribution in the forecast at all.
  - **Server actions**: `writeBudgetReplenishLegs` (`forecast/actions.ts`, shared by `settleOccurrence`'s income branch and `settleBudgetReplenish`) now credits real `balances` rows via `applyToBudgetAccount` (repointed to `balances` by this task, still no fee - this is bookkeeping against the user's own money, not an external settlement) instead of the old separate-storage path. `src/app/(app)/budgets/actions.ts` lost `createBudgetAccount`/`updateBudgetAccount`/`deleteBudgetAccount`/`addBudgetAccountFunds`/`takeBudgetAccountFunds`/`moveBudgetAccountFunds` entirely (~330 lines) - a budget-linked account is just an account now, so its CRUD and Add/Take/Move funds go through the existing `accounts/actions.ts` (already reachable from both `/accounts` and `/forecast` per T249) rather than a parallel implementation.
  - **UI**: the Budgets page's whole "Budget Accounts" management sub-section is gone (`BudgetAccountsSection.tsx`/`BudgetAccountModal.tsx`/`BudgetAccountFundsModal.tsx` deleted) - a budget's "connected account(s)" picker now sources from the regular accounts list. The Accounts page (and the Forecast page's balance chips, which share `BalanceModal.tsx` per T152/Bug#12) gained a "Used for budgets" checkbox on create/edit and a small pill badge on a tagged account, matching the existing "Auto-debited" pill's styling (T232). Forecast and the Dashboard's Peaks and Drops card each gained a "Cash Flow Only" toggle (client-side only, both datasets precomputed server-side in `loadForecast()` so switching is instant, no reload) - both toggles only render at all once at least one account is actually tagged, so they don't show as dead controls for a user with none.
  - **Two pre-existing gaps found and fixed while building this** (not part of the ask, but directly exposed by it): `sampleFixture.ts`'s preview-mode data was typed as `ForecastData` but missing the new required fields entirely, which would have broken preview mode outright; and a real runtime bug in `activateScenarioPermanently` (`scenarios/actions.ts`) - its `budget_budget_accounts` insert was still writing the pre-migration column name, which would have thrown the first time anyone activated a scenario with a linked account.
  - 289/289 tests (10 new: forecast.ts's paired credit-leg generation across single/multi-account/own-schedule/filtered-destination cases, and cashFlowFilter.ts's own filtering behavior), tsc, build, eslint at the same pre-existing 6-problem baseline. Verified against real production data via direct SQL (the 4 migrated accounts landed correctly, same ids, flagged) and against the (separately synced, so not fully representative of every account) staging project in the browser - Forecast/Budgets/Accounts pages all render real data with no console errors, budget rows show the expected "Connected to account" attribution and account pill; staging's own account set predates the 4 migrated accounts (STAGING.md's sync is on-request only), so the toggle's own visible effect on real numbers wasn't directly eyeballed this session - the underlying filter is unit-tested instead.
  - **Redesigned twice more the same day, both follow-ups raised live in chat.** First: "how will this behave if i have an account that is used for both budgets and cash flow?" - the shipped version excluded a *whole* flagged account whenever the toggle was on, hiding a mixed-use account's ordinary bills/income too. Fixed toward excluding budget *money* rather than whole accounts (row filter became "any `budget_replenish`/`budget_entry` row, wherever it lives"; starting-balance filter became "subtract only each account's budget-attributed portion" via `computeBudgetAccountBalance`, T222). This intermediate design is itself superseded by the second follow-up below - kept here only as the record of how the thinking evolved.
  - **Second follow-up, the same day, changed the feature's whole shape**: "when I say 'exclude budgets' i mean when its turned on the forecast will assume that those budgets are fully consumed... I want a slider for each budget, controllable from the forecast and peaks/drops." The binary toggle (a single "Cash Flow Only"/"Exclude budgets" switch for the whole account) is **replaced entirely** by a per-budget continuous assumption, resolved via two more questions asked before writing code: build a slider per budget rather than one global toggle (confirmed), and default new/existing budgets to **100%** ("bills-like," matching the user's own stated habit) rather than 0%.
    - **Migration** (`0055_budget_assumed_spend_percent.sql`, both projects): `budgets.assumed_spend_percent smallint not null default 100`, 0-100 checked.
    - **Engine**: `cashFlowFilter.ts` is deleted outright - there's no longer a second "toggle" dataset, since the assumed-spend math is baked directly into `generateForecast`'s own `runningBalance`. The tail of `generateForecast` (from the starting-balance line through the final row-map reduce) is extracted into two new exported functions: `computeAssumedAvailableStartingBalance` (today's account totals, minus each *linked* budget's already-happened ledger balance at its own percent) and `computeRunningBalances` (the same math walked forward through every row). Both are reused unchanged by the client-side sliders for instant, no-reload feedback while dragging - `ForecastClient.tsx`/`PeaksAndDropsCard.tsx` re-run just this tail (cheap - every row's amount/sourceType/sort order is already final) rather than the whole engine. The actual per-row math: a `budget_replenish` debit row's contribution becomes `amount * (1 + percent/100)` and a `budget_entry` row's becomes `amount * (1 - percent/100)` - together with the credit leg's unchanged full contribution, this exactly cancels to `-totalAmount * percent/100` net effect per replenish event, and exactly zeroes out a *later* actual spend against an already-100%-assumed budget (it was already priced in at replenish time - confirmed by its own test). **One real edge case, decided deliberately**: a budget with no connected account at all has nowhere for a discount to visibly land, so its plain unattributed deduction stays exactly as it was pre-T284 (fully deducted, unconditionally) regardless of its own percent slider - moving that budget's slider has no effect until it's linked to an account. Documented in-code and covered by its own test rather than silently inconsistent.
    - **Server action**: `updateBudgetAssumedSpendPercent(budgetId, percent)` (`budgets/actions.ts`) - called directly (not FormData/`useActionState`, since a slider fires many times while dragging), debounced client-side. No `logActivity` call - a personal display assumption, not a financial record; logging every drag tick would spam Updates.
    - **UI**: new shared `src/components/BudgetUsagePanel.tsx` - one modal, reused from both Forecast and Peaks and Drops per the user's explicit "controllable from the forecast and peaks/drops," listing every budget with its own 0-100 range slider. Both pages' own toggle buttons are replaced with a "Budget usage" button opening this same panel. The Forecast page's "Total balance" header and the Dashboard's "Total Balance" card both now show the assumed-available (discounted) figure instead of the raw account sum, for consistency between the two pages. The earlier "Total remaining budget" line (added in the first follow-up, unrelated to this - the budget's own raw ledger total) is unaffected and stays.
    - `groupPeaksAndDropsByYear` moved from a local, unexported helper in Dashboard's `page.tsx` into `engine/peaksAndDrops.ts` (exported) so `PeaksAndDropsCard.tsx` (a client component) can re-group its own live-recomputed data the same way the server does by default.
    - 290/290 tests (the T284-follow-up describe block rewritten with explicit percents at every test - 0%, 100%, a 40% partial case, the no-double-count case, the unlinked-budget edge case, and the already-happened-activity starting-balance case), tsc, build, eslint clean.
    - **Browser verification was only partial this session, stated plainly rather than glossed over**: the Forecast page's new "Total remaining budget" line and "Budget usage" button both confirmed rendering correctly against real (staging) data with no console errors. Actually opening the slider panel via simulated clicks did not visibly succeed in this session's Browser pane - console showed no errors and the DOM structure around the button looked correct, but the modal never appeared even after a hard reload, most likely the same documented `document.visibilityState`/rAF-freeze issue (Operations section) now also blocking React's post-hydration re-render commit, not just the initial reveal it was already known to block. Not independently confirmed as the cause this session. The underlying math is unit-tested thoroughly instead; the slider's own interactivity should be spot-checked by the user in a real, visible browser tab before relying on it.

### REMINDER.md batch, triaged 2026-08-08 (not yet built unless noted)

- [ ] **T285.** Realistic sample data: one full-time income source plus one side-hustle income, tuned so the balance first goes negative roughly 3 months after the data is generated. Raised: "fix the sample data, make it realistic, reflecting just one income source for full time work, and another for a side hustle, then the goes negative should be around 3 months from the time the data was initiated to be created." Touches `supabase/seed.sql` and/or `sampleFixture.ts` (T169's known-stale-dates follow-up is the same file family - worth fixing both in one pass).
- [ ] **T286.** Nav order: move "Income" above "Bills" in the sidebar. Raised: "income should be above bills in menu."
- [ ] **T287.** Dashboard monthly-breakdown card cleanup: replace the "Manage" text button with a right-arrow icon button, linking to that card's own category page (Total Monthly Bills to Bills, etc.), and extend the same right-arrow "more" pattern to Accounts/Bills/Income/Debt/Savings/Budgets/Misc In/Misc Out cards. Raised: "make the UI of total monthly breakdowns in dashboard visually easy on the eyes, they're clunked together in 6 columns one row, instead of 'Manage', it should be a right arrow button instead, and add that in the monthly breakdowns too... Accounts, bills, income, debt, savings, budgets, misc in, misc out should have their 'more' button which is the right arrow icon." Also asks for a general visual declutter of the 6-column monthly-totals grid - needs a real layout look, not just the button swap.
- [ ] **T288.** Auto-move destination picker shouldn't offer an income item as a valid "add to" target. Raised: "auto move options should remove the selected added to field for income because that should not be possible as that doesn't make sense." Needs a closer look at `income_auto_moves`' destination picker at build time to confirm exactly what's being offered that shouldn't be.
- [ ] **T289.** Rename "Total monthly" to a per-category label ("Total monthly bills," "Total monthly income," etc.) everywhere it appears, not just Bills. Raised: "add total monthly bills instead of just total monthly in the bills page, and apply this to all the other categories." Same pattern T234 already used for Budgets' own monthly-estimate line.
- [ ] **T290.** Budgets page wording: "Total across all budgets" to "Available budget balance." Raised verbatim. **Re-check against T284 once built** - the underlying figure this label describes may itself change shape under the new account model, so confirm the label still fits what's actually being displayed before just swapping the text.
- **Scenario item edits with a hypothetical start date (Before MVP launch, needs discussion).** Raised: "allow to switch off/edit current financial items in the scenario, allow to edit current financial item in a way where if i have an income set up permanently i can edit it in the scenario to change its amount anytime in the future like a hypothetical start date only for that amount change of the current actual active income." A real architecture question, not a small edit: scenarios today only ever add new hypothetical items (`scenario_recurring_items` etc.), never reference or override an existing real item - this asks for the opposite, a scenario-scoped override of a real item's future value from some hypothetical date forward, without ever touching the real row. Needs its own design pass (closest existing precedent: `occurrence_overrides`' per-occurrence shape, but this is a *from-date-forward* override, not a single-occurrence one) before it's buildable.
- [ ] **T291.** Audit scenario activation/deactivation events for Updates coverage. Raised: "need to show any and all scenario activities in the updates." Scenario item CRUD (bills/income/debt/savings/misc, T226; budgets, T221) already logs to `activity_log` - this needs a check specifically on the *activation* lifecycle (`toggleScenarioActive`, "Activate permanently," delete) to confirm those log too, closing whatever gap remains rather than assuming full coverage.
- **CSV import for onboarding (amends T256, not a new task).** Raised: "need to implement csv import option for easier onboarding." T256 (Phase 47) already scopes import as JSON-only *restore* (wipe-and-replace) - this specifically asks for CSV as an *import* format too (T256 only planned CSV as a one-way, non-reimportable export). Note left on T256 for whoever builds it: decide then whether CSV import is worth the extra parsing/validation surface over JSON-only, or whether a "guided CSV mapping" step is what "easier onboarding" actually needs.
- **Account interest-rate feature (Before MVP launch, needs discussion).** Split out of the account-redesign request above (see T284) rather than bundled into it. Raised: "I want the accounts to have an interest feature, where I can put a % number on there that indicates the interest rate (i.e. Maya Savings has 3% base interest, but grows up to 10%). The balance on there also updates depending on the base interest and when the interest actually gets deposited into the account. For example, with Maya savings, it gets deposited daily, so every day the interest arrives." A genuinely new, non-trivial modeling problem: a compounding schedule per account (daily/monthly/etc.), a tiered-rate concept ("3% base... grows up to 10%" - unclear yet whether that means balance-tiered, promo-tiered, or time-tiered), and how projected-but-not-yet-posted interest should show in the Forecast (a recurring synthetic income row? a running adjustment to the account's own projected balance?). Needs the user's own answers before it's scoped, not guessed at.
- **Two "excluded from overall available balance" wording items, superseded by T284.** Two REMINDER items - "Forecast total balance wording update: make it Overall available balance (excluding budget and goals accounts)" and "need to include helper text in budget and goals accounts to say 'excluded from overall available balance'" - both assumed the old always-excluded-by-default model. T284 replaces that default with an opt-in "Cash Flow Only" toggle, so neither wording change applies as originally phrased; not converted into separate tasks; revisit copy/labeling once T284's actual toggle UI exists.

### Cloud sandbox sessions
The user occasionally opens a separate cloud-hosted session (e.g. from a phone) that works on its own git branch in an isolated sandbox, distinct from every local PC session above. Work done there is invisible here until/unless that branch is pushed and merged - `git branch -a` shows only `main` as of 2026-08-01, so no cloud-sandbox branch has been merged into this repo yet. If the user pastes a transcript from one of those sessions, treat it as a real, actionable request the same as anything said directly - just verify any "this already exists" claims against the actual current `main` branch before trusting them, since the cloud sandbox's own view of the codebase can be stale or entirely separate.

### Out of scope
Payments/subscriptions, mobile app, ~~notifications~~ phone notifications, bank sync, multi-user families.

**Notifications is now split.** *Email* notifications are explicitly lifted as of 2026-08-03 (T239, Phase 42) - the user gave direct go-ahead in chat rather than the exclusion being eroded unilaterally. *Phone* (push) notifications remains excluded and untouched - a genuinely different delivery channel with its own infrastructure question (a mobile push service, not the email stack T239 stood up), still needing its own explicit decision.

**The other three of the original five are still actively contested and this list is still out of date (first noted 2026-07-31, worsened 2026-08-02).** *Multi-user families* is contested from three directions: the partner-sharing item in "Before MVP launch", the shareable no-login Updates link, and T163's updates feed itself, which is deliberately scoped to a single shared account precisely so it does not cross this line. *Mobile app* is contested as of 2026-08-02 by the mobile-app-with-local-option item above, and separately by the PWA path under "Offline budget-log entry" - though a PWA arguably never crossed this line at all, since it is the existing web app rather than a second one. *Payments/subscriptions* is contested by the beta launch strategy's own step 6, which explicitly ends in building a subscription setup with plans and pricing - so the exclusion is contradicted by the user's own stated plan rather than by a feature request.

Only *bank sync* is still fully uncontested. Nothing here should be treated as settled without the user saying so; the remaining exclusions stay in force until then, but at this point the list is doing more harm than good as written and should be revisited as a group - one sitting, the rest of it - rather than eroded one task at a time.
